# configure.py replaces this email with the approved Keycloak user's email.
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: platform-operator
spec:
  profile:
    email: operator@example.internal
  memberOf: [platform-team]
