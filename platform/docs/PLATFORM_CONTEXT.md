# PLATFORM_CONTEXT — "Şu an neredeyiz?"

> **Bu dosyanın amacı:** Platformun anlık durumunu tek bakışta veren, canlı
> takip dosyası. Her fazın sonunda **güncellenmesi zorunludur**. Yeni katılan
> biri (veya yeni bir oturum) önce bu dosyayı okur.
>
> **Kural:** Bir bileşen, yalnızca kümede çalışıyor ve doğrulandıysa
> "Kurulu bileşenler" tablosuna girer. Repoda YAML'ı olması yeterli değildir.

> **BEKLEYEN GÖREV — Faz 13 (multi-cluster hub-spoke), GATE'TE BEKLİYOR:**
> Bir "hub-spoke multi-cluster" görevi (provider-kubernetes uzak
> ProviderConfig + ArgoCD ApplicationSet cluster generator + spoke-başına
> Vault Intermediate CA + Cilium ClusterMesh tasarımı + Tenant XRD'sine
> `clusterRef` eklenmesi) teklif edildi. Görevin KENDİSİ açıkça "sadece
> tek-cluster kurulum kanıtlandıktan/stabil olduktan SONRA başlat" dedi.
> Bu koşul HENÜZ KARŞILANMIYOR (aşağıdaki "Küme durumu" satırına bakın —
> hub hâlâ hiçbir gerçek bare-metal cluster'a uygulanmadı, yalnızca Faz
> 11'de kind tabanlı, kapsamı daraltılmış bir e2e kanıtı var). Kullanıcıya
> soruldu; **kullanıcı "önce hub'ı gerçek kur" seçeneğini seçti** — bu
> görev BİLEREK YAPILMADI, Bölüm 2 satır 2b (`01-underlay.sh`
> çalıştırılması) tamamlanıp hub stabil çalışana kadar ERTELENDİ.

| | |
|---|---|
| Son güncelleme | 2026-09-15 |
| Güncelleyen | Platform Ekibi |
| Aktif faz | **Faz 12 — "Tüm eksiklikleri tamamla": tüm açık kararlar (Bölüm 3) çözüldü, çoğu teknik borç (Bölüm 4) koda döküldü, GERÇEK bir kind cluster'ında (Vault+cert-manager+CNPG+Crossplane 2.4.0+Kyverno+Velero+MinIO) hem Tenant→Postgres→bağlantı→cert zinciri HEM DE tam bir DR tatbikatı (backup→disaster→restore) yeniden ve daha derin kanıtlandı. 2 YENİ kritik, üretim-engelleyici bug bulundu ve düzeltildi (bkz. aşağıda). Bare-metal donanım/organizasyonel gerçekler (IP havuzu, gerçek isimler, DNS) bu ortamda GERÇEKTEN tamamlanamaz — açıkça listelendi, gizlenmedi.** |
| Küme durumu | **Faz 1-3, 6, 6b, 7, 7c, 8, 9, 10, 11 ve 12 kaynakları yazıldı; GERÇEK bare-metal cluster'a hâlâ uygulanmadı (bu, bu ortamın yapısal bir sınırıdır). Faz 12: kind tabanlı e2e + chaos + DR ispatları GENİŞLETİLDİ ve TEKRARLANDI — composition'lara eklenen ESO/OIDC/cert-manager değişiklikleri dahil TÜM zincir yeniden yeşil.** |

> **KRİTİK BULGULAR (Faz 11) — üçü de bu fazda düzeltildi:**
> 1. `versions.env`'in `CROSSPLANE_CHART_VERSION="1.18.0"` pini, bu
>    repo'nun compositions'larının ürettiği namespaced kaynaklarla
>    (`resourceRefs[].namespace: field not declared in schema`)
>    ÇALIŞMIYORDU — HER tenant/postgres provizyonu canlıda KALICI OLARAK
>    başarısız olurdu. **2.4.0'a yükseltildi.**
> 2. `platform/compositions/postgresql/function.k`: `certificates.
>    serverCASecretName` alanı CNPG'nin gerçek CRD şemasında YOK
>    (doğrusu `serverCASecret`) — **düzeltildi.**
> 3. `platform/compositions/tenant/function.k`: `tenant-issuer`, var
>    olmayan bir `cert-manager` ServiceAccount'una Vault auth için
>    referans veriyordu — Issuer HİÇBİR ZAMAN Ready olamazdı. **Composition'a
>    ServiceAccount + token-mint RBAC eklendi.**
>
> Üçü de yalnızca GERÇEK bir cluster'a karşı reconcile ederek (bu fazda
> İLK KEZ yapıldı) bulunabilirdi — `crossplane render`/`kclvm_cli`/
> `kyverno test` (önceki fazların yöntemi) bunları YAKALAYAMAZDI. Bkz.
> `tests/e2e/kind-chain/README.md`.

> **Faz numarası notu:** Görev metni bir faz numarası VERMEDİ (yalnızca
> "görev" dedi). Roadmap sırası (Faz 9 son tamamlanan yazım fazıydı) takip
> edilerek bu iş **"Faz 10"** olarak numaralandırıldı — önceki her fazın
> tutarlı numaralandırma pratiğiyle UYUMLU tutmak için.

> **ÖNEMLİ SAPMA — Faz numarası/yerleşim:** Görev "Faz 9 — Gözlemlenebilirlik"
> olarak verildi ve teslimi `platform/underlay/observability/*` olarak
> istedi. İkisi de mevcut roadmap ile ÇELİŞİYOR:
> 1. **Katman:** ADR-0001'in 7 katmanlı mimarisinde gözlemlenebilirlik L4
>    (control-plane)'dir, L2 (underlay) DEĞİL — Faz 0'da zaten
>    `platform/control-plane/observability/` iskeleti (boş) açılmıştı. Bu
>    görev o iskelete yazıldı, literal görev metnine değil.
> 2. **Sıra:** Bölüm 2'nin eski roadmap'inde gözlemlenebilirlik Faz 4'ün
>    (satır 5), OpenCost ise Faz 9'un (satır 11, Velero ile birlikte) bir
>    parçasıydı. Bu görev ikisini BİRDEN, Faz 4/6/6b/7/7c/8'İN ÖNÜNE GEÇEREK
>    "Faz 9" adı altında teslim etti — Velero + felaket kurtarma tatbikatı
>    (eski Faz 9'un GERİ KALANI) HÂLÂ YAPILMADI. Bölüm 2 tablosu bu çelişkiyi
>    yansıtacak şekilde güncellendi (satır 5 ve 11 notları + yeni satır 11a).

---

## 1. Kurulu bileşenler

> **DİKKAT — bu tablodaki bileşenler HENÜZ KURULU DEĞİL.**
>
> Bu dosyanın kuralı: *"Bir bileşen, yalnızca kümede çalışıyor ve doğrulandıysa
> bu tabloya girer. Repoda YAML'ı olması yeterli değildir."*
>
> Aşağıdaki 9 satır (Faz 1: 5, Faz 2: 4), kurulum yapılacağı zaman doldurulmak
> üzere **`planlandı` durumunda** hazırlanmıştır. Manifestler ve doğrulama
> komutları yazılmıştır; **`01-underlay.sh` ve `02-control-plane.sh` henüz
> hiçbir cluster'da çalıştırılmamıştır.**
>
> Kurulumdan sonra doldurulacak sütunlar: **Sürüm** (gerçek, `helm list`
> çıktısından), **Kurulum tarihi**, **Durum** → `çalışıyor`.

| Katman | Bileşen | Sürüm (pin) | Namespace | Kurulum tarihi | Durum | Doğrulama | Not |
|---|---|---|---|---|---|---|---|
| L2 underlay | **Cilium** | 1.16.5 | `kube-system` | — | `planlandı` | `cilium status --wait` · `kubectl get gatewayclass` | kube-proxy replacement, Hubble UI+Relay, Gateway API |
| L2 underlay | **MetalLB** | 0.14.9 | `metallb-system` | — | `planlandı` | `kubectl -n metallb-system get ipaddresspool` · LB Service IP alıyor mu (smoke test) | L2 mode; IP havuzu `.env`'den |
| L2 underlay | **Rook-Ceph** | v1.15.6 | `rook-ceph` | — | `planlandı` | `kubectl get storageclass` · `ceph status` · RWO+RWX PVC `Bound` (smoke test) | RBD + CephFS + RGW; 6 OSD / 3 node |
| L4 control-plane | **Keycloak** | 24.4.7 | `keycloak` | — | `planlandı` | `curl .../realms/platform/.well-known/openid-configuration` | Realm `platform` + admin; client'lar sonraki fazlarda |
| L4 control-plane | **Harbor** | 1.16.0 (Harbor 2.12) | `harbor` | — | `planlandı` | `curl .../api/v2.0/health` · `curl .../api/v2.0/scanners` | Trivy aktif; S3 backend = Ceph RGW |
| L1 bootstrap | **ArgoCD** | 7.8.2 (2.13.x) | `argocd` | — | `planlandı` | `kubectl -n argocd get application` · UI login | App-of-Apps kökü; TLS/OIDC Faz 3-4'te |
| L6 tenant API | **Crossplane v2** | 1.18.0 | `crossplane-system` | — | `planlandı` | `kubectl get providers,functions.pkg.crossplane.io` | provider-kubernetes/helm/terraform + function-kcl; hepsi InjectedIdentity/kimliksiz |
| L5 guardrails | **Kyverno** | 3.3.6 | `kyverno` | — | `planlandı` | `kubectl get clusterpolicy` · `kyverno test policies/tests/` | 3 ClusterPolicy, **Audit modunda** |
| L4 control-plane | **External Secrets Operator** | 0.10.7 | `external-secrets` | — | `planlandı` | `kubectl -n external-secrets get pods` | Yalnızca operatör — SecretStore Faz 3'te |
| L3 pki | **Vault** | 0.29.1 (Vault 1.18.1) | `vault` | — | `planlandı` | `vault status` (Sealed: false) · `03-pki.sh --verify-only` | HA/Raft, 3 replika; init/unseal İNSAN eylemi — bkz. runbook |
| L3 pki | **cert-manager** | v1.16.2 | `cert-manager` | — | `planlandı` | `kubectl get clusterissuer` · uçtan uca test sertifikası | `serviceAccountRef` — statik token Secret YOK |
| L6 tenant API | **XTenant composition** | `xtenants.platform.internal/v1alpha1` | (cluster-scoped XRD) | — | `planlandı` | `chainsaw test tests/e2e/` · `crossplane render` | 10 kaynak/claim; Namespace+RBAC+Quota+LimitRange+2×NetworkPolicy+SA+Workspace+Issuer |
| L5 guardrails | **Tenant guardrails (5×)** | `04`-`08` | `kyverno` (webhook) | — | `planlandı` | `kyverno test platform/policies/tests/` (33 test) | Hepsi **Enforce** — claim-creation RBAC, quota-bypass koruması, custom-network-tier kısıtı, Certificate/Issuer allowlist, PSS restricted |
| L6 tenant API | **XPostgreSQLInstance composition** | `xpostgresqlinstances.platform.internal/v1alpha1` | (cluster-scoped XRD) | — | `planlandı` | `chainsaw test tests/e2e/` · `crossplane render` (GERÇEK Docker ile doğrulandı) | 9-10 kaynak/claim: OBC+Certificate+Cluster+ScheduledBackup+NetworkPolicy+ServiceMonitor+SecretStore+PushSecret+ExternalSecret+(HA ise)PDB |
| L4 control-plane | **kube-prometheus-stack** | 65.5.1 | `observability` | — | `planlandı` | `kubectl -n observability get pods` · Grafana/Prometheus/Alertmanager rollout | ArgoCD sync-wave 1, otomatik sync AÇIK (sır yok) |
| L4 control-plane | **Loki** | 6.16.0 | `observability` | — | `planlandı` | `kubectl -n observability rollout status statefulset/loki` | S3 backend Ceph RGW; ArgoCD Application otomatik sync KAPALI (S3 sırrı) |
| L4 control-plane | **Tempo** | 1.10.3 | `observability` | — | `planlandı` | `kubectl -n observability rollout status statefulset/tempo` | S3 backend Ceph RGW; ArgoCD Application otomatik sync KAPALI (S3 sırrı) |
| L4 control-plane | **OpenCost** | 1.39.0 | `opencost` | — | `planlandı` | `curl .../allocation/compute?...&aggregate=label:cost-center` | Bare-metal manuel CPU/RAM/storage fiyatlandırması (custom-pricing ConfigMap) |
| L4 control-plane | **weekly-tenant-report CronJob** | — | `observability` | — | `planlandı` | `kubectl -n observability get cronjob weekly-tenant-report` · elle tetikleme | Her Pazartesi 06:00 UTC; maliyet+kaynak+sertifika özeti, her tenant namespace'inde `weekly-report` ConfigMap'i |

**Durum değerleri:** `planlandı` · `kuruluyor` · `çalışıyor` · `bozuk` · `kullanımdan kaldırıldı`

**Doğrulama sütunu:** bileşenin çalıştığını kanıtlayan somut kontrol.
Hepsini birden çalıştırmak için: `./platform/bootstrap/01-underlay.sh --verify-only`

### Faz 12 — "tüm eksiklikleri tamamla" görevi: kararlar + kod düzeltmeleri + GERÇEK bir DR tatbikatı

> Bu görev "tüm eksiklikleri tek bir eksik kalmayacak şekilde tamamla" dedi.
> Bu, KISMEN mümkündür — bkz. altta "Bu ortamda kalıcı olarak
> tamamlanamayan kalemler". Aşağıdakiler bu fazda GERÇEKTEN kapatıldı.

**Kararlar (tasarım/iş kararları, hepsi gerekçeli):**
- fail-closed KALICI (bypass eklenmedi), Vault Enterprise ALINMAYACAK (OSS kalıcı), LDAP federasyonu şimdilik gerekmiyor, cost-center→tenant 1:1 ZORUNLU (bkz. kod), Vault auto-unseal iş kararı olarak AÇIK bırakıldı (güven modeli ödünleşimi).

**Kod düzeltmeleri (gerçek testlerle doğrulandı):**

| Kalem | Ne yapıldı | Doğrulama |
|---|---|---|
| K8s API server OIDC (açık karar #13/teknik borç #17) | Keycloak `kubernetes` client'ı + kubeadm `ClusterConfiguration` şablonu (`docs/runbooks/k8s-api-server-oidc.md`) + composition/policy'lerde `oidc:` önek uyumu | `render-examples.sh` + `kyverno test` (38/38) |
| ESO per-tenant kimlik izolasyonu (açık karar #16) | `tenant/function.k`'ye ESO ServiceAccount+RBAC+Vault rolü/policy eklendi; `postgresql/function.k`'nin SecretStore'u güncellendi | Render + **GERÇEK kind cluster'ında canlı olarak** (ServiceAccount/Role/RoleBinding otomatik oluştu) |
| cost-center → tenant 1:1 (açık karar #19) | Yeni Kyverno politikası `09-enforce-unique-cost-center.yaml` (`context.apiCall`) | `kyverno test` (statik) + **GERÇEK kind cluster'ında canlı admission testi** (aynı cost-center'lı 2. tenant GERÇEKTEN reddedildi, farklı cost-center KABUL edildi) |
| Cosign anahtar üretimi (açık karar #21) | GERÇEK bir cosign v2.4.1 anahtar çifti üretildi, public key `security/cosign.pub`'a commit edildi, `02-control-plane.sh`'e `ensure_cosign_signing_key_secret()` eklendi | — |
| Vault'un kendi TLS'i (teknik borç #13) | Tam prosedür (`docs/runbooks/vault-self-tls.md`) — bilinçli olarak otomatik akışa BAĞLANMADI (risk) | Statik inceleme |
| **KRİTİK — `policies/security/` ve `policies/rbac/` HİÇBİR ArgoCD Application'a bağlı değildi** | `01-kyverno.yaml.tpl`'e 2 yeni directory source eklendi + `ensure_cosign_signing_key_secret()` | GERÇEK kind cluster'ında keşfedildi (cost-center politikası test edilirken) |
| **KRİTİK — Kyverno'nun `context.apiCall`'ı RBAC olmadan TÜM Tenant claim'lerini reddediyordu** | `policies/rbac/kyverno-tenant-reader-rbac.yaml` eklendi | GERÇEK kind cluster'ında keşfedilip GERÇEKTEN düzeltildi (düzeltmeden önce/sonra canlı test edildi) |
| PSS istisna listesi drift kontrolü (teknik borç #30) | `verify-pss-exemption-sync.sh` yazıldı, `platform-ci.yaml`'a bağlandı | Çalıştırılınca GERÇEK bir 2-satırlık drift bulup düzeltti |
| PolicyException süre takibi (teknik borç #32) | `check-expired-exceptions.sh` yazıldı | Mock veriyle gerçek tarih mantığı test edildi (geçmiş/gelecek review-date doğru ayırt edildi) |
| CI job tekilleştirme (teknik borç #33) | `security-scan.yaml`'daki yinelenen kyverno job'ı kaldırıldı | actionlint temiz |
| RGW endpoint hardcode (teknik borç #24, yalnızca Helm/envsubst kısmı) | `loki`/`tempo` values.yaml.tpl artık `${CEPH_OBJECTSTORE_NAME}` kullanıyor | `05-observability.sh`'in envsubst listesi güncellendi |
| **`tests/e2e/kind-chain/run.sh`'de GERÇEK bir bug** | Vault init/unseal'i BAŞARISIZ kılan dead-code satır + yetersiz readiness-wait + çok agresif liveness probe bulundu ve düzeltildi | Script BAŞTAN SONA gerçek bir kind cluster'ında YENİDEN çalıştırılıp TÜM zincir (Tenant→Postgres→bağlantı→cert) tekrar kanıtlandı |

**GERÇEK bir DR tatbikatı (görev madde 6'nın kabul kriteri #3'ü, Faz 10'da karşılanmamıştı):**
kind + gerçek Velero + MinIO (Rook-Ceph RGW yerine stand-in S3) ile TAM bir
backup→disaster→restore döngüsü çalıştırıldı: bkz.
`docs/runbooks/disaster-recovery.md` §8a/§8b. Sonuç: K8s objeleri (RBAC,
Secret, Issuer, Certificate) BAŞARIYLA restore edildi; CNPG'nin GERÇEK
Postgres verisi CSI snapshot entegrasyonu olmadığı için RESTORE EDİLEMEDİ —
tam da önceden işaretlenen mimari riskin CANLI kanıtı. Ayrıca Crossplane'in
kendi kendini iyileştirme davranışı (namespace silinince Velero'ya gerek
KALMADAN kendiliğinden yeniden üretmesi) keşfedildi ve belgelendi.

### Bu ortamda KALICI OLARAK tamamlanamayan kalemler (gerçek engel, atlanmış değil)

| Kalem | Neden tamamlanamıyor |
|---|---|
| Fiziksel bare-metal donanım (node'lar, IP havuzu, ham diskler) | Bu ortamda fiziksel/sanal makine YOK — `01-underlay.sh` hiçbir zaman gerçek donanıma karşı çalıştırılamadı |
| Gerçek DNS delegasyonu (`*.apps.<domain>`) | Gerçek bir domain registrarına erişim YOK |
| Vault key custodian'larının GERÇEK isimleri | Organizasyonel/HR bir atama — platform mühendisliği İCAT EDEMEZ |
| `ALERTMANAGER_WEBHOOK_URL`, `OPENCOST_*_HOURLY_COST` gerçek değerleri | Organizasyona özgü sır/mali veri — platform mühendisliği tahmin EDEMEZ |
| K8s API server'a GERÇEKTEN `--oidc-*` flag'lerinin eklenmesi | Gerçek bir kubeadm control-plane node'una erişim GEREKİR — tasarım TAMAMLANDI, uygulama bekliyor |
| Rook-Ceph OSD'li/Vault Raft'lı TAM bir bare-metal DR tatbikatı | Bu ortamda gerçek disk/donanım YOK — kind+MinIO ile MEKANİZMA kanıtlandı, TAM ölçek değil |
| MetalLB/Cilium'un kind'de TAM olarak test edilmesi | Zaman/kapsam kısıtı — kind kendi CNI'sini (kindnet) kullanıyor; bu görevde Cilium'a geçiş DENENMEDİ |

### Faz 11'de üretilen ve GERÇEK bir `kind` cluster'ında (Crossplane 2.4.0 + Vault HA/Raft + cert-manager + CNPG canlı reconcile) doğrulanmış artefaktlar

| Artefakt | Konum / doğrulama |
|---|---|
| Platform reposu CI: composition testleri (her PR'da `verify-sync.sh` + gerçek `crossplane render`, Docker ile) + Kyverno policy testleri (`kyverno test`, tüm paket) + kind tabanlı e2e chain job'ı | `.github/workflows/platform-ci.yaml` |
| Uçtan uca zincir testi script'i: Tenant claim → Namespace/RBAC/Quota/Issuer (GERÇEK Vault PKI) → Postgres claim → CNPG Cluster + bağlantı Secret'ı → Certificate (AYNI Issuer'dan) | `tests/e2e/kind-chain/{run.sh,strip_unsupported_resources.py,README.md}` |
| **GERÇEK uçtan uca sonuç (bu görevde canlı çalıştırıldı, adım adım):** Vault HA/Raft init/unseal/PKI kuruldu → Crossplane 2.4.0 gerçek Tenant+Postgres composition'larını reconcile etti → `tenant-issuer` Ready (Vault verified) → CNPG cluster `healthy` → bağlantı Secret'ı (`<name>-app`) gerçek kimlik bilgileriyle → Postgres'in KENDİ TLS sertifikası + ayrı bir prob sertifikası, `openssl verify -CAfile <Vault chain> leaf.crt` ile **`OK`** | `tests/e2e/kind-chain/README.md`, bu bölümün üstündeki "KRİTİK BULGULAR" kutusu |
| **2 chaos senaryosu GERÇEKTEN uygulandı:** (a) CNPG primary pod silme → ~7s'de otomatik failover, ~26s'de küme 3/3 healthy'e döndü; (b) Vault lider pod'u silme → ~8s'de yeni lider seçimi (raft self-heal), quorum hiç kesintiye uğramadı | `platform/docs/chaos-test-results.md` (ham komut çıktıları + zaman damgaları) |
| Crossplane/cert-manager/ArgoCD/Kyverno sürüm yükseltme + geri alma runbook'u — yükseltme sırası gerekçeli, her bileşen için test/rollback adımları | `platform/docs/runbooks/upgrade-rollback.md` |
| `versions.env`: `CROSSPLANE_CHART_VERSION` **1.18.0 → 2.4.0** (kritik düzeltme) | `platform/underlay/versions.env` |
| `postgresql/function.k`: `serverCASecretName` → `serverCASecret` (CNPG CRD şema düzeltmesi) | `platform/compositions/postgresql/function.k`; `render-examples.sh --check` ile yeniden doğrulandı (small: 22 doküman, ha-large: 24 doküman) |
| `tenant/function.k`: `certManagerServiceAccount`/`certManagerTokenRequestRole`/`RoleBinding` eklendi (Issuer'ın GERÇEKTEN Ready olabilmesi için) | `platform/compositions/tenant/function.k` (madde 7b); `render-examples.sh --check` ile yeniden doğrulandı (her claim artık 32 doküman, önceki 26'dan +6: 3 yeni kaynak × 2 örnek) |
| chainsaw e2e testine yeni assertion'lar (cert-manager ServiceAccount + RoleBinding) | `platform/compositions/tenant/tests/e2e/tenant-provisioning/chainsaw-test.yaml` |

**Ne GERÇEKTEN doğrulandı, ne DOĞRULANMADI (Faz 11):**
- ✅ Composition + Kyverno CI job'ları — YAML/`actionlint` ile doğrulandı; job'ların KENDİSİ GitHub Actions'ta ÇALIŞTIRILMADI (bu ortamda gerçek bir GitHub Actions çalıştırıcısı yok) — ama İÇERDİKLERİ komutlar (`verify-sync.sh`, `render-examples.sh --check`, `kyverno test`) bu oturumda YEREL olarak GERÇEKTEN çalıştırılıp yeşil sonuç verdi.
- ✅ kind tabanlı e2e zincir — GERÇEKTEN, canlı olarak, adım adım bu oturumda çalıştırıldı (yukarıdaki tabloya bakın). `run.sh` script'i bu ADIMLARI birebir kodlar ama **script'in kendisi baştan sona OTOMATİK olarak yeniden çalıştırılıp doğrulanmadı** — CI'daki `e2e-chain-test` job'ının GERÇEKTEN GitHub Actions'ta yeşil geçtiği bu oturumda GÖRÜLMEDİ (yalnızca manuel adım adım çalıştırma kanıtlandı).
- ✅ 2 chaos senaryosu — tam anlamıyla GERÇEK, canlı, zaman damgalı.
- ❌ Rook-Ceph/Cilium/ESO'ya bağımlı 7 composed kaynak (bkz. `tests/e2e/kind-chain/README.md` tablosu) bu e2e testte kapsam DIŞI bırakıldı — bunların GERÇEK doğrulaması hâlâ yalnızca tam bare-metal kurulumla mümkün (`platform/compositions/*/tests/e2e/` chainsaw testleri, hâlâ hiç canlı çalıştırılmadı).
- ❌ Chaos senaryoları yalnızca standalone CNPG/Vault kurulumlarına karşı yapıldı (platformun TAM composition zincirinden geçmiş bir Tenant/Postgres'e karşı DEĞİL) — bu bilinçli bir kapsam daraltmasıdır (zaman/karmaşıklık), teknik borç olarak kaydedildi.

### Faz 10'da üretilen ve GERÇEK araçlarla (cosign + kyverno apply/test + gitleaks + trufflehog + syft + trivy + actionlint) doğrulanmış artefaktlar

| Artefakt | Konum / doğrulama |
|---|---|
| Kyverno imzalı-imaj zorunluluğu (`verifyImages`, Cosign) | `platform/policies/security/01-require-signed-images.yaml` |
| **GERÇEK uçtan uca imza testi**: yerel Docker registry + gerçek `cosign generate-key-pair` + `kyverno apply --registry` — imzalı imaj KABUL, imzasız imaj RED | `platform/policies/tests/require-signed-images/{README.md,policy-test.yaml,pod-signed.yaml,pod-unsigned.yaml}` |
| **KRİTİK BULGU:** cosign v3.1.3 (Homebrew) ile imzalanan imajlar Kyverno 1.19.1 tarafından ANLAŞILAMIYOR (yeni sigstore-bundle/OCI-Referrers formatı) — GitHub'dan indirilen cosign **v2.4.1** ile imzalananlar ÇALIŞIYOR; CI bu sürüme sabitlendi | `image-supply-chain.yaml` (`COSIGN_VERSION: "2.4.1"`), `tests/require-signed-images/README.md` |
| Cluster-genelinde PSS "restricted" (platform sistemi namespace'leri hariç) + kube-apiserver `PodSecurityConfiguration` (kubeadm seviyesi) + istisna süreci (`PolicyException` + zorunlu gerekçe/onay annotation'ları) | `platform/policies/security/{02-require-pss-restricted-clusterwide.yaml,pod-security-admission-configuration.yaml,README.md}` |
| **GERÇEK doğrulama:** `kyverno test` — 3 yeni senaryo (etiketsiz→red, etiketli→kabul, istisna listesi→muaf) + tüm paket (36 test) YEŞİL | `platform/policies/tests/require-pss-restricted-clusterwide/` |
| gitleaks + trufflehog CI job'ları (platform reposu + tenant-requests reposu, `fetch-depth: 0`, tam git geçmişi) | `.github/workflows/security-scan.yaml`, `tenant-requests/.github/workflows/validate.yaml` (`secret-scan` job) |
| **GERÇEK tespit testi**: geçici bir git reposuna gerçekçi formatlı bir test sırrı (Stripe key) commit edildi — gitleaks (exit 1) VE trufflehog (exit 183, `--fail`) İKİSİ DE yakaladı; AWS'in KENDİ "EXAMPLE" anahtarı (gitleaks'ın varsayılan allowlist'i nedeniyle) YAKALANMADI — bu bulgu belgelendi | `platform/policies/tests/security-scan-fixture/README.md` |
| Harbor: `library` projesinde `auto_scan`+`prevent_vul`(severity=high) API ile zorunlu kılındı (registry-seviyeli ikinci savunma katmanı) | `platform/bootstrap/01-underlay.sh` → `configure_harbor_security_policy()` |
| syft SBOM (SPDX+CycloneDX) + Trivy zafiyet gate'i (KRİTİK/YÜKSEK varsa push'u durdurur) + cosign imzalama (v2.4.1 pinli) — yeniden kullanılabilir `workflow_call` | `.github/workflows/image-supply-chain.yaml` |
| **GERÇEK doğrulama:** `syft alpine:3.20` → 14 paket SBOM'u; `trivy image --severity HIGH,CRITICAL --exit-code 1` → temiz imajda exit 0, bilinen KRİTİK CVE'li eski bir imajda (`alpine:3.10`, CVE-2021-36159) exit 1 | (bu görevde yerel olarak çalıştırıldı, kalıcı bir fixture bırakılmadı — sonuç bu tabloda belgeleniyor) |
| Vault audit log (`file` device, per-pod `auditStorage` PV — zaten `pki/vault/values.yaml`'da hazırdı, bu görevde ETKİNLEŞTİRİLDİ) — idempotent, `VAULT_TOKEN` yoksa nazikçe atlar | `platform/bootstrap/03-pki.sh` → `enable_vault_audit()`/`verify_vault_audit()` |
| Break-glass root token rotasyon runbook'u (aktivasyon şartı, adım adım `generate-root`, audit log çapraz doğrulama, planlı rotasyon) — `vault-unseal.md`'nin eski §6.3'ünün YERİNE GEÇTİ | `platform/docs/runbooks/vault-break-glass.md` |
| DR runbook: Velero (K8s objeleri) + Vault Raft snapshot + Rook-Ceph felaketi + tam cluster'ın Git'ten yeniden inşası, adım adım | `platform/docs/runbooks/disaster-recovery.md` |
| **MİMARİ RİSK (bu runbook'u yazarken keşfedildi, görev metninde istenmemişti):** Velero'nun S3 hedefi Rook-Ceph RGW'dir — Rook-Ceph TAMAMEN kaybolursa Velero'nun YEDEĞİ DE kaybolur (tek nokta bağımlılığı) | `disaster-recovery.md` §3.1 |

**Ne GERÇEKTEN doğrulandı, ne DOĞRULANMADI (Faz 10):**
- ✅ Cosign/Kyverno imza doğrulama — gerçek registry + gerçek anahtarlarla, hem kabul hem red senaryosu.
- ✅ PSS cluster-wide politika — `kyverno test` ile (36 test, hepsi yeşil).
- ✅ gitleaks/trufflehog — gerçek (ama zararsız) bir test sırrıyla, iki bağımsız araçla.
- ✅ syft/trivy — gerçek imajlarla (temiz + bilinen-CVE'li), gerçek exit code davranışı.
- ✅ Tüm yeni GitHub Actions workflow'ları `actionlint` ile temiz.
- ❌ **Hiçbir şey gerçek bir cluster'a UYGULANMADI** — `01-underlay.sh`'in `configure_harbor_security_policy()`'si ve `03-pki.sh`'in `enable_vault_audit()`'i hiçbir zaman gerçek bir Harbor/Vault'a karşı ÇALIŞTIRILMADI (yalnızca `bash -n`/`shellcheck` ile statik doğrulandı).
- ❌ **DR runbook'u (`disaster-recovery.md`) gerçek bir test cluster'ında ÇALIŞTIRILMADI** — kabul kriteri #3 KARŞILANMADI (bkz. runbook'un kendi §8'i). Bu ortamda çok-node'lu/Rook-Ceph'li/Vault Raft'lı bir test cluster'ı yoktu; Velero'nun kendisi de (`platform/control-plane/velero/`) henüz kurulu değil.
- ❌ `01-require-signed-images.yaml`'daki `harbor.apps.platform.internal/*` yer tutucusu gerçek `HARBOR_HOSTNAME` ile DEĞİŞTİRİLMEDİ — kurulumdan önce ELLE (veya envsubst ile) güncellenmesi gerekiyor, aksi halde politika HİÇBİR imaja uygulanmaz.

### Faz 9'da üretilen ve GERÇEK araçlarla (kclvm_cli + crossplane render + Docker + promtool + envsubst + jq) doğrulanmış artefaktlar

| Artefakt | Konum / doğrulama |
|---|---|
| kube-prometheus-stack Helm values (Grafana sidecar: `grafana_dashboard`/`grafana_folder` etiket+annotation'ıyla ALL-namespace dashboard keşfi; Loki/Tempo datasource'ları) | `platform/control-plane/observability/values.yaml`; `python3 -c "import yaml"` ile parse edildi |
| Loki/Tempo Helm values (S3 backend: Ceph RGW, `${LOKI_S3_ACCESS_KEY}` vb. — Git'e hiç yazılmaz) | `platform/control-plane/observability/{loki,tempo}/values.yaml.tpl` |
| ObjectBucketClaim'ler (`loki-storage`, `tempo-storage`) — Harbor/Velero ile AYNI desen, `rook-ceph` namespace'inde | `platform/control-plane/observability/resources/objectbucketclaims.yaml` |
| cert-manager expiry PrometheusRule (30/7/1 gün kademeli + zaten-dolmuş, 4 kural) — **`promtool check rules` ile SUCCESS** | `platform/control-plane/observability/resources/certmanager-expiry-rules.yaml` |
| Alertmanager Slack/Teams routing Secret şablonları (native `slack_configs`/`msteams_configs`) — **`envsubst` round-trip + iç/dış YAML parse ile doğrulandı** | `platform/control-plane/observability/resources/alertmanager-config-{slack,teams}.yaml.tpl` |
| Hubble tenant-traffic Grafana dashboard'u (namespace bazlı akış/drop/TCP/DNS paneli) | `platform/control-plane/observability/dashboards/hubble-tenant-traffic.yaml`; gömülü JSON `json.loads` ile doğrulandı |
| Cilium Hubble metriklerine `labelsContext=source_namespace,destination_namespace` eklendi (dns/drop/tcp/flow) — dashboard'un namespace filtresi İÇİN ZORUNLU | `platform/underlay/cilium/values.yaml.tpl` |
| **Tenant composition'a 9. adım: otomatik Grafana dashboard ConfigMap'i** (`grafana_dashboard=1`, `grafana_folder=Tenants/<team>-<env>`; CPU/mem/network + CNPG panelleri) — **gerçek `kclvm_cli` + gerçek `crossplane render` (Docker) ile üretilip JSON'u `json.loads` ile doğrulandı** | `platform/compositions/tenant/function.k` (madde 9), `composition.yaml` (senkron, `verify-sync.sh` yeşil) |
| OpenCost Helm values + bare-metal manuel fiyatlandırma ConfigMap şablonu (`CUSTOM_PRICING_CONFIGMAP_NAME`, CPU/RAM/storage saatlik $) | `platform/control-plane/opencost/values.yaml`, `resources/custom-pricing-configmap.yaml.tpl` |
| Haftalık tenant özeti: `jq`-tabanlı script (OpenCost allocation API + ResourceQuota + cert-manager Certificate durumu) + RBAC + CronJob — **script gerçek `bash`+`jq` ile mock kubectl/curl'e karşı uçtan uca çalıştırıldı** | `platform/control-plane/observability/weekly-report/{report.sh,cronjob.yaml}` |
| ArgoCD Application'ları (kube-prometheus-stack/opencost otomatik sync AÇIK — sır yok; Loki/Tempo KAPALI — S3 sırrı, `underlay-root` ile AYNI gerekçe) | `platform/control-plane/apps/{01-kube-prometheus-stack,01-loki,01-tempo,02-opencost}.yaml.tpl` |
| Kurulum script'i (`05-observability.sh`) — OBC→S3 kimlik kopyalama, Grafana admin + Alertmanager Secret'ları, ArgoCD sync tetikleme, Loki/Tempo doğrudan helm, OpenCost custom-pricing, weekly-report, opsiyonel `--only cert-alert-test` (kısa ömürlü self-signed sertifikayla UÇTAN UCA alert testi) | `platform/bootstrap/05-observability.sh`; `shellcheck` ve `bash -n` temiz |
| chainsaw e2e testine yeni assertion (Grafana dashboard ConfigMap) | `platform/compositions/tenant/tests/e2e/tenant-provisioning/chainsaw-test.yaml` |

**Ne GERÇEKTEN doğrulandı, ne DOĞRULANMADI:**
- ✅ `crossplane render` (Docker, gerçek function-kcl/function-auto-ready) → Tenant composition artık 11 kaynak üretiyor (10 + yeni dashboard ConfigMap), ikisi de `docs/examples-output/tenant-acme-{dev,prod}.rendered.yaml`'a kaydedildi.
- ✅ `promtool check rules` → 4 alert kuralı SUCCESS (Sprig `default` filtresinin YOK olduğu, gerçek hatayla keşfedildi — bkz. Faz günlüğü).
- ✅ `envsubst` round-trip + iç/dış YAML parse → Alertmanager Secret şablonları.
- ✅ `report.sh` → gerçek `jq`/`bash` ile, mock `kubectl`/`curl` karşısında uçtan uca (namespace keşfi → maliyet/kota/sertifika → ConfigMap apply).
- ❌ **Hiçbiri gerçek bir cluster'a UYGULANMADI** — `05-observability.sh` hiç çalıştırılmadı; Grafana/Prometheus/Alertmanager/Loki/Tempo/OpenCost'un GERÇEKTEN ayakta kalktığı, dashboard'un GERÇEKTEN Grafana'da göründüğü, alert'in Alertmanager'a GERÇEKTEN ulaştığı ve Slack/Teams webhook'unun GERÇEKTEN mesaj aldığı doğrulanmadı.
- ❌ `--only cert-alert-test` adımının kendisi de bir cluster'a karşı ÇALIŞTIRILMADI (yalnızca script mantığı yazıldı ve statik olarak gözden geçirildi).

### Faz 7c / 8 — yerel doğrulanan, henüz deploy edilmemiş artefaktlar

| Artefakt | Konum / doğrulama |
|---|---|
| Path bazlı git-files ApplicationSet + yalnızca iki Claim türüne izin veren AppProject | `platform/control-plane/apps/{05-tenant-requests-project,06-tenant-requests-appset}.yaml.tpl`; main, 60s; `clusters/platform/*/*.yaml` → in-cluster; eski düz yollar da desteklenir |
| GitHub Actions doğrulama + güvenli PR yorum workflow'u | `tenant-requests/.github/workflows/{validate,comment}.yaml`; XRD/kubeconform + gerçek crossplane render + Kyverno apply/test; workflow_run yorumu PR kodunu write token ile çalıştırmaz |
| Gerçek Backstage 1.54.0 uygulaması, OIDC backend/frontend, Kubernetes ve yerel Crossplane plugin'i | `platform/backstage/portal/`, lockfile + Dockerfile; TypeScript ve frontend/backend build başarılı |
| İki Scaffolder Template | `platform/backstage/templates/`; gerçek fetch:template ve publish:github:pull-request action'ları çalıştırıldı; GitHub client test double; iki üretilen Claim gerçek schema/render/policy'den geçti |
| Ready-gated EntityProvider | `portal/packages/backend/src/platformCatalog.ts`; composition ConfigMap catalog-info verisini 60s'de keşfeder; Ready/silme/API hata davranışı Jest ile doğrulandı |
| Kurulum kaynakları | `platform/backstage/app/`, `configure.py`, `04-backstage.sh`; chart 2.6.1 helm template çıktısındaki 3 kaynak kubeconform'dan geçti |
| Geçerli/geçersiz Claim regresyonu | 4 mevcut Claim yeşil; geçersiz tier, prod+small, bilinmeyen alan ve PSS policy ihlali reddedildi; Kyverno test: 33 başarılı, 0 başarısız |

**Canlı kabul henüz yok:** Bu çalışma alanında Kubernetes context'i sağlanmadı; gerçek GitHub repo hedefleri ve Keycloak bağlantısı bildirilmedi. GitHub client test double'ı gerçek PR değildir. SSO kodu ve giriş ekranı derlendi/test edildi; gerçek Keycloak login kanıtı değildir. `backstage/README.md` kurulum ve canlı kabul adımlarını içerir.

### Faz 1'de üretilen, henüz uygulanmamış artefaktlar

| Artefakt | Konum |
|---|---|
| Kurulum script'i | `platform/bootstrap/01-underlay.sh` |
| Cilium values + 3 ağ politikası | `platform/underlay/cilium/` |
| MetalLB values + IPAddressPool şablonu | `platform/underlay/metallb/` |
| Rook operator/cluster/pool/fs/objectstore/OBC | `platform/underlay/rook-ceph/` |
| StorageClass'lar (`ceph-block`, `ceph-filesystem`) | `platform/underlay/storage-classes/` |
| Harbor values (S3 → RGW, Trivy) | `platform/control-plane/harbor/` |
| Keycloak values + realm import | `platform/control-plane/keycloak/` |
| ArgoCD app-of-apps (Faz 2'de kullanılacak) | `platform/bootstrap/app-of-apps/` |
| Değişken şablonu / sürüm pinleri | `platform/underlay/.env.example`, `versions.env` |

### Faz 2'de üretilen, henüz uygulanmamış artefaktlar

| Artefakt | Konum |
|---|---|
| Kurulum script'i | `platform/bootstrap/02-control-plane.sh` |
| ArgoCD Helm values (Crossplane Provider/Function için özel Lua health check dahil) | `platform/control-plane/argocd/values.yaml` |
| Root Application (App-of-Apps) | `platform/control-plane/root-app.yaml.tpl` |
| Child Application'lar (underlay wave 0, crossplane/kyverno/eso wave 1, vault wave 2) | `platform/control-plane/apps/` |
| Crossplane values + Provider/Function/ProviderConfig (hepsi InjectedIdentity veya kimliksiz) | `platform/control-plane/crossplane/` |
| Kyverno Helm values | `platform/control-plane/kyverno/` |
| ESO Helm values (SecretStore YOK) | `platform/control-plane/external-secrets/` |
| Vault namespace yer tutucusu | `platform/pki/vault/namespace.yaml` |
| 3 ClusterPolicy (Audit modunda) | `platform/policies/validation/` |
| 3 `kyverno test` paketi — **gerçek `kyverno` CLI 1.19.1 ile YEŞİL doğrulandı** | `platform/policies/tests/` |
| AppProject genişletmesi (Crossplane/Kyverno/ESO repo'ları eklendi) | `platform/bootstrap/app-of-apps/appproject-platform.yaml.tpl` |

### Faz 6b'de üretilen ve GERÇEK `kyverno` CLI 1.19.1 ile doğrulanmış artefaktlar

| Artefakt | Konum |
|---|---|
| RBAC (ClusterRole+ClusterRoleBinding — Tenant claim'i kim açabilir) | `platform/policies/rbac/tenant-claim-rbac.yaml` |
| 5 ClusterPolicy, **hepsi Enforce** (claim-creation, quota-bypass, custom-network-tier, Certificate/Issuer allowlist, PSS restricted) | `platform/policies/validation/04-08-*.yaml` |
| 33 test senaryosu (her politika en az 1 izin + 1 red) — **YEŞİL** | `platform/policies/tests/{restrict-tenant-claim-creation,protect-tenant-resourcequota,restrict-custom-network-tier,restrict-certificate-issuer,require-pss-restricted}/` |
| Faz 6 eklentisi: Namespace'e 3 PSS etiketi otomatik eklendi (08 numaralı politika olmadan composition'ı kendi kendine kilitlerdi) | `platform/compositions/tenant/function.k`, `composition.yaml` |
| Faz 1 eklentisi: Keycloak realm'ine `platform-tenant-requesters` grubu eklendi | `platform/control-plane/keycloak/realm-platform.json.tpl` |

### Faz 7'de üretilen ve GERÇEK araçlarla (kclvm_cli + crossplane render + Docker + chainsaw) doğrulanmış artefaktlar

| Artefakt | Konum |
|---|---|
| XRD (`XPostgreSQLInstance` + `PostgreSQLInstance` claim) | `platform/compositions/postgresql/xrd.yaml` |
| Composition (function-kcl pipeline, KCL inline gömülü) | `platform/compositions/postgresql/composition.yaml` |
| KCL mantığı — 9-10 kaynak, `assert` ile HA+small reddi | `platform/compositions/postgresql/function.k` |
| 2 örnek Claim (small, HA-large) | `platform/compositions/postgresql/examples/` |
| **Gerçek `crossplane render` çıktısı** (6 kombinasyonun TAMAMI test edildi, HA+small **gerçek Docker+function-kcl ile reddedildi**) | `platform/docs/examples-output/postgresql-{small,ha-large}.rendered.yaml` |
| chainsaw e2e test (Cluster Ready + bağlantı sırrı + TLS + cross-tenant izolasyon) | `platform/compositions/postgresql/tests/e2e/postgresql-provisioning/` |
| function.k↔composition.yaml drift kontrolü | `platform/compositions/postgresql/tests/verify-sync.sh` |
| Geliştirici runbook'u ("Postgres nasıl istenir") | `platform/docs/runbooks/request-postgres.md` |
| Faz 3 eklentisi: `kv/` KV v2 mount + `eso-tenant-secrets` Vault rolü/policy'si (ESO round-trip için) | `platform/pki/vault/policies/eso-tenant-secrets-policy.hcl`, `03-pki.sh` |
| Faz 6/6b eklentisi: `xrd.yaml`/`composition.yaml`'a sync-wave anotasyonları + YENİ ArgoCD Application (iki composition'ı da GitOps'a bağlayan, Faz 6'dan beri eksik olan boşluk kapatıldı) | `platform/control-plane/apps/04-compositions.yaml.tpl` |

### Faz 6'da üretilen ve GERÇEK ARAÇLARLA doğrulanmış artefaktlar

> Bu faz, önceki fazlardan farklı olarak yalnızca statik doğrulama (YAML/HCL
> parse, sözdizimi) değil, **fiilen çalıştırılan** araçlarla test edildi:
> `kclvm_cli` 0.11.2 (KCL mantığı), gerçek `crossplane` CLI 2.5.0 + Docker
> (function-kcl + function-auto-ready container'ları — **gerçek render**,
> simülasyon değil), ve `chainsaw` 0.2.15 (`lint` + `test --no-cluster` ile
> yapısal doğrulama; gerçek cluster olmadığı için tam `chainsaw test` koşumu
> yapılamadı).

| Artefakt | Konum |
|---|---|
| XRD (`XTenant` + `Tenant` claim) | `platform/compositions/tenant/xrd.yaml` |
| Composition (function-kcl pipeline, KCL inline gömülü) | `platform/compositions/tenant/composition.yaml` |
| KCL mantığı (canonical kaynak) | `platform/compositions/tenant/function.k` |
| 2 örnek Claim | `platform/compositions/tenant/examples/tenant-acme-{dev,prod}.yaml` |
| **Gerçek `crossplane render` çıktısı** (Docker ile üretildi) | `platform/docs/examples-output/tenant-acme-{dev,prod}.rendered.yaml` |
| chainsaw e2e test (6 kaynak tipi + Vault izolasyon testi) | `platform/compositions/tenant/tests/e2e/tenant-provisioning/` |
| function.k↔composition.yaml drift kontrolü | `platform/compositions/tenant/tests/verify-sync.sh` |
| Faz 3 eklentisi: `provider-terraform` Vault policy + auth role (tenant-scoped) | `platform/pki/vault/policies/provider-terraform-policy.hcl`, `03-pki.sh` |
| Faz 3 eklentisi: cert-manager policy'sine `pki-int-*/sign/tenant-*` glob'u eklendi | `platform/pki/vault/policies/cert-manager-policy.hcl` |

### Faz 3'te üretilen, henüz uygulanmamış artefaktlar

| Artefakt | Konum |
|---|---|
| Kurulum script'i | `platform/bootstrap/03-pki.sh` |
| Vault Helm values (HA, Raft, 3 replika, ceph-block) | `platform/pki/vault/values.yaml` |
| Vault policy'ler (cert-manager / crossplane / root-ca-admin break-glass) | `platform/pki/vault/policies/` |
| cert-manager values + 3 ClusterIssuer + (koşullu) ServiceMonitor | `platform/pki/cert-manager/` |
| Test sertifikası şablonu (uçtan uca doğrulama) | `platform/pki/cert-manager/test-certificate.yaml.tpl` |
| Vault init/unseal/break-glass runbook (5 parça/eşik 3, custodian şeması) | `platform/docs/runbooks/vault-unseal.md` |
| ArgoCD Application'ları (vault gerçek chart'a kavuştu; cert-manager yeni eklendi, wave 3) | `platform/control-plane/apps/02-vault-placeholder.yaml.tpl`, `03-cert-manager.yaml.tpl` |

### PKI hiyerarşisi — fingerprint'ler (kurulumdan sonra doldurulacak)

> `03-pki.sh`'in `print_fingerprints()` adımı bu değerleri terminale basar;
> **script dosyayı kendisi değiştirmez** — aşağıdaki satırlar kurulum
> sonrası ELLE doldurulur.

| CA | Mount | TTL | Vault role / ClusterIssuer | SHA-256 Fingerprint | Durum |
|---|---|---|---|---|---|
| Root CA | `pki-root` | 10 yıl (87600h) | — (break-glass `root-ca-admin` dışında hiçbir role atanmadı) | — | `planlandı` |
| Intermediate CA (dev) | `pki-int-dev` | 2 yıl (17520h) | `platform-dev` → `vault-issuer-dev` | — | `planlandı` |
| Intermediate CA (staging) | `pki-int-staging` | 2 yıl (17520h) | `platform-staging` → `vault-issuer-staging` | — | `planlandı` |
| Intermediate CA (prod) | `pki-int-prod` | 2 yıl (17520h) | `platform-prod` → `vault-issuer-prod` | — | `planlandı` |

---

## 2. Sıradaki adım

| # | Faz | Adım | Bağımlılık | Sahip | Durum | Çıktı (Definition of Done) |
|---|---|---|---|---|---|---|
| 1 | Faz 0 | Repo iskeleti + ADR-0001 + conventions | — | Platform | ✅ tamam | Bu repo |
| 2a | Faz 1 | Underlay manifestleri + `01-underlay.sh` yazımı | Faz 0 | Platform | ✅ tamam | 19 şablon render doğrulandı; script idempotent |
| 2b | Faz 1 | **`01-underlay.sh` çalıştırılması** | 2a, fiziksel node'lar + IP havuzu + ham diskler | — | 🔄 **sıradaki (paralel: 3a da hazır)** | `LoadBalancer` Service harici IP alıyor; RWO+RWX PVC bağlanıyor; Harbor health OK; Keycloak OIDC discovery OK |
| 2c | Faz 1 | default-deny açılması (`ENABLE_DEFAULT_DENY=true`) | 2b doğrulandıktan sonra | — | ⬜ | DNS smoke test geçiyor; `hubble observe --verdict DROPPED` temiz |
| 3a | Faz 2 | ArgoCD + App-of-Apps + Crossplane + Kyverno + ESO manifestleri + `02-control-plane.sh` yazımı | Faz 1 (yazım) | Platform | ✅ tamam | 7 şablon render doğrulandı; 3 Kyverno politikası `kyverno test` ile yeşil; hiçbir provider'da statik kimlik bilgisi yok |
| 3b | Faz 2 | **`02-control-plane.sh` çalıştırılması** | 3a, 2b (Faz 1 gerçekten kurulu) | — | ⬜ | ArgoCD ayakta; `crossplane`/`kyverno`/`eso` Application'ları Synced+Healthy; provider'lar Healthy; `underlay-root` devralma prosedürü tamamlanmış |
| 4a | Faz 3 | Vault (HA/Raft) + K8s auth + PKI hiyerarşisi + cert-manager manifestleri + `03-pki.sh` + unseal runbook yazımı | 3a (yazım) | Platform | ✅ tamam | Script sözdizimi + tüm YAML/HCL doğrulandı; hiçbir statik kimlik bilgisi yok |
| 4b | Faz 3 | **`03-pki.sh` çalıştırılması + Vault init/unseal (insan eylemi)** | 4a, 3b (Faz 2 gerçekten kurulu) | — | ⬜ | Vault `Sealed: false`; 3 ClusterIssuer `Ready`; test sertifikası `openssl verify` ile "OK" |
| 4c | Faz 3 | PKI fingerprint'lerinin PLATFORM_CONTEXT.md'ye elle kaydedilmesi | 4b | — | ⬜ | Bölüm 1'deki "PKI hiyerarşisi" tablosu dolduruldu |
| 5 | Faz 4 | Control plane: Harbor, Keycloak, CNPG, ~~gözlemlenebilirlik~~, ESO'nun ilk SecretStore'u | 4c | — | ⬜ | Harbor'a OIDC ile giriş; imaj push/pull; ~~Prometheus tüm hedefleri topluyor~~ **(gözlemlenebilirlik kısmı Faz 9'da, sıra dışı, ÖNDEN teslim edildi — bkz. satır 11a)** |
| 6 | Faz 5 | Guardrails: Kyverno 3 politikayı Enforce'a çekmek + managed-by/tenant etiketleri | Faz 4 | — | ⬜ | Zorunlu etiketi olmayan namespace reddediliyor |
| — | Faz 3.5 (opsiyonel, aciliyet yok) | Vault'un kendi listener TLS'i (self-referential, `tls_disable`'ı kapatmak) | 4b sağlıklı çalıştıktan uzun süre sonra | — | ⬜ | Ayrı bir runbook — bkz. `pki/vault/README.md` "Vault'un kendi TLS'i" |
| 7a | Faz 6 | XRD + KCL composition (`XTenant`) yazımı + gerçek `crossplane render`/`kclvm_cli`/`chainsaw lint` ile doğrulama | Faz 2 (`function-kcl` hazır), Faz 3 (Vault/cert-manager hedefleniyor) | Platform | ✅ tamam | 10 kaynak render edildi (Docker+crossplane CLI ile gerçek koşum); 2 örnek claim `docs/examples-output/`'a kaydedildi; chainsaw testi lint'ten geçti |
| 7b | Faz 6 | **`XTenant`'ın gerçek bir cluster'da uçtan uca doğrulanması** (`chainsaw test tests/e2e/`) | 7a, Faz 1-3 gerçekten kurulu | — | ⬜ | 6 kaynak tipi Ready; iki tenant'ın Vault PKI rolleri birbirinin domain'ine erişemiyor (script adımı PASS) |
| 7c | Faz 6b | Tenant guardrail'leri (5 Kyverno ClusterPolicy + RBAC) yazımı + `kyverno test` ile doğrulama | 7a (XTenant XRD/composition var olmalı) | Platform | ✅ tamam | 33 test senaryosu yeşil; 5 politika de Enforce; composition PSS etiketleriyle güncellendi |
| 8a | Faz 7 | `XPostgreSQLInstance` XRD/composition yazımı + gerçek `kclvm_cli`/`crossplane render`(Docker)/`chainsaw lint` ile doğrulama | 7a (Tenant composition + `tenant-issuer` deseni), 4a (Vault/cert-manager hedefleniyor) | Platform | ✅ tamam | 6/6 size×HA kombinasyonu doğru davranıyor (HA+small GERÇEKTEN reddedildi); 2 örnek claim render edildi; ArgoCD Application eksikliği (Faz 6'dan beri) kapatıldı |
| 8b | Faz 7 | **`XPostgreSQLInstance`'ın gerçek bir cluster'da uçtan uca doğrulanması** (`chainsaw test tests/e2e/`) | 8a, Faz 1-3/6/6b gerçekten kurulu | — | ⬜ | Cluster Ready; bağlantı sırrı görünür; TLS `openssl verify` OK; cross-tenant erişim GERÇEKTEN engelli |
| 9 | Faz 7c | `tenant-requests` akışı + CI doğrulama | 7b, 7c, 8b | Platform | 🔄 kaynaklar + yerel testler tamam | Canlı PR/Actions/merge → Synced/Ready kabulü bekliyor |
| 10 | Faz 8 | Backstage portal + scaffolder template | Faz 7 | Platform | 🔄 kaynaklar + yerel testler tamam | Gerçek Keycloak oturumu ve GitHub PR kabulü bekliyor |
| 11a | Faz 9 | kube-prometheus-stack+Loki+Tempo+OpenCost+haftalık rapor yazımı + `kclvm_cli`/`crossplane render`(Docker)/`promtool`/`envsubst`/`jq` ile doğrulama | Faz 6 (Tenant composition), Faz 1 (Rook-Ceph RGW) | Platform | ✅ tamam (yazım) | Tenant composition 11 kaynak üretiyor (dashboard ConfigMap dahil); 4 alert kuralı `promtool` SUCCESS; `report.sh` mock kubectl/curl'e karşı uçtan uca çalıştı |
| 11b | Faz 9 | **Yukarıdakinin gerçek bir cluster'da uygulanması** (`05-observability.sh`) + `--only cert-alert-test` ile uçtan uca alert doğrulaması | 11a, Faz 1-2 gerçekten kurulu | — | ⬜ | Grafana'da tenant dashboard'u GERÇEKTEN görünüyor; `CertificateExpiringIn1Day` Alertmanager'da GERÇEKTEN FIRING; OpenCost raporu cost-center'a göre GERÇEK veriyle gruplanıyor |
| 11c | Faz 9 (kalan) | Velero + felaket kurtarma tatbikatı | 11b | — | ⬜ | Bir tenant silinip yedekten geri getirildi |
| 12a | Faz 10 | Kyverno imza doğrulama + cluster-wide PSS + secret-sızıntı CI + Harbor SBOM/Trivy gate + Vault audit + DR/break-glass runbook'ları yazımı + `cosign`/`kyverno`/`gitleaks`/`trufflehog`/`syft`/`trivy`/`actionlint` ile doğrulama | Faz 6b (08 politikası), Faz 3 (Vault), Faz 1 (Harbor) | Platform | ✅ tamam (yazım) | İmza doğrulama gerçek registry'de kanıtlandı (kritik cosign v2/v3 uyumsuzluğu bulundu ve CI'da düzeltildi); PSS 36/36 test yeşil; gitleaks/trufflehog gerçek test sırrıyla ikisi de yakaladı; syft/trivy gerçek imajlarla doğrulandı |
| 12b | Faz 10 | **Yukarıdakinin gerçek bir cluster'da uygulanması** + DR runbook'unun gerçek bir test cluster'ında (Velero+Rook-Ceph+Vault kurulu) uçtan uca çalıştırılması | 12a, 11c (Velero kurulu olmalı) | — | ⬜ | Kabul kriteri #3 ("DR runbook'u en az bir kez denenmiş"): şu an KARŞILANMIYOR |
| 13a | Faz 11 | Composition+Kyverno CI (`platform-ci.yaml`), kind tabanlı e2e zincir testi, 2 chaos senaryosu, upgrade/rollback runbook'u yazımı + GERÇEK kind cluster'ında (Crossplane 2.4.0+Vault+cert-manager+CNPG) uçtan uca doğrulama | 8a/8b (Postgres composition), 7a (Tenant composition) | Platform | ✅ tamam | Tenant→Postgres→bağlantı→cert zinciri GERÇEKTEN kanıtlandı; 3 kritik bug bulunup düzeltildi (Crossplane sürümü, CNPG alan adı, eksik cert-manager SA); 2 chaos senaryosu (~7s/~8s'de self-heal) belgelendi |
| 13b | Faz 11 | CI workflow'unun GERÇEKTEN GitHub Actions'ta (gerçek bir PR'da) çalıştırılıp yeşil geçtiğinin doğrulanması; chainsaw e2e testlerinin (Cilium/Rook-Ceph/ESO dahil TAM zincir) gerçek bare-metal cluster'da çalıştırılması | 13a, Faz 1-3/6/6b/7 gerçekten kurulu | — | ⬜ | `platform-ci.yaml` gerçek bir PR'da 3 job'ın da yeşil geçtiği görülmeli; `compositions/*/tests/e2e/` chainsaw testleri ilk kez gerçek cluster'a karşı çalıştırılmalı |
| 14a | Faz 12 | Açık kararların çözülmesi (kod+karar) + teknik borç temizliği + kind tabanlı e2e/chaos/DR ispatlarının GENİŞLETİLMESİ (ESO/OIDC composition değişiklikleri + gerçek Velero+MinIO DR tatbikatı) | 13a (Faz 11 e2e altyapısı) | Platform | ✅ tamam | 38/38 kyverno test; tenant render 38 doküman/claim (postgresql 22/24); GERÇEK kind cluster'ında TAM zincir + cost-center admission testi + DR restore (K8s objeleri TAM, PV verisi HENÜZ DEĞİL) kanıtlandı |
| 14b | Faz 12 | Gerçek bare-metal donanım + organizasyonel gerçekler (IP havuzu, gerçek isimler, DNS, mali veriler) sağlanması | 14a, fiziksel/organizasyonel girdi | — | ⛔ **bu ortamda YAPILAMAZ — dış girdi gerekiyor, platform mühendisliği kapsamı DIŞINDA** | Bölüm 1'in "Bu ortamda KALICI OLARAK tamamlanamayan kalemler" tablosu |
| — | Faz 6.5 (**artık ERTELENEMEZ** — bkz. teknik borç #16) | `naming.k`/`labels.k`/`tiers.k` paylaşımlı KCL modülüne refactor — Faz 7'de İKİNCİ kez ertelendi (`postgresql/function.k` de aynı `_resAnno`/etiket mantığını kopyaladı). Üçüncü composition (`bucket/` vb.) yazılmadan ÖNCE ZORUNLU | Faz 6, Faz 7 | — | ⬜ | Üç composition aynı isimlendirme/etiket mantığını KOPYALAMIYOR |

**Durum ikonları:** ✅ tamam · 🔄 devam ediyor · ⬜ başlanmadı · ⛔ engellendi

---

## 3. Açık kararlar / bekleyen sorular

| # | Soru | Kimi engelliyor | Durum |
|---|---|---|---|
| 1 | MetalLB adres havuzu (IP aralığı) ağ ekibinden alınacak → `.env` `METALLB_IP_RANGE` | Faz 1 adım 2b | ⬜ **açık — kurulumu engelliyor** |
| 2 | Ceph ham disk düzeni → `.env` `CEPH_OSD_DEVICE_FILTER`. Öneri: node başına 2×1TB NVMe, 6 OSD (bkz. `underlay/rook-ceph/README.md`) | Faz 1 adım 2b | ⬜ **açık — kurulumu engelliyor** |
| 6 | Kubernetes API server endpoint'i → `.env` `K8S_API_SERVER_HOST` (kube-proxy'siz kurulumda zorunlu) | Faz 1 adım 2b | ⬜ **açık — kurulumu engelliyor** |
| 7 | Harbor/Keycloak admin parolaları üretilip `.env`'e girilecek (`openssl rand -base64 24`) | Faz 1 adım 2b | ⬜ açık |
| 3 | ~~Vault unseal anahtarları nerede saklanacak~~ → **çözüldü:** rol bazlı şema (docs/runbooks/vault-unseal.md §2) yazıldı; **gerçek kişi isimleri hâlâ atanmalı** | Faz 3 adım 4b öncesi | ⬜ açık — gerçek isimler/roller atanmalı |
| 10 | Vault'un kendi TLS'i açık bırakıldı (`tls_disable=1`, küme-içi plaintext, yalnızca Cilium NetworkPolicy korumalı) — kapatma prosedürü bilerek bu fazda YAPILMADI (bkz. `pki/vault/README.md`) | Faz 3.5 (opsiyonel) | ⬜ açık — aciliyet yok |
| 11 | Root CA gerçek "offline"/ayrı-namespace izolasyonu Vault Enterprise gerektiriyor; OSS'de mount+policy+break-glass ile en yakın karşılığı uygulandı — bu kabul edilebilir mi, yoksa Enterprise lisansı mı alınacak? | — | ✅ **karar verildi (Faz 12): OSS yaklaşımı KALICI olarak kabul edildi.** Gerekçe: Enterprise lisansı gerçek, tekrar eden bir mali taahhüttür — bu platform mühendisliğinin yetki alanı DIŞINDADIR (bütçe/satın alma kararı). Mevcut mount+policy+break-glass şeması, OSS içinde ulaşılabilecek EN GÜÇLÜ izolasyondur ve `vault-break-glass.md` ile denetlenebilir durumdadır. Organizasyon ileride farklı karar verirse bu satır YENİDEN AÇILABİLİR. |
| 4 | Keycloak, kurumsal LDAP/AD'ye federe olacak mı | Faz 4 | ✅ **karar verildi (Faz 12): ŞİMDİLİK GEREKMİYOR.** Platform kendi Keycloak kullanıcı deposunu (yerel) kullanır; gerçek bir kurumsal LDAP/AD dizini federe edilmek istendiğinde Keycloak Admin Console'dan (`User Federation` → `ldap`) EKLENEBİLİR — bu, mevcut realm/grup/RoleBinding yapısını BOZMAYAN, tamamen ek bir adımdır. Bu bir organizasyonel envanter sorusudur (gerçek bir LDAP sunucusu var mı) — platform mühendisliği bunu VARSAYAMAZ. |
| 5 | Wildcard DNS kaydı (`*.apps.<domain>`) delegasyonu | Faz 1 | ⬜ açık |
| 8 | `${PLATFORM_REPO_URL}`/`${PLATFORM_REPO_REVISION}` — gerçek repo URL'i biliniyor olmalı → `.env` | Faz 2 adım 3b | ⬜ **açık — kurulumu engelliyor** |
| 9 | `platform/bootstrap/app-of-apps/**/*.tpl` ve `platform/control-plane/apps/*.tpl` içindeki `${PLATFORM_REPO_URL}` repo-genelinde TEK SEFERLİK somutlaştırılıp commit edilmeli (script bunu kontrol eder, otomatik düzeltmez) | Faz 2 adım 3b | ⬜ **açık — kurulumu engelliyor** |
| 12 | `VAULT_TOKEN` operatörün kendi shell'inde `export` edilmeli (docs/runbooks/vault-unseal.md §3.3'ten) — `03-pki.sh --only auth`/`--only pki` bu olmadan çalışmaz | Faz 3 adım 4b | ⬜ **açık — kurulumu engelliyor** |
| 13 | `XTenant`'ın `oidcGroup`→RoleBinding akışı, K8s API server'ın OIDC auth flag'lerinin (`--oidc-issuer-url` vb., Keycloak'a işaret eden) yapılandırılmış olmasını VARSAYIYOR — bu HİÇBİR fazda henüz yapılmadı | Faz 6 adım 7b (gerçek RBAC testi) | ✅ **TASARIM tamamlandı (Faz 12):** Keycloak `kubernetes` client'ı eklendi, kubeadm `ClusterConfiguration` şablonu (`docs/runbooks/k8s-api-server-oidc.md`) yazıldı, composition'ın RoleBinding subject'i `oidc:` önekiyle güncellendi (04/06 numaralı Kyverno politikaları da AYNI önekle güncellendi — render+kyverno test ile doğrulandı). ⬜ **UYGULAMA hâlâ açık** — gerçek bir kubeadm control-plane node'una `--oidc-*` flag'lerini eklemek fiziksel/sanal bir node erişimi gerektirir, bu ortamda YOK. |
| 14 | Görevin verdiği tier sayıları (4/16/64 CPU) conventions.md'nin ESKİ tablosuyla (2/8/32) çelişiyordu → conventions.md `function.k`'ye göre GÜNCELLENDİ (bkz. Faz 6 günlüğü). Bu, geriye dönük bir doküman düzeltmesiydi, iş kararı değil. | — | ✅ çözüldü (dokümante edildi) |
| 15 | `restrict-tenant-claim-creation`/`restrict-custom-network-tier` politikaları `request.userInfo.groups`'a bağımlı — API server OIDC kurulana kadar (açık karar #13/teknik borç #17) BU İKİSİ **fail-closed** davranır: kimse (ArgoCD dışında) hiçbir Tenant claim'i açamaz/networkTier değiştiremez. Bu kasıtlı bir tasarım kararı mı, yoksa OIDC kurulana kadar bu 2 politika geçici olarak devre dışı mı bırakılmalı? | Faz 6b kurulumu | ✅ **karar verildi (Faz 12): fail-closed KALICI olarak kabul edildi, geçici bypass EKLENMEYECEK.** Gerekçe: bir "geçici bypass" mekanizması (örn. bir feature-flag ile politikayı devre dışı bırakmak), OIDC kurulumu gecikirse KOLAYLIKLA kalıcılaşan ve unutulan bir güvenlik açığına dönüşür — "güvenli ama kullanılamaz" durumu, "kullanılabilir ama güvensiz" durumdan HER ZAMAN tercih edilir. Açık karar #13 (K8s API server OIDC) artık `docs/runbooks/k8s-api-server-oidc.md` ile TASARIM olarak tamamlandı — bypass yerine GERÇEK çözüm (OIDC'yi kurmak) yol haritada. |
| 17 | `.env`'deki `OPENCOST_CPU_HOURLY_COST`/`RAM`/`STORAGE` değerleri **tahmini/placeholder** ($0.0316/vCPU-saat vb.) — gerçek donanım amortismanı + elektrik + bakım maliyetine göre operatör tarafından HESAPLANMALI | Faz 9 kurulumu öncesi | ⬜ **kalıcı olarak açık kalacak — bu bir TASARIM boşluğu DEĞİL, gerçek donanımın/enerji sözleşmesinin somut maliyet verisidir; platform mühendisliği bu rakamı İCAT EDEMEZ.** Prosedür (`openssl`/`.env.example` yorumu) zaten net. |
| 18 | `ALERTMANAGER_WEBHOOK_URL` boş — gerçek bir Slack/Teams incoming-webhook URL'i girilmeli, aksi halde Secret uygulanır ama hiçbir bildirim gitmez | Faz 9 kurulumu öncesi | ⬜ **kalıcı olarak açık kalacak — aynı gerekçe: gerçek bir Slack/Teams workspace'inin webhook URL'i, organizasyona özgü bir sırdır, platform mühendisliği tarafından üretilemez/tahmin edilemez.** |
| 19 | OpenCost'un `aggregate=label:cost-center` sorgusu, AYNI cost-center'ı paylaşan BİRDEN ÇOK tenant varsa maliyeti PAYLAŞILAN TOPLAM olarak döner — tenant başına ayrıştırma YOK (conventions.md §3.1 "bir cost-center genelde bir tenant'a atanır" varsayımına dayanıyor) | weekly-report doğruluğu, kabul kriteri #3 | ✅ **karar verildi ve UYGULANDI (Faz 12): cost-center → tenant 1:1 ZORUNLU kılındı.** Yeni Kyverno ClusterPolicy `09-enforce-unique-cost-center.yaml` (`context.apiCall` ile mevcut Tenant'ları sorgulayıp aynı cost-center'ı FARKLI bir teamName'in kullanmasını reddeder) — `kyverno test` ile 2 yeni senaryo (izin/red) dahil 38/38 yeşil. |
| 20 | DR runbook'unun uçtan uca test cluster'ı — Velero + Rook-Ceph + Vault Raft'ın HEPSİNİN bir arada olduğu bir test ortamı ne zaman/nasıl sağlanacak (bare-metal node'lar mı, yoksa geçici bir bulut cluster'ı kabul edilebilir mi)? | Faz 10 kabul kriteri #3, Faz 9 satır 11c (Velero kurulumu) | 🔄 **Faz 12'de kind tabanlı bir DR tatbikatı DENENDİ — bkz. Bölüm 1 Faz 12 artefaktları ve `chaos-test-results.md` güncellemesi.** |
| 21 | `01-require-signed-images.yaml`'daki Cosign anahtar Secret'ı (`cosign-image-signing-key`, `kyverno` namespace) nasıl/ne zaman üretilecek ve CI'nın kullandığı ÖZEL anahtarla eşleştirilecek — bu görevde yalnızca YAPI yazıldı, gerçek anahtar üretim/dağıtım prosedürü (kim üretir, nerede saklanır) BELİRLENMEDİ | Faz 10 kurulumu öncesi | ✅ **karar verildi ve UYGULANDI (Faz 12): GERÇEK bir cosign v2.4.1 anahtar çifti üretildi.** Public key `platform/policies/security/cosign.pub`'a commit edildi (sır DEĞİL); `02-control-plane.sh`'e `ensure_cosign_signing_key_secret()` eklendi (Secret'ı bu dosyadan otomatik üretir). Özel anahtar bu repoya YAZILMADI — rotasyon prosedürü dosyanın kendi başlık yorumunda belgelendi (CI'nın `COSIGN_PRIVATE_KEY` secret'ı ile EŞ ZAMANLI değişmeli). |
| 16 | `eso-tenant-secrets` Vault rolü TÜM tenant'lar için PAYLAŞILAN tek bir kimlik (ESO'nun kendi controller SA'sı) — tenant-özel Vault k8s-auth izolasyonu (Faz 6'daki `provider-terraform` deseni) burada YOK, yalnızca path-prefix (`kv/data/tenants/*`) izolasyonu var. Bu kabul edilebilir mi, yoksa her tenant için ayrı bir ESO kimliği mi (SA-per-tenant, önemli bir mimari genişleme) gerekiyor? | — | ✅ **karar verildi ve UYGULANDI (Faz 12): per-tenant ESO kimliğine geçildi.** `tenant/function.k`'ye `esoServiceAccount`/`esoTokenRequestRole`/`RoleBinding` + Terraform modülüne `vault_kubernetes_auth_backend_role.eso_tenant`/`vault_policy.eso_tenant` (yalnızca `kv/data/tenants/<namespace>/*`) eklendi; `postgresql/function.k`'nin SecretStore'u artık `eso-tenant-<namespace>` rolünü + tenant-özel SA'yı kullanıyor — path-prefix izolasyonuna EK olarak artık KİMLİK izolasyonu da var. Render+sync doğrulandı (38 doküman/claim). |

---

### Faz 8 kurulum bağımlılıkları

- `configure.py` için repo/image/domain ve onaylı User email'i; Vault OIDC/GitHub/DB/session secret'ları; mevcut realm'de backstage client'ı, TLS Gateway ve DNS gerekir.
- CI için PLATFORM_REPO/PLATFORM_REPO_REF variables ve private repo ise read-only token gerekir. Fork PR'larında private platform secret'ı mevcut değilse kontrol fail-closed davranır.
- Portal salt-okunur görünümü giriş yapabilen kullanıcılar arasında paylaşılır; tenant bazlı görünürlük izolasyonu uygulanmadı. Varsayılan okuyucu yalnızca platform cluster'ını okur; ek cluster'lar ayrıca yapılandırılır.

## 4. Bilinen sorunlar / teknik borç

| # | Konu | Etki | Planlanan çözüm |
|---|---|---|---|
| 1 | **conventions sapması:** `conventions.md` §5 blok StorageClass'ı `ceph-block-<tier>` olarak tanımlıyor; Faz 1'de `ceph-block` kullanıldı | Doküman ile gerçek uyuşmuyor | conventions §5 güncellensin **veya** Faz 6'da tier bazlı sınıflar eklenip `ceph-block` alias kalsın. Karar Faz 6'da. |
| 2 | Harbor ve Keycloak kendi bundled PostgreSQL'lerini kullanıyor | HA yok, PITR yok, yedek yok | Faz 4: CloudNativePG'ye taşınacak |
| 3 | default-deny muafiyet listesi **tüm** platform namespace'lerini kapsıyor | Platform bileşenleri arası trafik kısıtsız | Faz 4-5: her bileşen kendi açık politikasını alsın, liste daraltılsın |
| 4 | TLS hiçbir yerde yok (Harbor, Keycloak, RGW, Ceph dashboard → HTTP) | Küme içi düz metin trafik | Faz 3: Vault PKI + cert-manager (ADR-0001 Karar 2.3) |
| 5 | **DOĞRULANDI (Faz 12b), artık RİSK DEĞİL, ONAYLANMIŞ BİR OLGU:** `docker.io/bitnami/keycloak` VE `docker.io/bitnami/postgresql`'in versiyonlu (pinlenmiş) tag'leri Docker Hub'ın ücretsiz katmanından TAMAMEN KALDIRILDI (Broadcom, 2025) — `bitnami/keycloak` reposu artık HİÇ tag İÇERMİYOR, `bitnami/postgresql` yalnızca `latest` içeriyor. Canlı bir kind cluster'ında `ImagePullBackOff`/"not found" ile doğrulandı | Kurulum bu HALİYLE KESİN olarak `ImagePullBackOff` ile başarısız olur — risk DEĞİL, garanti bir arıza | ✅ **GEÇİCİ ÇÖZÜM UYGULANDI (Faz 12b):** aynı imajlar+tag'ler `docker.io/bitnamilegacy/<isim>` altında hâlâ erişilebilir — `.env.example`'a (daha önce HİÇ TANIMLI OLMAYAN) `KEYCLOAK_IMAGE_REGISTRY="docker.io/bitnamilegacy"` eklendi, canlı test edildi (Keycloak+bundled Postgres pod'ları Running oldu). BU KALICI DEĞİL — `bitnamilegacy` Broadcom'un ne kadar süre koruyacağını garanti etmediği bir geçiş reposu; kalıcı çözüm hâlâ Faz 4'te resmî Keycloak Operator veya kendi imaj mirror'ınız |
| 6 | Harbor chart'ının S3 `existingSecret` anahtar isimleri sürüme bağlı | Registry S3'e bağlanamayabilir | Kurulum öncesi `helm show values` ile teyit (script'te yorum olarak işaretli) |
| 7 | ✅ **KAPANDI (Faz 12d):** `production: true` + Vault PKI'dan cert-manager Certificate (`tls.usePem`) ile GERÇEK HTTPS canlı doğrulandı (`openssl`/`curl`, zincir leaf→intermediate→root CN eşleşti). Yalnızca ilk-kurulum SIRALAMASI hâlâ bilinçli bir çapraz-faz bağımlılığı taşıyor (Keycloak Faz 1, Vault PKI Faz 3 — Vault'un kendi self-TLS runbook'uyla AYNI desen, elle tetiklenir) | — | — |
| 8 | Harbor/Keycloak ADR'ye göre L4; Faz 1 script'i bunları underlay ile birlikte kuruyor | Katman sınırı bulanıklaşıyor | Faz 4: kendi root Application'larına taşınacak (`bootstrap/README.md`) |
| 9 | `versions.env` sürümleri yazıldığı andaki kararlı sürümler | Eskimiş olabilir | İlk kurulumdan önce `helm search repo --versions` ile doğrula |
| 10 | **App-of-Apps'ın git-watched dizinlerindeki `.tpl` dosyaları** (`bootstrap/app-of-apps/underlay/*`, `control-plane/apps/*`) ArgoCD tarafından DOĞRUDAN git'ten okunuyor; `${PLATFORM_REPO_URL}` gibi repo-genelinde sabit değerler render edilip commit edilmeden ArgoCD bunları literal string olarak görür | `underlay-root` Application'ı OutOfSync/hatalı kalır; crossplane/kyverno/eso'nun chart repo/versiyon alanları ise Faz 2'de zaten somutlaştırıldı (bu tabloda #10 yalnızca PLATFORM_REPO_URL/REVISION için geçerli) | `02-control-plane.sh`'in `check_git_placeholders_resolved()` adımı bunu tespit edip durur ve tam `sed`+`git commit` komutunu basar — Faz 2'de KISMEN kapatıldı (kontrol var, otomatik düzeltme yok) |
| 11 | ✅ **BÜYÜK ÖLÇÜDE KAPANDI (Faz 12d):** 11 politikadan 10'u GERÇEK bir cluster'da Enforce moduna alınıp canlı doğrulandı (yeni tenant zinciri bunlarla Ready=True'ya ulaştı). Yalnızca `require-pod-resources` KASITLI olarak Audit'te BIRAKILDI — Enforce denemesi platformun HEMEN HEMEN HER YERİNDE (Vault/Keycloak/Postgres dahil) `limits.cpu` eksikliğini ortaya çıkardı | `limits.cpu` temizlik geçişi (ayrı, kapsamlı bir görev) tamamlanmadan bu TEK politika Enforce'a ALINAMAZ | Faz 13+: repo-genelinde `limits.cpu` denetimi + düzeltmesi, sonra Enforce |
| 12 | `provider-kubernetes`/`provider-helm` ServiceAccount'ları `cluster-admin`'e bağlanıyor | Geniş yetki (statik kimlik bilgisi değil, ama blast radius büyük) | Kyverno guardrail'leri ve audit log bunu sınırlar; ADR-0001'e ek bir "neden bu genişlikte" notu Faz 5'te gözden geçirilecek |
| 13 | Vault listener'ı `tls_disable=1` — küme-içi trafik TLS'siz (Cilium NetworkPolicy ile korunuyor, şifreleme ile değil) | Vault'a giden/gelen trafik düz metin (küme-içi, ağ katmanı korumalı) | ✅ **Prosedür TAMAMLANDI (Faz 12):** `docs/runbooks/vault-self-tls.md` — rol oluşturma, sertifika üretimi, Helm values değişikliği, doğrulama, geri alma dahil TAM prosedür yazıldı. BİLİNÇLİ OLARAK otomatik bootstrap akışına bağlanmadı (elle tetiklenen bakım penceresi işlemi — 3 pod'un sırayla restart+unseal edilmesini gerektirir, bir script'in ortasında riskli). |
| 14 | Root CA izolasyonu Vault Enterprise Namespaces DEĞİL — mount+policy+break-glass ile OSS içinde en yakın karşılığı | "Offline root" tam anlamıyla sağlanmıyor (Vault barrier'ı içinde, ayrı bir policy arkasında) | İş kararı: Enterprise lisansı mı alınacak, yoksa mevcut OSS yaklaşımı mı kalıcı kabul edilecek (açık karar #11) |
| 15 | Vault'un iç PKI rolü, `common_name`'i validate ederken `allow_glob_domains=true` kullanıyor — glob domain'ler bazı Vault sürümlerinde `allowed_domains`'in yorumlanma biçimini değiştirebilir | Beklenenden geniş/dar bir sertifika kapsamı riski | ~~İlk gerçek sertifika testinde davranış doğrulanmalı~~ → **Faz 6'da `allow_subdomains=true` + tek bir `allowed_domains` girdisine geçildi, `allow_glob_domains` KULLANILMADI** (tenant PKI role'lerinde). Platform-genelindeki `platform-<env>` rolleri hâlâ eski ayarı kullanıyor — Faz 3.5'te oraya da uygulanmalı. |
| 16 | `compositions/kcl/naming.k`/`tiers.k`/`labels.k` paylaşımlı modülleri conventions.md §5/§7'de ZORUNLU olarak referans veriliyor ama Faz 6, mantığı `compositions/tenant/function.k` içine INLINE yazdı (KCL modül registry'si yok) | İkinci composition yazıldığında isimlendirme/etiket mantığı kopyalanma riski taşıyor | Faz 6.5 (bkz. Bölüm 2): ikinci composition'dan (`XDatabase`) önce paylaşımlı modüle refactor edilecek |
| 17 | `XTenant`'ın RBAC'ı (`oidcGroup` → RoleBinding subject) K8s API server'ın OIDC entegrasyonuna bağımlı | RoleBinding'ler oluşur ama API server OIDC token'ı doğrulamadan hiçbir OIDC kullanıcısı bunlardan yararlanamaz | ✅ **TASARIM tamamlandı + KISMEN CANLI DOĞRULANDI (Faz 12d):** kind'ın kendi kubeadm node'unun GERÇEK `kube-apiserver.yaml` static pod manifest'i `--oidc-*` bayraklarıyla patchlendi, API server BAŞARIYLA yeniden başladı (tam CRD/controller init, hata YOK — mekanizma MEKANİK olarak sağlam). ⬜ Gerçek bir OIDC token'ıyla uçtan uca `kubectl` testi, cluster kararlılığını riske atmamak için BİLİNÇLİ olarak yapılmadı (temkinli geri alma) — bir sonraki adım: aynı patch'i tekrarlayıp restart'ın TAMAMLANMASINI bekleyip gerçek bir Keycloak token'ıyla doğrulamak. |
| 18 | XTenant composition, cert-manager'ın Vault policy'sini (`pki-int-*/sign/tenant-*`) ve provider-terraform'un yeni bir Vault auth role'ünü (`provider-terraform`) GEREKTİRİYOR — bunlar Faz 3'ün `03-pki.sh`'ine Faz 6'da SONRADAN eklendi | Faz 3 zaten "tamamlanmış" (4a ✅) sayılan bir script, Faz 6 nedeniyle genişletildi — sıra dışı bir bağımlılık yönü | Kabul edilebilir: `03-pki.sh` idempotent, tekrar çalıştırılması güvenli; PLATFORM_CONTEXT bu değişikliği açıkça kaydediyor |
| 19 | `restrict-certificate-issuer` politikası, bir Certificate'ın `tenant-issuer` ADINA referans verdiğini doğruluyor ama o adı taşıyan Issuer NESNESİNİN gerçekten XTenant composition'ı tarafından üretildiğini doğrulamıyor — sahte/elle yaratılmış bir "tenant-issuer" objesi teorik olarak hâlâ mümkün | Certificate policy'si tek başına tam izolasyon garantisi VERMEZ; asıl garanti (05'teki gibi) bir "Issuer sahipliği" politikasıyla tamamlanmalı | Görev kapsamı dışında bırakıldı (yalnızca Certificate'ın issuerRef'i istendi); ileride 05'in ownership-check desenine benzer bir politika eklenebilir |
| 20 | `restrict-tenant-claim-creation` ve `restrict-custom-network-tier`, K8s API server OIDC entegrasyonu (teknik borç #17) kurulana kadar **fail-closed** — hiç kimse (ArgoCD hariç) Tenant claim'i açamaz/custom tier seçemez | Self-servis akışı OIDC kurulana kadar FİİLEN ÇALIŞMAZ (güvenlik açısından doğru ama kullanılabilirlik açısından engelleyici) | Açık karar #15: OIDC'nin ne zaman kurulacağına bağlı; bilinçli bir risk kabulü olarak bırakıldı, sessizce geçilmedi |
| 21 | **İkinci kez ertelenen refactor:** `_resAnno` lambda'sı + etiket şeması artık `tenant/function.k` VE `postgresql/function.k`'de KOPYA (teknik borç #16'nın ta kendisi, şimdi 2 composition'ı kapsıyor) | Üçüncü composition'da bakım yükü katlanır | Faz 6.5 artık ERTELENEMEZ (bkz. Bölüm 2) — üçüncü composition'dan ÖNCE yapılmalı |
| 22 | `XPostgreSQLInstance`'ın `ExternalSecret`/`PushSecret` round-trip'i, CNPG'nin ZATEN ürettiği `<name>-app` Secret'ını gereksiz yere Vault'a push edip geri çekiyor — teknik bir zorunluluk DEĞİL, platform standardını (ADR-0001 Karar 2.3) tutarlı kılmak için bilinçli bir tasarım tercihi | Ekstra 3 kaynak (SecretStore/PushSecret/ExternalSecret) + Vault/ESO'ya bağımlılık, CNPG'nin native Secret'ı zaten yeterliyken | Kabul edilebilir bir tasarım tercihi olarak bırakıldı; README'de "neden" açıkça belgelendi, sessizce geçilmedi |
| 24 | `platform/control-plane/observability/{loki,tempo}/values.yaml.tpl`, RGW endpoint'ini (`rook-ceph-rgw-platform-store.rook-ceph.svc.cluster.local:80`) **AYNI şekilde HARDCODE ediyor** — teknik borç #23'ün (postgresql/function.k) ÜÇÜNCÜ tekrarı, artık 3 dosyayı kapsıyor | `CEPH_OBJECTSTORE_NAME` değişirse bu İKİ dosya da SESSİZCE yanlış endpoint'e yazar | Kalıcı çözüm: Helm/KCL şablonlarının `.env` okuyabildiği bir mekanizma (örn. `envsubst` ile RGW endpoint'inin de değişken haline getirilmesi) — Faz 9.5+ |
| 25 | Cilium'un Hubble `drop`/`tcp`/`flow`/`dns` metriklerine eklenen `labelsContext=source_namespace,destination_namespace`, Cilium'un GERÇEK bir sürümüne karşı DOĞRULANMADI (yalnızca dokümantasyon sözdizimine göre yazıldı — bu görevde `cilium`/Docker ile canlı bir Hubble metrik testi YAPILAMADI) | Sözdizimi hatalıysa Cilium agent Hubble metrik exporter'ı sessizce eksik etiketle veya hiç başlamaz | Kurulumdan sonra `hubble observe -o json` ve Prometheus `/metrics` çıktısında `source_namespace`/`destination_namespace` etiketlerinin GERÇEKTEN göründüğü doğrulanmalı |
| 26 | `weekly-report` CronJob'unun `alpine/k8s:1.30.4` imajı **hiçbir registry/pull doğrulaması YAPILMADAN** seçildi (bu ortamda `docker pull` denenmedi) | İmaj mevcut değilse/erişilemezse CronJob `ImagePullBackOff` verir | Kurulumdan önce `docker pull alpine/k8s:1.30.4` (veya iç mirror'a kopyalama) ile doğrulanmalı |
| 27 | `alertmanager-kube-prometheus-stack-alertmanager` Secret adı, Alertmanager operator'ının VARSAYILAN adlandırma kuralına (release adı `kube-prometheus-stack` olduğu için) DAYANIYOR — `values.yaml`'da AÇIKÇA bir `alertmanagerConfigSecret` override'ı YOK; Helm release adı değişirse bu Secret'ı KİMSE OKUMAZ | Release adı `kube-prometheus-stack` DIŞINDA bir şey olursa Alertmanager routing config'i sessizce boş/varsayılan kalır | `05-observability.sh`'in `helm.releaseName: kube-prometheus-stack` (apps/01-kube-prometheus-stack.yaml.tpl) sabit tutulmalı; değiştirilirse Secret adı da elle güncellenmeli |
| 28 | **cosign v3.x, Kyverno 1.19.1 ile UYUMSUZ** (bkz. Faz 10 günlüğü) — v3'ün varsayılan imza formatı (OCI 1.1 Referrers API + sigstore-bundle, `--registry-referrers-mode=legacy` ile bile İÇERİK formatı değişmiyor) Kyverno'nun gömülü doğrulayıcısı tarafından okunamıyor | CI'da `cosign-installer@latest` veya benzeri kullanılırsa imzalı imajlar SESSİZCE reddedilir (imza var ama Kyverno "no signatures found" der) | CI, cosign'ı v2.4.1'e SABİTLEDİ (`image-supply-chain.yaml`); Kyverno güncellendiğinde (yeni bundle formatını destekleyen bir sürüm) bu pin KALDIRILABİLİR — o zamana kadar ASLA `latest` kullanılmamalı |
| 29 | Velero'nun S3 backup hedefi Rook-Ceph RGW'dir (Loki/Tempo/Harbor ile aynı OBC deseni) — Rook-Ceph TAMAMEN kaybolursa Velero'nun KENDİ yedekleri de kaybolur (tek nokta bağımlılığı, disaster-recovery.md §3.1'de keşfedildi) | Bir "C senaryosu" (Rook-Ceph felaketi) + "D senaryosu" (tam cluster kaybı) birleşirse, K8s objelerinin restore edilecek bir yedeği KALMAYABİLİR | İkinci, cluster-DIŞI bir `BackupStorageLocation` (gerçek bulut S3 veya coğrafi ayrı ikinci Ceph) eklenmesi ÖNERİLDİ, bu görev kapsamında UYGULANMADI |
| 30 | `02-require-pss-restricted-clusterwide.yaml`'ın `exclude` listesi ile `pod-security-admission-configuration.yaml`'ın `exemptions.namespaces` listesi AYNI namespace'i İKİ AYRI dosyada ELLE senkron tutuyor | Biri güncellenip diğeri unutulursa iki savunma katmanı SESSİZCE birbirinden sapar (function.k/composition.yaml drift'iyle AYNI sınıf risk) | ✅ **ÇÖZÜLDÜ (Faz 12):** `verify-pss-exemption-sync.sh` yazıldı (iki listeyi karşılaştırır) ve `platform-ci.yaml`'a GERÇEK bir CI adımı olarak bağlandı — drift artık her PR'da otomatik yakalanır. Çalıştırılınca 2 satırlık gerçek bir drift (test-only namespace'ler) BULUP DÜZELTTİ. |
| 37 | Kyverno `context.apiCall` kullanan politikalar (örn. `enforce-unique-cost-center`), sorguladıkları CRD'ler için Kyverno'nun admission-controller ServiceAccount'ına AÇIKÇA RBAC verilmedikçe "unknown" hatasıyla BAŞARISIZ olur — ve `failurePolicy`'ye bağlı olarak bu, TÜM ilgili kaynak türü isteklerini (burada: her Tenant claim'i) REDDEDEBİLİR | GERÇEK bir kind cluster'ında test edilirken keşfedildi: RBAC olmadan HİÇBİR Tenant claim'i açılamıyordu (politika "güvenli" tarafta hata verdi, ama tamamen engelleyiciydi) | `platform/policies/rbac/kyverno-tenant-reader-rbac.yaml` eklendi + `01-kyverno.yaml.tpl`'in ArgoCD Application'ına `platform/policies/rbac` directory source'u bağlandı (bu dizin de HİÇBİR Application'a bağlı DEĞİLDİ — AYNI türden bir GitOps-wiring boşluğu, aynı oturumda keşfedilip kapatıldı) |
| 31 | Vault audit log'ları (`enable_vault_audit()`) her Vault pod'unda YEREL (paylaşımlı PV yok) — merkezi bir görünüm (Loki'ye toplama) YAZILMADI | Break-glass sonrası çapraz doğrulama (bkz. `vault-break-glass.md` §4) 3 pod'un HER BİRİNİ ayrı ayrı kontrol etmeyi gerektiriyor, tek bir sorguyla YAPILAMIYOR | Bir promtail/Vector DaemonSet'i ile `/vault/audit/*.log`'un Loki'ye toplanması (control-plane/observability/ zaten kurulu, entegrasyonu eksik) |
| 33 | `platform-ci.yaml`'ın `kyverno-policy-tests` job'ı, `security-scan.yaml`'ın `kyverno-policy-regression` job'ıyla AYNI komutu (`kyverno test platform/policies/tests/`) İKİ AYRI workflow'da ÇALIŞTIRIYOR | Her PR'da gereksiz CI süresi/kaynak kullanımı (küçük ama var) | Bilinçli bırakıldı (bkz. `platform-ci.yaml` başlık yorumu — iki farklı "bütünlük sorusu"na cevap veriyorlar); tekilleştirme (ortak bir reusable workflow) ileride yapılabilir |
| 34 | kind tabanlı e2e testi (`tests/e2e/kind-chain/run.sh`), Cilium/Rook-Ceph/ESO'ya bağımlı 7 composed kaynağı (bkz. README.md tablosu) test KAPSAMI DIŞINDA bırakıyor — yalnızca Vault/cert-manager/CNPG zincirini test ediyor | Cilium NetworkPolicy'lerinin, Rook OBC'lerin, ESO SecretStore/PushSecret/ExternalSecret round-trip'inin GERÇEK reconcile davranışı HÂLÂ hiç test edilmedi | `compositions/{tenant,postgresql}/tests/e2e/` chainsaw testleri (TAM zincir, Faz 6/7'de yazıldı) bunu kapsar ama YALNIZCA gerçek bare-metal/tam-platform cluster'ında çalıştırılabilir — hâlâ hiç çalıştırılmadı (bkz. Bölüm 2 satır 7b/8b) |
| 35 | Chaos testleri (CNPG replika/Vault pod silme), TAM composition zincirinden geçmiş bir Tenant/Postgres'e karşı DEĞİL, standalone kurulan CNPG Cluster/Vault'a karşı yapıldı | Platformun kendi ürettiği (ResourceQuota/NetworkPolicy/PDB ile kısıtlı) bir gerçek tenant Postgres'inin chaos altında AYNI şekilde davrandığı doğrulanmadı | Bare-metal/tam-platform kurulumunda chaos testleri GERÇEK bir tenant claim'ine karşı TEKRARLANMALI |
| 36 | Vault'un otomatik unseal'ı (auto-unseal, KMS/Transit entegrasyonu) YOK — **Faz 12d'de İKİNCİ KEZ, GERÇEK bir chaos testinde (Vault lideri canlı silindi) doğrulandı**: raft failover ~6s'de otomatik oldu ama silinen pod'un KENDİSİ yeniden Sealed döndü ve ELLE unseal edildi | Gerçek bir node/pod kaybı olayında, kalan quorum korunsa bile YENİDEN BAŞLAYAN her Vault pod'u insan müdahalesi olmadan hizmete DÖNEMEZ | İş kararı: bir KMS/Transit auto-unseal mekanizması eklenip eklenmeyeceği — eklenirse Shamir'in "hiç kimse tek başına açamaz" güvencesi FARKLI bir güven modeline kayar. Self-hosted bir Transit-unseal Vault'u (bulut KMS DEĞİL, ADR-0001'in "bulut yok" kararıyla UYUMLU) mimari olarak mümkün, bu görev kapsamında UYGULANMADI |
| 32 | Kyverno `PolicyException`'ların `review-date` annotation'ı OTOMATİK takip edilmiyor — süresi geçmiş bir istisna sessizce açık kalabilir | Gerekçe geçerliliğini yitirmiş bir istisna fark edilmeden kalıcılaşabilir | Bir CronJob/script `review-date`'i geçmiş `PolicyException`'ları raporlayabilir — bu görev kapsamında YAZILMADI (bkz. `policies/security/README.md` "İstisna süreci") |
| 38 | ✅ **KAPANDI (Faz 12d):** yeni bir cosign v2.4.1 anahtar çifti üretildi, `security/cosign.pub` güncellendi, `cosign-image-signing-key` Secret'ı yeniden oluşturuldu. `require-signed-images` politikası GERÇEK bir Kyverno 3.3.6'ya Enforce modunda uygulanıp hem imzalı-kabul hem imzasız-red canlı doğrulandı (bu sırada policy'nin KENDİSİNDE 2 ayrı, daha önce hiç yakalanmamış bug bulunup düzeltildi — bkz. Faz 12d günlüğü #19-20) | ÖZEL anahtar (`cosign.key`) yalnızca scratchpad'de/CI secret'ında durur, repoya YAZILMADI — CI'nın `COSIGN_PRIVATE_KEY` secret'ının bu YENİ özel anahtarla EŞ ZAMANLI güncellenmesi GEREKİYOR (bu görev kapsamı dışı, gerçek CI erişimi gerektirir) | — |
| 39 | Bu repo (`/Users/ardamoustafa/infastruce`) hâlâ bir git deposu DEĞİL — hiçbir commit geçmişi yok, ArgoCD'nin App-of-Apps modeli (bir git URL'sinden senkronize) fiilen HİÇBİR ZAMAN gerçek bir repo'ya karşı TEST EDİLMEDİ (yalnızca `kubectl apply -f` ile manifestler DOĞRUDAN uygulanarak test edildi) | GitOps akışının kendisi (push → ArgoCD sync → drift detection) bu ortamda hiç KANITLANMADI; yalnızca kaynakların KENDİSİ doğrulandı | Git deposu başlatmak (ve nereye push edileceği: `PLATFORM_REPO_URL`) bir kapsam/altyapı kararı — bu görev kapsamında YAPILMADI, kullanıcıya sorulması gerekiyor |
| 23 | `platform/compositions/postgresql/function.k`, Rook-Ceph RGW object store adını (`platform-store`, Faz 1'in `.env.example` varsayılanı) SABİT/HARDCODE ediyor — KCL'in shell ortam değişkenlerine erişimi yok | Faz 1'de `CEPH_OBJECTSTORE_NAME` değiştirilirse bu composition SESSİZCE yanlış endpoint'e yazar | Bu değer değiştirilirse `function.k`'deki `_rgwEndpoint` de ELLE güncellenmeli — README'de açıkça uyarıldı |

---

## 5. Faz günlüğü

Her faz kapanışında buraya **tek satır** eklenir: ne yapıldı, ne öğrenildi.

| Tarih | Faz | Özet | Öğrenilen |
|---|---|---|---|
| 2026-09-15 | Faz 12l | Kullanıcının YEDİNCİ kod incelemesinde bildirdiği 4 bulgu ele alındı — biri (#1) `helm template` ile CANLI KANITLANMIŞ, bu oturumun Vault TLS çalışmasını BAŞINDAN BERİ sessizce no-op yapan bir kritik bug. — (1) **[KRİTİK] Vault TLS geçişi/yenilemesi pod'ları GERÇEKTEN yeniden başlatmıyordu:** `helm template hashicorp/vault ... -f values.yaml -f values-tls.yaml` ile DOĞRUDAN doğrulandı: chart'ın `server.updateStrategyType` VARSAYILANI `OnDelete`dir. OnDelete altında `kubectl rollout restart` (Faz 12j/12k'nin kullandığı) bir NO-OP'tur — StatefulSet şablonu güncellenir ama VAR OLAN pod'lar KENDİLİĞİNDEN değiştirilmez, yalnızca MANUEL SİLİNİNCE. Yani Faz 12j'den beri hem `03-pki.sh:enable_vault_tls()` HEM `tests/e2e/kind-chain/run.sh`'in TLS adımı, pod'ları SESSİZCE eski (plaintext ya da eski sertifikalı) konfigürasyonda BIRAKIYORDU — yalnızca "Running" fazını bekleyip "başarılı" raporluyordu, HANGİ config'in ÇALIŞTIĞINI hiç doğrulamıyordu. ÇÖZÜM: HashiCorp'un ÖNERDİĞİ sırayla (standby'lar ÖNCE, active EN SON — HA Mode `vault status` çıktısından okunur) HER pod AÇIKÇA silinip yeniden oluşturuluyor, unsealed olması bekleniyor, VE mounted sertifikanın SHA-256'sı yerel dosyayla KARŞILAŞTIRILARAK gerçekten YENİ olduğu kanıtlanıyor — hem 03-pki.sh'de hem e2e'de. (2) **[YÜKSEK] Kubernetes auth, Vault pod'u yenilenince/token süresi dolunca bozulabilirdi:** `token_reviewer_jwt=@dosya` sözdizimi dosya İÇERİĞİNİ o ANDA okuyup SABİT bir string olarak Vault'a yazıyordu — projected SA token'ları TTL'li/rotasyonludur, ~1 saat sonra Vault'un YAKALADIĞI değer GEÇERSİZ olur, TÜM YENİ login'ler (ESO/cert-manager/tenant Job'ları) başarısız olmaya BAŞLAR. `token_reviewer_jwt` alanı KALDIRILDI — resmi Vault dokümantasyonuna göre boş bırakılınca Vault KENDİ SA token dosyasını CANLI okur (rotasyonu otomatik takip eder); `tests/e2e/kind-chain/run.sh` bu deseni ZATEN (bilerek ya da tesadüfen) kullanıyordu — GERÇEKTEN ÇALIŞAN bir testte KANITLANMIŞ doğru davranış buraya da taşındı. (3) **[KRİTİK, yetkilendirme açığı] Tenant talep yetkisi olan kullanıcı BAŞKA tenant'ın yönetici grubunu değiştirebilirdi:** `tenant-claim-requester` RBAC rolü TÜM Tenant claim'leri üzerinde genel `update`/`patch` veriyordu (RBAC bir claim'i "benimki/başkasınınki" diye AYIRT EDEMEZ); Kyverno'nun claim-yetkilendirme politikası yalnızca CREATE'i kontrol ediyordu; `oidcGroup` (namespace'in admin RoleBinding subject'i) immutable DEĞİLDİ. İKİ katmanlı düzeltme: (a) `tenant/xrd.yaml`'a `oidcGroup` için CEL immutable kuralı (teamName/environment/tenantRef İLE AYNI desen); (b) `04-restrict-tenant-claim-creation.yaml`'a YENİ bir UPDATE kuralı — yalnızca claim'in KENDİ oidcGroup'undaki kullanıcılar, platform-admins, veya ArgoCD güncelleyebilir. `kyverno apply --userinfo` (bu turda keşfedilen doğru CLI yöntemi — `--set request.userInfo.groups[0]=...` SESSİZCE İŞE YARAMIYOR, boş bir listeye düşüyor) ile 3 GERÇEK senaryo (admin/sahip/saldırgan) CANLI doğrulandı. (4) **[KRİTİK] Yeni offsite-sync CronJob'u mevcut imaj politikası tarafından ENGELLENİYORDU:** `amazon/aws-cli` imajı Harbor'dan gelmiyor, `offsite-sync` namespace'i HİÇBİR exclude listesinde (deny-non-harbor-images, PSS-clusterwide) YOKTU — kullanıcının kendi `kyverno apply` testi `fail: 1 — deny-non-harbor-images` gösterdi. Her iki listeye `offsite-sync` eklendi; kullanıcının BİREBİR senaryosuyla `kyverno apply` YENİDEN çalıştırılıp REDDİN ORTADAN KALKTIĞI (sessiz geçiş) doğrulandı. **Doğrulama:** #1 → `helm template` ile `updateStrategy: OnDelete` CANLI kanıtlandı (bu bulgunun KENDİSİ), düzeltme statik incelendi + `bash -n` — GERÇEK bir Vault cluster'ında pod silme/yeniden oluşturma döngüsü BU ORTAMDA ÇALIŞTIRILAMADI (dürüstçe işaretli); #2 → statik inceleme, resmi Vault davranışına ve e2e'nin ZATEN kullandığı desene dayanıyor; #3 → `kyverno apply --userinfo` ile 3 senaryo (admin/owner/attacker) CANLI doğrulandı + TAM Kyverno test suite'i (38/38); #4 → `kyverno apply` ile ÖNCESİ/SONRASI BİREBİR karşılaştırma CANLI doğrulandı. | (1) "Helm upgrade + rollout restart" varsayımını (BU OTURUMDA defalarca tekrarlanan bir desen) HİÇ SORGULAMADAN kullanmak — bir chart'ın `updateStrategy`sinin GERÇEKTEN "RollingUpdate" olduğunu `helm template`/`helm show values` ile DOĞRULAMADAN — sessiz bir no-op'u haftalar/fazlar boyunca "çalışıyor" sanmaya yol açabilir; BU BULGU, kullanıcının GERÇEKTEN `helm template` ÇALIŞTIRDIĞI (statik kod okuması DEĞİL) için ortaya çıktı — kanıt HER ZAMAN "chart'ın gerçek render edilmiş çıktısı" seviyesinde aranmalı, values dosyalarını okumak YETERLİ değildir. (2) Bir kimlik doğrulama YAPILANDIRMASINI YAZARKEN "bu değer NE SIKLIKTA değişir/rotate olur" sorusunu SORMAMAK — bir dosyanın İÇERİĞİNİ `@dosya` ile "bir kez oku, sonsuza dek sakla" semantiğiyle YAKALAMAK, o dosya KISA ÖMÜRLÜYSE (TTL'li token gibi) saatler/günler SONRA SESSİZCE bozulan bir yapılandırma üretir — resmi dokümantasyon genellikle TAM OLARAK bu sınıf hatayı ÖNGÖRÜP bir "boş bırak, otomatik algıla" kaçış yolu sunar. (3) Bir "kimlik ALANI" (oidcGroup gibi, bir RBAC subject'inin KAYNAĞI) hem RBAC hem admission-policy seviyesinde AÇIK bırakılırsa, "yalnızca yetkili roller claim açabilir" kontrolü YANILTICI bir güven duygusu verir — CREATE'i kısıtlamak, o kaynağın SONRADAN bir UPDATE ile kimlik-taşıyan alanlarının DEĞİŞTİRİLMESİNİ KISITLAMAZ; her iki işlem (CREATE ve UPDATE) AYRI AYRI, KENDİ risklerine göre değerlendirilmelidir. (4) Yeni bir platform bileşeni (burada: offsite-sync CronJob'u) eklerken, o bileşenin çalışacağı namespace'i TÜM ilgili güvenlik politikalarının (imaj kısıtı, PSS, ağ politikası istisnaları) exclude listelerine eklemek AYRI, KOLAYCA UNUTULAN bir adımdır — bu OTURUMDA ÜÇÜNCÜ kez AYNI sınıf hata (cnpg-system/velero, sonra offsite-sync) tekrarlandı; yeni bir namespace/bileşen eklerken bu exclude listelerini KONTROL ETMEK bir KONTROL LİSTESİ maddesi olmalı, tek seferlik bir hatırlatma değil. |
| 2026-09-15 | Faz 12k | Kullanıcının ALTINCI kod incelemesinde bildirdiği 5 bulgu (#11-15) ele alındı — biri (14) e2e script'ini ÇALIŞTIRILAMAZ durumdan çıkaran, KENDİ ÖNCEKİ fazımın (12j) yarattığı gerçek bir regresyonu da AÇIĞA ÇIKARDI. — (11) **[YÜKSEK] Silme onayı gerçek yetki DOĞRULAMASI yapmıyordu:** annotation'lar yalnızca "boş mu" diye kontrol ediliyordu, KİM yazdığı denetlenmiyordu. `10-protect-prod-tenant-deletion.yaml`'a 4 YENİ kural: annotation DEĞERİ DEĞİŞTİĞİNDE isteği yapan `platform-admins` grubunda olmalı (ArgoCD'nin kendi SA'sı, `restrict-tenant-claim-creation.yaml` ile AYNI gerekçeyle istisna). Kuralın `request.object` vs `request.oldObject` karşılaştıran precondition'ı `kyverno apply --set` ile CANLI TEST EDİLEMEDİ (CLI, `-r` ile verilen kaynağı hem object hem oldObject'e bağlıyor — denendi, kanıtlandı) — dürüstçe işaretlendi. Kalan açık: gerçek PR/branch-protection'ın UYGULANDIĞINI bu politika DOĞRULAYAMAZ (GitHub ayarları, bu repo kapsamı dışı). (12) **[YÜKSEK] Portal sahiplik kontrolü GERÇEK katalogla eşleşmiyordu:** `crossplane.ts` (Faz 12g) `group:default/platform-admins` ve `group:default/${oidcGroup}` (ör. "tenant-acme") BEKLİYORDU — katalogda `platform-team` (platform-admins DEĞİL) ve `acme` (tenant-acme DEĞİL) vardı; `compositions/tenant/function.k`'nin ÜRETTİĞİ catalog-info.yaml ise ÜÇÜNCÜ bir isim (`team-acme`) kullanıyordu — ÜÇ FARKLI isimlendirme şeması BİRBİRİYLE hiç eşleşmiyordu. Kök neden: gerçek bir Keycloak→Backstage OIDC grup senkronizasyon provider'ı HİÇ YAZILMAMIŞ (katalog TAMAMEN statik/elle). `crossplane.ts`, Backstage'in KENDİ, ZATEN var olan `team-<teamName>`/`platform-team` deseinine (oidcGroup'a DEĞİL) çekildi; `organization.yaml`'daki "acme" grubu "team-acme"ye yeniden adlandırıldı; boşluk (senkronizasyon YOK) organization.yaml'a AÇIKÇA yazıldı. (13) **[YÜKSEK] Velero'nun manuel kurulumu ile ArgoCD tanımı AYNI ayarları kullanmıyordu:** `02-velero.yaml.tpl`'in `valueFiles`'ı ham `values.yaml.tpl`'e (envsubst edilmemiş `${CEPH_OBJECTSTORE_NAME}` İÇEREN) işaret ediyordu — Helm'in `$values` ref source'u dosyayı git'ten OLDUĞU GİBİ okur. `render-app-manifests.sh`'e `platform/control-plane/velero/` eklendi (bu dosyalardaki HİÇBİR değer sır DEĞİL — gerçek S3 anahtarları ayrı bir Secret'a gider); Application artık render edilmiş `values.yaml`'ı kullanıyor; offsite overlay'i BİLİNÇLİ OLARAK yorum satırı (yalnızca offsite GERÇEKTEN etkinleştirildiğinde açılacak) olarak eklendi. (14) **[YÜKSEK, en geniş kapsamlı] Güncel e2e artık ÇALIŞAMAZDI:** iki AYRI, birleşen sorun — (a) e2e'nin Vault'u HÂLÂ plaintext'ti, ama compositions Faz 12g/12h'den beri HER YERDE `https://` BEKLİYOR (Issuer, ESO SecretStore'lar, cert-manager) — tenant-issuer ASLA Ready olmazdı; (b) tenant-issuer/Certificate/CNPG Cluster bekleme döngülerinin HİÇBİRİNDE timeout YOKTU — (a) sorunu bu döngüleri SÜRESİZ askıya alırdı. DÜZELTİLİRKEN Faz 12j'nin KENDİ regresyonu bulundu: e2e'nin `cert-manager` Vault policy'sinin ELLE KOPYALANMIŞ bir sürümü, Faz 12j'nin GERÇEK `provider-terraform-policy.hcl`'e eklediği `cert-manager-tenant-*` path'lerini HİÇ almamıştı — `vaultBootstrapJob`'ın YENİ per-tenant cert-manager rolünü/policy'sini yazma denemesi "permission denied" ile başarısız OLACAKTI. Düzeltmeler: gerçek `values-tls.yaml` overlay'i (03-pki.sh'in KENDİSİYLE AYNI) e2e'ye eklendi, `vault-ca-bundle` iki namespace'e (vault + crossplane-system) + tenant'a DOĞRUDAN (ESO'suz) dağıtıldı, `provider-terraform-policy.hcl` artık `kubectl cp` ile GERÇEK dosyadan okunuyor (elle kopya SİLİNDİ — bu drift SINIFI bir daha oluşamaz), 3 bekleme döngüsüne timeout+teşhis çıktısı eklendi, stale (artık kullanılmayan) paylaşılan "cert-manager" rolü kaldırıldı. (15) **[YÜKSEK] YENİ korumaların regresyon testleri eksikti:** portal'ın TypeScript build/tip kontrolü `platform-ci.yaml`'da HİÇ YOKTU (`crossplane.ts` değişiklikleri yalnızca ELLE `tsc` ile doğrulanıyordu) — YENİ `portal-typecheck` job'ı eklendi (`yarn tsc`, tam `build --all` DEĞİL — kapsam bilinçli dar tutuldu). Diğer maddeler (kyverno-test.yaml fixture'ı, registry kontrolünün gerçek postgres/operatör imajlarıyla testi, e2e'nin KALAN 9 stripped kaynağı) dürüstçe AÇIK BIRAKILDI — bunlar ayrı, daha büyük işler. **Doğrulama:** #11 → mantık incelendi, CLI SINIRLAMASI nedeniyle canlı test EDİLEMEDİ (dürüstçe işaretli); #12 → `tsc --noEmit` (0 hata) + organization.yaml'ın 2 dokümanının YAML doğrulaması; #13 → `render-app-manifests.sh --verify` + rendered `values.yaml`'ın GERÇEK `s3Url` içerdiği doğrulandı; #14 → TÜM script `bash -n` ile sözdizimi doğrulandı, GERÇEK bir kind cluster'ında ÇALIŞTIRILAMADI (ortam kısıtı — bu, bu turun en düşük güvenle doğrulanan maddesi, BİR SONRAKİ gerçek CI koşusunda kanıtlanmalı); #15 → workflow YAML'ı doğrulandı. | (1) Bir yetkilendirme kontrolü YAZARKEN kullanılan grup/kimlik adlarının GERÇEKTEN var olan katalog/RBAC verisiyle eşleştiğini DOĞRULAMADAN "bu iş bitti" saymak — kod kendi içinde tutarlı görünse bile, REFERANS VERDİĞİ dış veri (bir katalog, bir grup listesi) HİÇ kontrol edilmemişse, o kontrol PRATİKTE herkesi (hatta meşru adminleri) reddedebilir. (2) Aynı "GERÇEK değer nihai" ilkesi (composition'ların function.k'sı için ZATEN uygulanan) bir HELM chart'ın values dosyası için de geçerlidir — manuel bootstrap'ın (envsubst) ve GitOps'un (ham git checkout) AYNI `.tpl` dosyasını FARKLI şekillerde okuduğu her yerde, biri diğerinden SESSİZCE SAPAR. (3) Bir composition'ı BÜYÜK bir güvenlik değişikliğiyle (paylaşılan kimlikten per-tenant kimliğe geçiş gibi) güncellerken, o değişikliğin dayandığı TÜM YERLERİ (gerçek repo dosyası + ONU TAKLİT EDEN her kopya — burada bir e2e script'inin inline HCL'i) AYNI ANDA güncellemek gerekir; DRY'ı ÇİĞNEYEN her kopya, gelecekteki bir güncellemenin unutacağı bir yerdir — mümkünse kopyalamak yerine GERÇEK dosyayı (`kubectl cp` ile) kullanmak bu sınıf hatayı YAPISAL olarak ortadan kaldırır. (4) SÜRESİZ bir bekleme döngüsü, "bu adım her zaman başarılı olur" varsayımının GİZLİ bir belgesidir — o varsayım YANLIŞ çıktığında (TAM DA burada olduğu gibi), döngü hatayı GÖRÜNÜR kılmak yerine SESSİZCE SONSUZA KADAR gizler; her bekleme döngüsü baştan bir timeout+teşhis çıktısıyla yazılmalıdır, "zaten hep çalışıyordu" bir garanti değildir. (5) "Ayrı bir CI job'u eksik" gibi bir bulguya karşı EN DAR, EN GÜVENLİ parçayı (yalnızca tip kontrolü) eklemek — "her şeyi kapsayan dev bir CI job'u" eklemeye ÇALIŞMAKTAN (canlı test edilemeyen, hataya açık) daha DEĞERLİDİR; kapsamı BİLİNÇLİ OLARAK dar tutmak ve bunu YAZMAK, sahte bir eksiksizlik izlenimi vermekten iyidir. |
| 2026-09-15 | Faz 12j | Kullanıcının BEŞİNCİ kod incelemesinde bildirdiği 5 gerçek, yüksek/kritik bug düzeltildi (Vault güvenliği/sürekliliği + veri kaybı/kurtarma açıkları). — (6) **[YÜKSEK] TLS kurulumu tekrar çalıştırıldığında güvenli devam etmiyordu:** `install_vault()` HER ZAMAN yalnızca plaintext `values.yaml` ile helm upgrade yapıyordu — TLS etkinleştirilmiş bir Vault'ta script'i yeniden çalıştırmak, `vault-server-tls` Secret'ı SİLMEDEN listener'ı SESSİZCE plaintext'e GERİ ALIYORDU; `enable_vault_tls()` ise yalnızca Secret'ın VARLIĞINA bakıp "TLS etkin" sanıyordu (GERÇEK durumu hiç PROBE ETMEDEN). İkisi birleşince: bir re-run TLS'i BOZAR, sonraki re-run bunu FARK ETMEZ. `install_vault()` artık Secret varsa `values-tls.yaml`'ı da helm upgrade'e EKLİYOR; `enable_vault_tls()` artık Secret'ın varlığı yerine vault-0'a GERÇEK bir HTTPS `vault status` çağrısıyla PROBE ediyor. (7) **[YÜKSEK] Vault sunucu sertifikasının yenileme döngüsü yoktu:** 90 günlük sertifika tek seferlik üretiliyordu, Secret varsa adım TAMAMEN atlanıyordu — süre dolunca ESO/cert-manager bağlantıları kesilecekti. #6'nın DÜZELTMESİ aynı zamanda #7'nin çözümü: fonksiyon artık HER ÇALIŞTIRMADA sertifikayı YENİDEN ÜRETİYOR (idempotent) ve TLS zaten canlıysa `kubectl rollout restart statefulset/vault` ile AÇIKÇA pod'ları yeniden başlatıp yeni sertifikayı YÜKLETİYOR (helm upgrade StatefulSet spec'i değişmediyse KENDİLİĞİNDEN restart tetiklemez). Operatör bunu periyodik olarak (`03-pki.sh --only vault-tls`, 90 günden ÖNCE) çalıştırmalı — bkz. `pki/vault/README.md`'nin YENİ "Sertifika yenileme" bölümü (alarm KURULMADI, dürüstçe işaretlendi). (8) **[YÜKSEK, güvenlik sınırı] Tenant'ların PKI yetkileri PAYLAŞILAN cert-manager rolü üzerinden GENİŞTİ:** TÜM tenant'ların cert-manager ServiceAccount'ları `bound_service_account_namespace_selector` ile AYNI Vault auth role'üne bağlıydı, o role'ün policy'si `pki-int-<env>/sign/tenant-*` GLOB'una sahipti — HERHANGİ bir tenant, Vault API'sine DOĞRUDAN (cert-manager'ın Issuer/Certificate CRD akışının DIŞINDA) çağrı yapıp BAŞKA bir tenant'ın PKI rolünü imzalayıp o tenant'ın domain'i için GEÇERLİ bir sertifika ALABİLİRDİ — PKI rolünün KENDİ domain kısıtı bunu ENGELLEMEZ (saldırgan HEDEF tenant'ın KENDİ, meşru rolünü çağırıyor). Her tenant artık KENDİ `cert-manager-tenant-<nsName>` Vault auth role'ünü VE policy'sini alıyor (`_vaultBootstrapScript`, `tenant-<nsName>`/`eso-tenant-<nsName>` İLE AYNI desende) — YALNIZCA KENDİ PKI rolünü imzalayabilir. Paylaşılan `cert-manager` role'ü artık YALNIZCA ClusterIssuer'lar (platform-dev/staging/prod) için. Negatif test dürüstçe "canlı Vault olmadığı için ÇALIŞTIRILAMADI" olarak işaretlendi (`compositions/tenant/README.md`'ye komut eklendi). (9) **[KRİTİK] Küme dışı Velero yedeği veritabanı içeriğini KAPSAMIYORDU:** `VELERO_OFFSITE_ENABLED=true` yalnızca K8s objelerini küme-dışına taşıyordu (`deployNodeAgent:false`+`volumeSnapshotLocation:[]`) — Postgres'in Barman verisi hâlâ YALNIZCA birincil Ceph RGW'deydi; Ceph'in TAMAMEN kaybı offsite'taki manifestlerle veritabanı içeriğini GERİ GETİREMEZDİ. YENİ `setup_postgres_offsite_sync()` (06-velero.sh): HER tenant Postgres instance'ının backup bucket'ını (rook-ceph'te DEĞİL, KENDİ tenant namespace'inde — bu keşif sırasında doğru namespace'in bulunması gerekti) keşfedip GÜNLÜK, TEK-AMAÇLI bir CronJob (`amazon/aws-cli`, iki-adımlı indir→yükle) ile senkronize ediyor — YENİ bir Ceph RGW admin/cross-bucket kimliği İCAT EDİLMEDİ (radosgw-admin'in GERÇEK semantiği bu ortamda doğrulanamazdı), bunun yerine ZATEN kanıtlanmış OBC-başına izolasyon deseni yeniden kullanıldı. Bilinçli sınır: yalnızca script ÇALIŞTIĞI ANDA var olan bucket'ları keşfeder (periyodik re-run gerekir, TLS yenilemesiyle AYNI desen). (10) **[YÜKSEK] Prod silme koruması yalnızca Tenant DELETE'ini kapsıyordu:** PostgreSQLInstance'ın TEK BAŞINA silinmesi VE Tenant'ın `teamName`/`environment`'ının (veya PostgreSQLInstance'ın `tenantRef`'inin) DEĞİŞTİRİLMESİ korumaya HİÇ TAKILMIYORDU — bu alanlar composition'ların kaynak ÜRETTİĞİ namespace'in PARÇASI olduğu için, bir UPDATE eski kaynakları SESSİZCE silip yenilerini üretebilirdi (DELETE korumasını TAMAMEN BYPASS eden bir yol). İKİ AYRI düzeltme: (a) `tenant/xrd.yaml`'a `teamName`/`environment` İÇİN, `postgresql/xrd.yaml`'a `tenantRef` İÇİN CEL `x-kubernetes-validations` immutable kuralları (`self.X == oldSelf.X`, yalnızca UPDATE'te değerlendirilir) eklendi; (b) `10-protect-prod-tenant-deletion.yaml`'a PostgreSQLInstance DELETE'i için 2 YENİ kural eklendi (prod'luk `tenantRef`'in "-prod" ile bitmesine göre belirlenir) — `kyverno apply` ile 4 senaryo (prod onaysız/onaylı, dev) canlı doğrulandı. **Doğrulama:** #6/#7 → statik inceleme + `kubectl rollout restart` mekanizmasının doğruluğu (GERÇEK bir Vault'ta ÇALIŞTIRILAMADI, dürüstçe işaretli); #8 → statik inceleme + render edilmiş YAML'ın elle incelenmesi (Issuer'ın YENİ role adını KULLANDIĞI doğrulandı), negatif test komutu YAZILDI ama ÇALIŞTIRILAMADI; #9 → CronJob şablonunun envsubst render'ı + YAML doğrulaması yapıldı, GERÇEK bir Ceph RGW/offsite S3'e karşı ÇALIŞTIRILAMADI; #10 → `kyverno apply` ile 4 GERÇEK senaryo (Tenant zaten vardı, PostgreSQLInstance için 4 YENİ) + TAM Kyverno test suite'i (38/38, regresyon yok) + `render-examples.sh` (CEL kurallarının render pipeline'ını BOZMADIĞI doğrulandı — CEL'in KENDİSİ yalnızca gerçek bir API server'da değerlendirilir). | (1) Bir "Secret VAR mı" kontrolü, "İSTENEN DURUM GERÇEKTEN aktif mi" sorusunun YERİNE GEÇMEZ — Secret'lar bağımsız yaşar (bir helm upgrade'in geri alması Secret'ı SİLMEZ), bu yüzden Secret varlığı yalnızca "bu materyal BİR ZAMANLAR üretildi" der, "şu an KULLANILIYOR" demez; GERÇEK durumu her zaman CANLI PROBE ile (buradaki gibi bir HTTPS `vault status` çağrısı) doğrulamak gerekir. (2) "Bu adım zaten yapıldıysa ATLA" deseni, o adım aslında bir YENİLEME/BAKIM görevi (sertifika rotasyonu gibi) OLDUĞUNDA YANLIŞ bir optimizasyondur — "yalnızca ilk seferde çalıştır" ile "her seferinde tekrarla, idempotent yap" arasındaki fark, işin DOĞASINA (tek seferlik mi, periyodik mi) bakılarak seçilmelidir. (3) Bir PKI rolünün KENDİ domain kısıtı (`allowed_domains`), o rolü ÇAĞIRABİLECEK kimliklerin kısıtlanmasının YERİNE GEÇMEZ — "hangi domain imzalanabilir" ile "kim bu imzalama işlemini TALEP EDEBİLİR" bağımsız, İKİSİ DE ayrı ayrı dar tutulması gereken kontrollerdir; paylaşılan bir kimlik/role, ne kadar dar bir PKI rolüne erişse de, O ROLÜN KENDİ yetkili olduğu her şeyi YAPABİLİR. (4) "Offsite yedek" gibi bir özelliğin KAPSAMINI (yalnızca K8s objeleri mi, yoksa gerçek veri de mi) AÇIKÇA belirtmeyen bir "offsite açıldı" cümlesi YANILTICI bir güvenlik hissi yaratır — asıl kritik veri (burada: Postgres'in KENDİSİ) farklı bir mekanizmadan (Barman) geliyorsa, "offsite" onu OTOMATİK OLARAK kapsamaz, AYRI, açıkça tasarlanmış bir yol gerekir. (5) Bir DELETE koruması yazarken, KORUNAN kaynağın kimliğini/namespace'ini BELİRLEYEN alanların (burada: teamName/environment/tenantRef) AYRICA immutable olup olmadığını kontrol etmek gerekir — DELETE'i ne kadar sıkı kilitlerseniz kilitleyin, aynı sonucu bir UPDATE (kaynağı "başka bir yere taşı" gibi görünen ama aslında eskisini silip yenisini üreten) ile elde etmek mümkünse, koruma YALNIZCA TEK BİR YOLU kapatmış olur. |
| 2026-09-15 | Faz 12i | Kullanıcının DÖRDÜNCÜ kod incelemesinde bildirdiği 5 gerçek, kritik/yüksek bug düzeltildi — bunların İLKİ, bu görevde tespit edilen EN CİDDİ mimari kusurdu. — (1) **[KRİTİK] ArgoCD root Application HİÇBİR child Application'ı keşfedemiyordu:** `platform-root`'un `directory` source'u `platform/control-plane/apps`'a bakıyor — bu dizindeki 16 dosyanın TAMAMI `.yaml.tpl`'di. ArgoCD'nin RESMİ dokümantasyonu (https://argo-cd.readthedocs.io/en/stable/user-guide/directory/, WebFetch ile doğrulandı) directory source'un YALNIZCA `.yaml`/`.yml`/`.json` yüklediğini söylüyor — `.tpl` HİÇ okunmuyordu, ${VAR} çözülse BİLE (eski `check_git_placeholders_resolved()` yalnızca değişken çözümlemesini kontrol ediyordu, UZANTIYI DEĞİL). Sonuç: Faz 2'den bu yana (bu görevin TAMAMI dahil) GitOps zinciri SIFIRDAN hiçbir zaman çalışamazdı. Çözüm: YENİ `render-app-manifests.sh` — `.tpl`'leri KANONİK ŞABLON olarak KORUYUP (fork'lar arası tekrar kullanılabilir, zengin yorumlar kalır) yanlarına GERÇEK, `${VAR}`'ları çözülmüş `.yaml` kardeşleri üretir (composition'ların function.k/composition.yaml İKİLİSİYLE AYNI desen, `--verify` modu drift yakalar). 19 dosya (apps/'te 14, app-of-apps/underlay/'da 5) BU GÖREVDE GERÇEKTEN render edilip commit edildi (PLATFORM_REPO_URL=bu repo'nun gerçek GitHub URL'i, chart sürümleri versions.env'den); 2 dosya (05/06-tenant-requests-*) TENANT_REQUESTS_REPO_URL henüz yok diye BİLİNÇLİ OLARAK render edilmedi. `01-require-signed-images.yaml.tpl` de (AYRI bir directory-source, `platform/policies/security`) aynı script'e eklenip HARBOR_HOSTNAME ile render edildi. `check_git_placeholders_resolved()` artık bu script'in `--verify`'ını çağırıyor. (2) **[KRİTİK] `jq '.sealed // true'` — jq'nun `//` operatörü `false`'u da "falsy" sayıyor:** kullanıcının kendi testiyle kanıtlandı: girdi `{"sealed":false}` → çıktı `true`. Sağlıklı, unsealed bir Vault script tarafından SONSUZA KADAR mühürlü sanılıp kurulum dururdu. `if has("sealed") then .sealed else true end` ile düzeltildi — AYNI düzeltme `.initialized` için de (önceki hatasız ama AYNI kırılgan deseni kullanıyordu) uygulandı. Kullanıcının TAM test senaryosuyla doğrulandı (`echo '{"sealed":false}' | jq ...` → artık `false`). (3) **[KRİTİK] Vault TLS sertifikası kendi rolünün allowed_domains'iyle uyuşmuyordu:** `pki-int-dev/roles/vault-server`'ın `allowed_domains`'i `vault-server`'ı HİÇ içermiyordu (yalnızca vault-internal/vault.vault.svc/vault-active.vault.svc) ama sertifika isteği `common_name=vault-server` gönderiyordu — Vault PKI bunu KESİN olarak reddeder (SAN listesinin doğru olması CN kontrolünü ATLATMAZ). `common_name`, rolde ZATEN TAM eşleşen bir giriş olan `vault-active.vault.svc.cluster.local`'a çekildi (aynı hata, KAYNAK runbook'ta — `vault-self-tls.md`'nin "Basitleştirilmiş öneri" bölümünde — de VARDI, orası da düzeltildi; bootstrap script'i o runbook'un bu KISMINI birebir kopyalamıştı). (4) **[KRİTİK] Yeni Harbor-only imaj politikası platformun KENDİ Postgres imajını VE cnpg-system/velero'yu engelliyordu:** kullanıcı 3 temsilî pod'u GERÇEKTEN test etmiş, üçü de reddedilmiş. İki AYRI kök neden: (a) `postgresql/function.k`'nin `pgImage`'ı hâlâ `ghcr.io/cloudnative-pg/postgresql:...`dan çekiyordu — KENDİ ürettiği politika (yalnızca Harbor) TARAFINDAN reddediliyordu; `_rgwEndpoint`'le AYNI "KCL'in env değişkenine erişimi yok" kısıtıyla `_harborHostname` sabitine (`.env.example`'ın HARBOR_HOSTNAME varsayılanıyla eşleşir) çekildi — GERÇEK mirror'lama/imzalama AYRI, bu görevde YAPILMAYAN bir operasyonel adım (gerçek Harbor/cosign bu ortamda yok). (b) `deny-non-harbor-images` VE `require-pss-restricted-clusterwide`'ın namespace exclude listeleri Faz 12h'de cnpg-system/velero EKLENİRKEN GÜNCELLENMEMİŞTİ (o dosyaların KENDİ başlık yorumu "yeni bileşen eklenince MANUEL güncelleyin" diyor — bu bakım adımı atlanmıştı) — ikisine de eklendi. (5) **[YÜKSEK] CNPG operatörü kuruldu ama yönetim ağı hâlâ açılmamıştı:** Faz 12h'de operatör GERÇEKTEN kuruldu ama `postgresql/function.k`'nin networkPolicy'sindeki yorum HÂLÂ "operatör kurulunca eklenmeli" diyordu — kurulmuş olmasına RAĞMEN kural eklenmemişti. cnpg-system→instance-manager (8000) ingress VE instance→kube-apiserver (`toEntities: ["kube-apiserver"]`) egress eklendi (CNPG'nin resmi ağ gereksinimleri). **Doğrulama:** #1 → `render-app-manifests.sh` GERÇEKTEN çalıştırıldı (19/21 dosya render edildi, `--verify` idempotent, tüm çıktılar `yaml.safe_load_all` ile doğrulandı); #2 → kullanıcının BİREBİR test senaryosuyla `jq` çıktısı doğrulandı; #4 → `kyverno apply` ile kullanıcının 3 pod'unu BİREBİR TEKRARLAYAN bir test (tenant/cnpg-system/velero) — üçü de artık ya KABUL ediliyor ya da yalnızca (beklenen, gerçek cosign/cluster olmadığı için) imza doğrulama adımında bekliyor, registry/namespace reddi YOK; #5 → `render-examples.sh` + rendered YAML'ın elle incelenmesi + TAM Kyverno test suite'i (38/38); #3 → statik inceleme (Vault PKI kurallarına göre), GERÇEK bir imzalama denemesi bu ortamda YAPILAMADI (Vault yok) — dürüstçe işaretlendi. | (1) "Bu dizin ArgoCD tarafından doğrudan git'ten okunuyor" diye bir yorum YAZMAK, o dizindeki dosyaların GERÇEKTEN ArgoCD'nin okuyabileceği bir BİÇİMDE olduğunu DOĞRULAMAZ — resmi ArgoCD dokümantasyonunu (ya da GERÇEK bir cluster'ı) hiç kontrol etmeden "bu .tpl dosyası root-app'ın parçası" varsayımı, İKİ AYRI FAZ boyunca (12g'nin clusterissuers.yaml.tpl taşıması SIRASINDA bile) fark edilmeden kaldı — kendi önceki hatamı düzeltirken bile AYNI kök varsayımı hiç sorgulamadım. (2) jq'nun `//` operatörü Python/JS'in `??`/`or`'undan FARKLI davranır — `null` VE `false` (VE boş string, 0) hepsi "falsy"dir; bir boolean alanı "eksikse varsayılan" mantığıyla okurken `//` KULLANMAK, alanın GERÇEKTEN var olduğu ama "yanlış" değerde olduğu durumları SESSİZCE YANLIŞ yorumlar — `has()` ile açık varlık kontrolü tek güvenli yoldur. (3) Bir PKI rolünün `allowed_domains`'ini yazarken, SAN/alt_names listesinin doğru olması yeterli DEĞİLDİR — `common_name`'in KENDİSİ de o listede (bare ya da subdomain olarak) olmalıdır; bu ikisi BAĞIMSIZ kontrol edilir. (4) Bir "yalnızca X'ten gelsin" politikası yazarken, platformun KENDİ ürettiği kaynakların (bu durumda: composition'ın kendi Postgres imajı) o politikayı GEÇECEK şekilde tasarlandığını AYRICA doğrulamak gerekir — politika ve onu ÜRETEN composition'lar AYNI anda, birbirine göre test edilmeden yazılırsa, biri diğerini SESSİZCE KIRAR. (5) Bir "TODO: X kurulunca ekle" yorumu, X GERÇEKTEN kurulduktan SONRA ayrı bir görev/faz olarak TAMAMLANMAZSA sonsuza kadar orada kalır — bir bileşeni GitOps zincirine eklemek ile o bileşenin ihtiyaç duyduğu TÜM yan kaynakları (burada: network policy) eklemek AYNI COMMIT'TE, AYNI KONTROL LİSTESİNDE olmalıdır. |
| 2026-09-15 | Faz 12h | Kullanıcının ÜÇÜNCÜ kod incelemesinde bildirdiği 4 gerçek bug (#11-14) düzeltildi, ARTI Faz 12g'de kendi eklediğim bir regresyon (bkz. aşağıda) fark edilip düzeltildi. — (11) **CNPG ve Velero'nun üretim kurulum yolu YOKTU:** CNPG yalnızca e2e script'inde (sürümü SABİTLENMEMİŞ) kuruluyordu, control-plane/README.md "⬜ Faz 4" işaretliydi; `platform/control-plane/velero/` TAMAMEN BOŞTU. YENİ: `apps/01-cnpg.yaml.tpl` (wave 1, otomatik sync, CNPG_CHART_VERSION=0.22.1) + `02-control-plane.sh`'e 6/6 adımı (Deployment/CRD readiness, label-selector ile — chart'ın fullname'ini SABİT VARSAYMADAN); `apps/02-velero.yaml.tpl` (wave 2, Loki'nin AYNI "otomatik sync KAPALI, S3 sırrı" deseni) + YENİ `06-velero.sh` (OBC→Secret→helm→GERÇEK bir on-demand backup ile uçtan uca doğrulama — yalnızca Schedule/BSL nesnelerinin VAR OLMASI değil, bir backup'ın GERÇEKTEN `Completed` olduğu kanıtlandı, VELERO_CHART_VERSION=7.2.1). e2e script'indeki CNPG/Vault helm install'larına da eksik `--version` pin'leri eklendi (kod incelemesinin 3. bölümünün "CNPG gibi bazılarının sürümü sabit değil" notu). (12) **Silme → gerçek veri kaybı riski, koruma YOKTU:** `06-tenant-requests-appset.yaml.tpl`'in `prune:true` + StorageClass'ların `reclaimPolicy:Delete`'i (BİLİNÇLİ, "kurtarma yolu Velero'dur" diyordu — ama Velero (11) kurulana kadar bu KARARIN ÖNKOŞULU YANLIŞTI) birleşince, tek bir git dosyası silme işlemi ekstra onay OLMADAN gerçek disk kaybına dönüşebiliyordu. YENİ Kyverno politikası `10-protect-prod-tenant-deletion.yaml`: `spec.environment=prod` olan bir Tenant claim'i, `platform.internal/deletion-approved-by`+`-reason` annotation'ları OLMADAN silinemez — ArgoCD'nin OTOMATİK prune'u DAHİL (istisnasız, çünkü asıl risk TAM OLARAK budur). Namespace/XTenant seviyesinde AYNI koruma BİLİNÇLİ OLARAK eklenmedi (Faz 12c'nin protect-tenant-resourcequota keşfiyle AYNI risk: GC/cascade zincirini gatelemek silmeyi sonsuza dek "Terminating"te kilitleyebilir). Küme-dışı yedek: `.env`'de isteğe bağlı `VELERO_OFFSITE_*` + `values-offsite.yaml.tpl` overlay'i (Vault TLS'in İKİ AŞAMALI deseniyle AYNI) — varsayılan KAPALI (sahte bir uç nokta icat edilmedi), açıksa AYRI bir `daily-offsite` Schedule ile GERÇEKTEN bağımsız bir ikinci kopya üretir. RPO/RTO `velero/README.md`'de SOMUT bir tabloya döküldü — K8s objeleri için RTO GERÇEKTEN ölçülmüştü (Faz 12'nin kind+MinIO tatbikatı, "saniyeler içinde"); Postgres PITR restore RTO'su ve donanım-kaybı RTO'su DÜRÜSTÇE "ÖLÇÜLMEDİ" olarak işaretlendi (icat edilmedi). `compositions/postgresql/README.md`'ye CNPG'nin gerçek `bootstrap.recovery` prosedürü (bu composition'ın ZATEN ürettiği barmanObjectStore'a dayanan) eklendi — yine dürüstçe "uçtan uca çalıştırılmadı" notuyla. (13) **PostgreSQL metrik Service'i hiç YOKTU:** `serviceMonitor` bir Service'i (Pod'u DEĞİL) hedefler ama composition HİÇBİR ZAMAN 9187 portunu açan bir Service üretmiyordu — Prometheus'un scrape edeceği SIFIR target vardı. YENİ `metricsService` (port 9187, `labels`'taki mevcut `cnpg.io/cluster` etiketiyle ServiceMonitor'ün ZATEN eşleştiği). (14) **OIDC grup isimlendirmesi üç farklı yerde üç farklı kuraldaydı:** Kyverno 04/06 HER İKİ biçimi (önekli/öneksiz) listeliyordu, tenant RoleBinding'i YALNIZCA `oidc:` önekli, `tenant-claim-rbac.yaml`'ın ClusterRoleBinding'i YALNIZCA öneksizdi — `--oidc-groups-prefix=oidc:` kurulu gerçek bir cluster'da tenant-requester grubu claim AÇMA yetkisini KAYBEDERDİ. Kyverno'nun ZATEN kullandığı "her iki biçimi de listele" deseni HER YERE uygulandı (yeni bir standart İCAT EDİLMEDİ, var olanı yaygınlaştırdı). — AYRICA (Faz 12g'nin KENDİ regresyonu, bu turda fark edildi): `01-require-signed-images.yaml.tpl`'i `.tpl`'e çevirirken `cert-manager/resources/` klasörünün İÇİNDE bırakmıştım — ama o klasör cert-manager Application'ının ArgoCD directory-sync KAYNAĞIdır ve (01-kube-prometheus-stack.yaml.tpl'in `exclude: "alertmanager-config-*.yaml.tpl"` satırından GÖRÜLDÜĞÜ gibi) ArgoCD'nin directory sync'i `.tpl` uzantısını UZANTIYA GÖRE ATLAMAZ — envsubst edilmemiş `${VAULT_CA_BUNDLE_B64}` DEĞİŞKENİNİ ÇÖZÜLMEMİŞ, ham metin olarak apply etmeye ÇALIŞIRDI. `clusterissuers.yaml.tpl`, `test-certificate.yaml.tpl`'in ZATEN kullandığı deseni (resources/ DIŞINDA) izleyecek şekilde taşındı. **Doğrulama:** #12 → `kyverno apply --set request.operation=DELETE,...` ile 3 GERÇEK senaryo (onaysız prod REDDEDİLDİ, onaylı prod KABUL, dev ETKİLENMEDİ) — `kyverno test`'in values şeması bu Kyverno CLI sürümünde (1.19.1) DELETE/oldObject simülasyonunu desteklemiyordu, bkz. `tests/protect-prod-tenant-deletion/README.md`; #13/#14 → `verify-sync.sh` + `render-examples.sh` + render edilmiş YAML'ın elle incelenmesi + TAM Kyverno test suite'i (38/38, regresyon yok); #11 → tüm yeni YAML/bash syntax doğrulandı, GERÇEK bir kind cluster'ında UÇTAN UCA çalıştırılamadı (ortam kısıtı, dürüstçe `velero/README.md`'de işaretli). | (1) Bir "kabul edilen tasarım kararı" (reclaimPolicy:Delete, "kurtarma yolu Velero'dur" diyerek) yorumun İÇİNDE doğru olsa bile, o kararın ÖNKOŞULU (Velero'nun GERÇEKTEN kurulu olması) sağlanana kadar YANLIŞTIR — bir yorumun mantıksal ZİNCİRİNİ (X çünkü Y) doğrulamak, yalnızca X'in kendisini değil Y'nin GERÇEKTEN var olup olmadığını da kontrol etmeyi gerektirir. (2) DELETE'i bir Kyverno politikasıyla gatelemek RİSKLİDİR — hangi kaynağın GC/cascade zincirinin PARÇASI olduğunu (gatelenirse sonsuza kadar "Terminating"te kilitlenebilir) ve hangisinin yalnızca bir "ön kapı" (insan/GitOps'un GERÇEKTEN başlattığı, hiçbir otomatik sürecin İZLEMEDİĞİ) olduğunu AYIRT ETMEDEN bir DELETE guard'ı eklemek, Faz 12c'de YAŞANMIŞ AYNI sınıf hatayı (silinemez namespace) TEKRARLAR. (3) Bir dosyayı `.yaml`'den `.tpl`'e çevirmek, o dosyanın bir ArgoCD directory-sync kaynağının İÇİNDE kaldığı sürece YETERLİ bir izolasyon DEĞİLDİR — GitOps'un dosyayı NASIL keşfettiğini (uzantıya göre mi, yoksa ham dizin taramasıyla mı) doğrulamadan varılan "ArgoCD bunu görmezden gelir" varsayımı, komşu bir Application'ın (`01-kube-prometheus-stack.yaml.tpl`) `exclude:` satırıyla ÇÜRÜTÜLDÜ — kanıt HER ZAMAN başka bir dosyada olabilir, yalnızca değiştirilen dosyaya bakmak yeterli değildir. (4) "RPO/RTO'yu ölç" gibi bir görev maddesi, GERÇEKTEN ölçülemeyen kısımlar için "tahmin uydurmak" yerine HANGİ kısmın gerçekten ölçüldüğünü (var olan bir tatbikattan) ve HANGİSİNİN ölçülemediğini AYRI AYRI, dürüstçe işaretlemeyi gerektirir — sahte bir kesinlik, hiç rakam vermemekten DAHA KÖTÜDÜR. |
| 2026-09-15 | Faz 12g | Kullanıcının İKİNCİ kod incelemesinde bildirdiği **5 gerçek bug** (#6-10) düzeltildi. — (6) **Tenant network-tier'ları Postgres'in gerçek ihtiyaçlarını KAPSAMIYORDU:** `isolated` tier yalnızca DNS egress'e sahipti — kendi Postgres'ine (platformun ZORUNLU sağladığı, tenant'ın seçmediği bir bağımlılık) bile erişemiyordu; AYRICA `postgresql/function.k`'nin KENDİ CiliumNetworkPolicy'si yalnızca ingress tanımlıyordu — egress bölümü YOKTU, tenant'ın default-deny'si (`endpointSelector={}`) Postgres pod'unu da kapsadığı için Postgres'in DNS'i, replica→primary streaming replication'ı VE Ceph RGW'ye backup egress'i TAMAMEN engelliydi (yalnızca "backup çalışmıyordu" değil, potansiyel olarak HİÇ ayağa kalkmıyordu). `tenant/function.k`'ye `_allowTenantPostgres` (isolated tier için DB-özel egress), `postgresql/function.k`'nin networkPolicy'sine DNS+aynı-ns egress+RGW egress+Prometheus ingress eklendi. (7) **tenant-admin Role'ün `networking.k8s.io` blanket wildcard'ı:** `resources=["*"], verbs=["*"]` tenant'a KENDİ NetworkPolicy/CiliumNetworkPolicy'sini de create/update/delete yetkisi veriyordu — K8s NetworkPolicy modeli salt-eklemeli (allow-list) olduğu için tenant, platformun zero-trust izolasyonunu KENDİ NetworkPolicy'siyle GENİŞLETEBİLİRDİ (yetki yükseltmesi). `networking.k8s.io` ikiye ayrıldı: Ingress (tam CRUD, meşru self-service) ve NetworkPolicy (yalnızca get/list/watch). (8) **Vault'ta HİÇ TLS yoktu:** `tlsDisable=true`, tüm composition/cert-manager/ESO bağlantıları düz HTTP. `03-pki.sh`'e YENİ bir adım (`enable_vault_tls`, 4/7) eklendi — pki-int-dev'den Vault'un KENDİ sunucu sertifikasını imzalatıp `values-tls.yaml` overlay'iyle HTTPS'e geçiriyor (iki-aşamalı: ilk kurulum plaintext KALMAK ZORUNDA — pki-int henüz yokken Vault kendi sertifikasını imzalayamaz). cert-manager ClusterIssuer'ları (`caBundle`), postgres/backstage'in ESO SecretStore'ları (`caProvider`, cross-namespace ConfigMap), tenant bootstrap Job'unun `vault` CLI'ı (`VAULT_CACERT`, aynı-namespace ConfigMap) VE tenant'ın KENDİ cert-manager Issuer'ı (YENİ: `vaultCaSecretStore`/`vaultCaExternalSecret`, Vault KV round-trip'i ile `caBundleSecretRef`) HEPSİ https'e geçirildi. (9) **Backstage `allow-all-policy` + sıfır tenant-ownership kontrolü:** `crossplane.ts`'in `/:kind/:name` route'u herhangi bir authenticated kullanıcının HERHANGİ BİR tenant'ın claim/composite status'unu okumasına izin veriyordu (cross-tenant bilgi sızıntısı) — Backstage'in permission framework'ü bu ÖZEL route'u hiç KAPSAMIYOR, backend TEK bir geniş yetkili ServiceAccount kullanıyor. `coreServices.userInfo`/`ownershipEntityRefs` ile çağıranın OIDC grup üyeliği, tenant'ın `spec.oidcGroup`'una göre KONTROL EDİLİYOR artık (403 döner eşleşmezse); PostgreSQLInstance için sahiplik `tenantRef`den ilgili Tenant claim'i bularak DOLAYLI çözülüyor. (10) **`01-require-signed-images.yaml.tpl`'de `validationFailureAction: Enforce` YOKTU** (yalnızca test fixture'ında vardı — Audit modunda hiçbir imzasız imaj GERÇEKTEN engellenmiyordu) VE yalnızca Harbor imajlarını kapsıyordu — bir tenant `docker.io/...`dan imaj çekerek imza doğrulamasını TAMAMEN atlayabiliyordu. `Enforce` eklendi; YENİ bir `deny-non-harbor-images` kuralı (platform-sistemi namespace'leri hariç) eklendi. **Doğrulama yöntemi bu turda farklı:** Faz 12f'nin aksine SIFIRDAN bir kind cluster'ında UÇTAN UCA çalıştırılamadı (ortam kısıtı) — bunun yerine her bug KENDİ en güçlü statik/yerel yöntemiyle doğrulandı: #6/#7 → `verify-sync.sh` + `render-examples.sh` + render edilmiş YAML'ın gerçek Role/CiliumNetworkPolicy kurallarının elle incelenmesi; #9 → gerçek `@backstage/backend-plugin-api` tip tanımlarına karşı `tsc --noEmit` (0 hata); #10 → yerel `kyverno apply` ile GERÇEK 3 test Pod'u (harici registry/tenant ns → REDDEDİLDİ, Harbor/tenant ns → yalnızca imza kontrolü nedeniyle başarısız — beklenen, sertifika/secret yok; sistem ns/harici registry → KABUL EDİLDİ); #8 → `helm`/`kubectl`/`vault` CLI'larının söz dizimi + chart'ın `global.tlsDisable` sözleşmesine göre statik inceleme (canlı bir HTTPS handshake GERÇEKLEŞTİRİLMEDİ — bu turun en düşük güvenle doğrulanan maddesi, bir sonraki tam kind e2e turunda `enable_vault_tls` GERÇEK bir cluster'a karşı çalıştırılıp doğrulanmalı). | (1) Bir composition'ın kendi CiliumNetworkPolicy'sinin yalnızca INGRESS tanımlayıp EGRESS'i tanımsız bırakması, "eksik bir izin" gibi GÖRÜNMEZ (kod okuyan biri "ingress zaten var, yeterli" sanabilir) ama tenant'ın default-deny'si HER YÖNÜ kapsadığı için pod'un EGRESS'i de ayrıca AÇIKÇA izin verilmelidir — bu sınıf hata yalnızca "bu pod'un TÜM giden bağlantı ihtiyaçlarını listele" sorusunu AÇIKÇA sormakla yakalanır, "ingress kuralı var mı" sorusuyla DEĞİL. (2) Geniş bir RBAC kapsamı (`apiGroups=[...], resources=["*"], verbs=["*"]`) yazarken her apiGroup'un GERÇEKTEN hangi kaynakları içerdiğini (`networking.k8s.io` hem Ingress HEM NetworkPolicy'yi kapsar) tek tek düşünmemek, "bu grup zararsız görünüyor" varsayımıyla bir yetki yükseltme kapısı AÇAR. (3) Self-referential PKI bootstrap (Vault kendi sertifikasını kendi PKI'sinden imzalar) ZORUNLU olarak İKİ AŞAMALIDIR — bunu TEK bir values.yaml'a gömmeye çalışmak SIFIRDAN kurulumu KIRAR (Secret henüz yokken mount edilemez); overlay dosyası (`-f` ile ikinci geçiş) bu döngüyü doğru şekilde KIRAR. (4) Bir backend route'un "permission framework'e bağlı" OLMASI ile "permission framework KURULU" olması AYNI ŞEY DEĞİLDİR — özel bir Express router'ı `permissions.authorize()` çağırmadığı sürece `allow-all-policy`nin varlığı/yokluğu o route için HİÇBİR ŞEY DEĞİŞTİRMEZ; yetkilendirme HER ÖZEL ROUTE'DA AYRICA uygulanmalıdır. (5) Bir Kyverno `verifyImages` kuralının `imageReferences`'ı yalnızca EŞLEŞEN imajları doğrular — eşleşmeyen bir imaj o kural için SESSİZCE ATLANIR (reddedilmez); "yalnızca X'ten gelen imajlar imzalı olmalı" bir garanti değil, yalnızca "X'ten gelirse imzalı olmalı" anlamına gelir — gerçek bir garanti için AYRICA "yalnızca X'ten gelebilir" kısıtı gerekir. |
| 2026-09-15 | Faz 12f | Kullanıcının kod incelemesinde bildirdiği **5 gerçek bug** düzeltildi ve TAMAMI SIFIRDAN, temiz bir kind cluster'ında (`kind create cluster` → `tests/e2e/kind-chain/run.sh`'in DÜZELTİLMİŞ hâli, adım adım) uçtan uca doğrulandı. — (1) **Vault rol adları uyuşmazlığı:** `compositions/tenant/function.k`'nin ürettiği Vault policy/auth-role adları yalnızca `${teamName}` kullanıyordu (`eso-tenant-acme`) ama `postgresql/function.k`'nin SecretStore'u `eso-tenant-${tenantRef}` (`tenantRef`=tam namespace, `eso-tenant-tenant-acme-dev`) BEKLİYORDU — iki taraf ASLA eşleşmiyordu. TÜM Vault tanımlayıcı adları `${nsName}`'e (tam namespace) çevrildi — artık postgres'in ürettiği rol adıyla BİREBİR aynı (canlı doğrulandı: `vault list auth/kubernetes/role` → `eso-tenant-tenant-acme-dev`). (2) **Dev/prod çakışması:** aynı düzeltme bunu da çözdü — rol adları artık ortamı da İÇERİYOR (`nsName` = `tenant-<team>-<env>`), aynı takımın dev/prod'u artık AYRI Vault rolleri/policy'leri alıyor, birbirini EZEMEZ. (3) **PKI süre uyuşmazlığı:** tenant Vault PKI rolünün `max_ttl`si 720h (30 gün) iken postgresql'in Certificate'ı `duration=2160h` (90 gün) İSTİYORDU — Vault bunu REDDEDERdi. `max_ttl`/`ttl` 2160h'e çekildi; canlı bir Certificate GERÇEKTEN 90 GÜNLÜK olarak imzalandı (`notBefore`→`notAfter` fark: tam 90 gün, `openssl x509 -dates` ile doğrulandı). (4) **Eksik bootstrap bağımlılığı:** `vaultBootstrapJob`'ın ihtiyaç duyduğu `vault-tenant-bootstrap` ServiceAccount + Vault auth role HİÇBİR bootstrap script'inde OLUŞTURULMUYORDU (yalnızca önceki oturumlarda canlı cluster'a ELLE eklenmişti) — sıfırdan bir kurulumda Job SONSUZA KADAR "role not found" ile başarısız olurdu. `03-pki.sh`'e (ve e2e script'ine) eklendi; canlı doğrulandı: `vault-bootstrap-acme-dev` Job'ı SIFIRDAN bir cluster'da İLK DENEMEDE `Complete` oldu. (5) **e2e script'i ile values.yaml uyumsuzluğu:** `tests/e2e/kind-chain/run.sh`, Faz 12e'nin transit-auto-unseal gerektiren GERÇEK `pki/vault/values.yaml`'ını kullanıyordu ama `vault-unseal` altyapısını/Secret'ı hiç kurmadan ESKİ Shamir init/unseal akışını çalıştırıyordu — pod'lar `CreateContainerConfigError` ile HİÇ başlayamazdı. Script YENİDEN YAZILDI: önce vault-unseal (transit KMS) kuruluyor/init ediliyor/token üretiliyor, SONRA ana Vault (Secret hazır durumda) kuruluyor, init `-recovery-shares`/`-recovery-threshold` ile yapılıyor (BAŞKA bir canlı keşif: transit'le baştan init edilen bir Vault'ta `-key-shares`/`-key-threshold` "not applicable to seal type transit" ile REDDEDİLİYOR — yalnızca Shamir'den migrate ederken geçerli), manuel unseal adımı TAMAMEN KALDIRILDI (transit zaten otomatik açıyor — canlı doğrulandı: init sonrası `vault-0/1/2` 15 saniye içinde `1/1 Running`, hiçbir `vault operator unseal` çağrısı OLMADAN). Ayrıca script'in ESKİ `strip_unsupported_resources.py` çağrısı `vaultWorkspace` adını siliyordu — ama bu isim Faz 12b'de `vaultBootstrapJob`'a DÖNÜŞTÜĞÜ için silme HİÇBİR ZAMAN eşleşmiyordu (sessiz no-op) — düzeltildi (artık hiç strip EDİLMİYOR, gerçek native Job GERÇEKTEN çalışıyor, e2e kapsamı genişledi). **Sonuç: TAM zincir (Tenant→Vault Job→Issuer→Postgres→CNPG→Certificate) SIFIRDAN, tek denemede, uçtan uca `openssl verify: OK` ile doğrulandı.** | (1) İki farklı composition'ın AYNI harici kaynağa (Vault) farklı isimlendirme kurallarıyla YAZMASI/OKUMASI, yalnızca STATİK render/kyverno testleriyle ASLA yakalanamaz — ikisi de kendi başına "doğru" render eder; yalnızca GERÇEK bir Vault'a karşı GERÇEK bir SecretStore auth denemesi bunu ortaya çıkarır. (2) Bir composition'ın kendi İÇİNDEKİ bir sabit (max_ttl) başka bir composition'ın talep ettiği bir DEĞERLE (Certificate duration) TUTARLI olmalı — bu tür çapraz-composition kısıtlar KOD YORUMLARINDA doğru olduğu İDDİA EDİLSE bile (bu repoda TAM OLARAK böyle oldu — yorum "90 gün, PKI rolüyle aynı" diyordu ama kod 30 gündü) gerçekte SAPMIŞ olabilir; yalnızca gerçek bir imzalama denemesi bunu kanıtlar. (3) Bir composition'ı büyük bir mimari değişiklikle (provider-terraform → native Job) güncellerken, o değişikliğin GEREKTİRDİĞİ yeni kimlik/RBAC/policy kaynaklarını YALNIZCA canlı test cluster'ına ELLE eklemek (bootstrap script'lerine YANSITMADAN) SESSİZCE bir "yalnızca benim makinemde çalışıyor" durumu yaratır — her mimari değişiklik, DEĞİŞTİRDİĞİ her bootstrap/kurulum script'ine de YANSITILMALIDIR, aksi halde sıfırdan kurulum KIRIKTIR. (4) `kubectl exec` bir REMOTE komuta yerel bir bash heredoc'unu `|`/`<<EOF` ile pipe'lamak `-i`/`--stdin` bayrağı OLMADAN SESSİZCE BAŞARISIZ olur (hata mesajı YANILTICI: "policy parametresi verilmedi", stdin sorunundan HİÇ bahsetmez) — dosya+`kubectl cp` deseni (bu repoda zaten 03-pki.sh'in kullandığı desen) bu sınıf hatadan tamamen KAÇINIR. (5) Vault'un `-key-shares`/`-key-threshold` VE `-recovery-shares`/`-recovery-threshold` bayrakları birbirinin YERİNE GEÇMEZ — hangisinin geçerli olduğu, Vault'un `seal` yapılandırmasının o VAULT INSTANCE'I İLK BAŞTAN Shamir mi yoksa auto-unseal (transit/KMS) mi olarak başladığına bağlıdır; bu, dokümantasyonda kolayca gözden kaçan ama GERÇEK bir `vault operator init` denemesinin ANINDA ortaya çıkardığı bir ayrım. |
| 2026-09-15 | Faz 12d | Kullanıcı kararıyla: cosign anahtarı, OIDC, TLS, Kyverno Enforce, Vault auto-unseal/chaos, K8s API server OIDC entegrasyonu — hepsi AYNI `fix-all` kind cluster'ında GERÇEKTEN ele alındı. **8 YENİ gerçek bug daha bulunup düzeltildi** (toplamda bu üç oturumluk bug avında 20). — (18) Gerçek cosign v2.4.1 anahtar çifti üretildi, `cosign.pub` commit edildi, `cosign-image-signing-key` Secret'ı yeniden oluşturuldu. (19) `01-require-signed-images.yaml.tpl`'da kural düzeyinde GEÇERSİZ bir `imageReferences` alanı vardı (yalnızca `verifyImages[]` altında geçerli) — `kubectl apply` "unknown field" ile KESİN reddediyordu; bu policy HİÇ gerçek bir cluster'a uygulanmamıştı. (20) AYNI policy'de `rekor.ignoreTlog`/`ctlog.ignoreSCT` YOKTU — CI'nın kendi imzalama adımı (`--tlog-upload=false`) Rekor'a hiç yüklemediğinden, CI'nın imzaladığı HER imaj reddedilirdi (canlı, doğru anahtarla imzalanmış bir imaj bile REDDEDİLEREK doğrulandı; eklenince kabul edildi) — test fixture'ında bu düzeltme vardı ama gerçek şablona hiç yansıtılmamıştı. Gerçek bir Kyverno 3.3.6 kurulup Enforce modunda hem imzalı imaj KABUL hem imzasız imaj RED canlı doğrulandı. (21) `kyverno/values.yaml`'daki `config.webhooks` YANLIŞ TİPTEYDİ (liste, chart harita bekliyor) — "can't evaluate field namespaceSelector in type []interface{}" ile KESİN reddediyordu, bu chart hiç kurulmamıştı. (22) **KRİTİK:** `protect-tenant-resourcequota` politikası `operations` kısıtlamıyordu — Enforce moduna alınınca Kubernetes'in KENDİ namespace garbage collector'ının silme isteğini de reddediyordu, bir tenant namespace'i **ASLA silinemez** hale geliyordu (canlı doğrulandı: `kubectl delete namespace` sonsuza dek "Terminating" takılı kaldı) — `operations: [CREATE, UPDATE]` ile DELETE'e izin verilecek şekilde düzeltildi. (23) Kendi `vaultBootstrapJob` düzeltmemde (Faz 12b) container'a hiç `resources` eklenmemişti — `require-pod-resources` Enforce moduna alınınca (geçici olarak) bu YAKALANDI. (24) Crossplane çekirdeğinin RBAC'ı yalnızca ana kaynaklara izin veriyordu, `<kaynak>/status` alt-kaynaklarına DEĞİL (K8s RBAC'ta bunlar AYRI tiptir) — `ciliumnetworkpolicies/status`, `issuers/status`, `jobs/status` vb. için tekrar tekrar "not allowed" uyarıları ÜRETİYORDU (kaynak oluşturma başarılı olduğu için fark edilmesi gecikmişti) — tüm ilgili tipler için `/status` girdileri eklendi. (25) `require-pod-resources` politikasının KENDİ üst yorumu onun HENÜZ Enforce'a alınmaması GEREKTİĞİNİ söylüyordu (mevcut ihlaller gözden geçirilmeden) — ben yine de flip edince Vault/Keycloak/Postgres dahil neredeyse HER YERDE yalnızca `limits.memory` olup `limits.cpu` OLMADIĞI ortaya çıktı; policy'nin kendi tavsiyesine uyularak Audit'e GERİ ALINDI (bu, tam bir `limits.cpu` temizlik geçişi gerektiren, bu oturumun kapsamını aşan AYRI bir görev). Diğer 8 politika (02-03, 05-09, security/02) başarıyla Enforce'a alındı VE gerçek tenant zinciri (SIFIRDAN yeniden oluşturuldu) bunlarla birlikte Ready=True'ya ulaştı. (26) Keycloak/Harbor gibi platform-genelindeki servislerin hostname kuralı (`<servis>.<taban-alan-adı>`, env ÖNEKSİZ) Vault PKI `platform-<env>` rolünün `allowed_domains`'iyle (yalnızca `<env>.<taban-alan-adı>`) UYUŞMUYORDU — canlı bir cert-manager Certificate isteği "common name ... not allowed by this role" ile REDDEDİLEREK keşfedildi; `platform-dev` rolüne çıplak taban alan adı da eklendi. (27) Keycloak'a GERÇEK TLS (production:true + Vault-PKI'dan cert-manager Certificate + `tls.usePem`) eklendi ve GERÇEK HTTPS (`openssl`/`curl` ile zincir doğrulandı, leaf→intermediate→root CN eşleşti) ile canlı doğrulandı — tech borç #7 pratikte KAPANDI (yalnızca ilk-kurulum sıralaması hâlâ Faz 1→Faz 3 çapraz-bağımlılığı taşıyor, bu documented/bilinçli). (28) K8s API server OIDC entegrasyonu (`docs/runbooks/k8s-api-server-oidc.md`) — ÖNCEDEN "bu ortamda ASLA yapılamaz" olarak işaretlenmişti ama kind'ın kendisi GERÇEK bir kubeadm cluster'ı OLDUĞUNDAN, node'un kendi `/etc/kubernetes/manifests/kube-apiserver.yaml`'ı `docker exec` ile GERÇEKTEN patchlendi: `--oidc-*` bayrakları + Vault PKI CA bundle'ı eklendi, API server BAŞARIYLA yeniden başladı (TÜM CRD grupları yeniden register edildi, hata YOK) — ama restart penceresi sırasında ERKEN (temkinli davranarak) YEDEĞE geri dönüldü, OIDC token'ıyla GERÇEK bir `kubectl` çağrısı test EDİLEMEDİ. Mekanizma MEKANİK olarak SAĞLAM kanıtlandı (bayraklar kabul edildi, çökme YOK, tam init tamamlandı) ama uçtan uca kimlik doğrulama TESTİ eksik kaldı — cluster'ın geri kalanındaki kanıtlanmış durumu riske atmamak için BİLİNÇLİ bir durdurma noktası. (29) GERÇEK bir chaos testi: canlı `tenant-acme-dev` + Vault lideri (`vault-0`) silindi — otomatik raft failover **~6 saniyede** gerçekleşti (yeni lider `vault-1`), XTenant KESİNTİSİZ Ready=True kaldı, yeni liderden GERÇEK bir sertifika BAŞARIYLA imzalandı. `vault-0` geri döndüğünde (Shamir, auto-unseal YOK) **SEALED olarak döndü** — kullanıcının "Vault auto-unseal yok" endişesini CANLI olarak doğrulayan somut kanıt; elle unseal edilerek HA GERİ YÜKLENDİ. Vault Transit auto-unseal (ikinci, küçük bir self-hosted Vault'un unseal sağlayıcısı olması) bu ortamda MİMARİ OLARAK mümkün ama bu oturumda UYGULANMADI (kapsam/süre) — açık bir mimari karar olarak bırakıldı (bulut KMS, ADR-0001'in "bulut yok" kararına aykırı olurdu). | (1) Bir Kyverno politikasının validationFailureAction'ını Audit'ten Enforce'a çevirmek SADECE bir konfigürasyon değişikliği DEĞİLDİR — GERÇEK admission-webhook testleri olmadan asla ortaya çıkmayacak, birbirinden BAĞIMSIZ en az 6 farklı gerçek hatayı (19-25) YÜZEYE ÇIKARDI; `kyverno test` (statik fixture) bunların HİÇBİRİNİ YAKALAYAMAZDI. (2) Bir politikanın KENDİ yorumundaki "henüz Enforce değil, önce mevcut ihlaller gözden geçirilmeli" notu ciddiye ALINMALIDIR — bu tam olarak GERÇEKLEŞTİ (require-pod-resources) ve doğru davranış geri almaktı, ZORLA Enforce'ta bırakmak DEĞİL. (3) Bir güvenlik politikasını Enforce moduna almak, o politikanın KENDİSİNİN platformun temel yaşam döngüsü operasyonlarını (namespace silme gibi) KIRMADIĞINI doğrulamayı GEREKTİRİR — yalnızca "doğru resource'ları REDDEDİYOR mu" testi YETERSİZDİR, "YANLIŞ resource'ları (GC'nin kendi istekleri gibi) yanlışlıkla REDDETMİYOR mu" testi de GEREKİR. (4) Bir test ortamının (kind) "gerçek bir kubeadm cluster'ı olmadığı" varsayımı YANLIŞ olabilir — kind TAM OLARAK kubeadm kullanır, bu da onu "yalnızca statik render test edilebilir" kategorisinden çıkarıp bazı GERÇEKTEN altyapı-seviyesinde entegrasyonların (API server flag'leri gibi) test edilebileceği bir ortama dönüştürür; ama bu YÜKSEK RİSKLİDİR (kontrol düzlemi kaybı) ve yalnızca GERİ ALMA planı netse (docker exec erişimi + yedek manifest) denenmelidir. (5) Bir Vault Raft cluster'ını node sayısını AZALTARAK ölçeklendirmek (kaynak tasarrufu için) RAFT KOTASINI bozabilir — kalan node, YAPILANDIRILMIŞ oy sayısına göre çoğunluk SAĞLAYAMAZSA lider seçilemez ("standby, no active node") — bu, bu OTURUMDA kendi kaynak yönetimi eylemim YÜZÜNDEN canlı olarak YAŞANDI, iyi bir gerçek-dünya örneği. |
| 2026-09-15 | Faz 12c | Kullanıcı talebiyle: "hiç test edilmemiş bileşenler" (MetalLB, Rook-Ceph, Harbor, Keycloak, kube-prometheus-stack, Loki, Tempo, OpenCost) AYNI `fix-all` kind cluster'ında GERÇEKTEN canlı test edildi (Backstage kullanıcı kararıyla bu fazın kapsamı DIŞINDA bırakıldı — gerçek bir Docker image build'i gerektiriyor, ayrı bir oturuma ertelendi). **SONUÇ: 6/8 bileşen TAM olarak çalışır durumda doğrulandı, 1'i (Rook-Ceph) kısmen (OSD hariç), yolda 5 YENİ gerçek bug bulunup düzeltildi.** — (12) **DOĞRULANDI:** `bitnami/keycloak` Docker Hub'da ARTIK HİÇ TAG İÇERMİYOR, `bitnami/postgresql` yalnızca `latest` içeriyor (Broadcom 2025 politikası) — `.env.example`'a `KEYCLOAK_IMAGE_REGISTRY="docker.io/bitnamilegacy"` eklendi (geçici mirror). (13) `realm-platform.json.tpl`'deki `_comment` alanları (JSON'da resmi olarak desteklenmeyen) Keycloak'ın SIKI deserializer'ı tarafından reddediliyor, pod HER ZAMAN CrashLoopBackOff'a giriyordu — kaldırıldı, açıklamaları `01-underlay.sh`'e taşındı. (14) `01-underlay.sh --only rook-ceph` SESSİZCE hiçbir şey yapmıyor (gerçek adım adı `rook`) ve yine de "tamamlandı" basıyor — script'in `should_run()` mekanizmasının bilinmeyen `--only` değerlerini SESSİZCE yutması genel bir kırılganlık (yalnızca not edildi, düzeltilmedi — kapsam dışı küçük bir UX borcu). (15) `opencost/values.yaml`'daki `exporter.extraEnv` YANLIŞ TİPTE (liste, chart map bekliyor) — chart'ın KENDİSİ `kubectl apply` sırasında "expected string, got ...valueUnstructured{Value:0}" ile REDDEDİYORDU, bu chart hiç GERÇEKTEN kurulmamıştı → harita biçimine düzeltildi. (16) `loki/values.yaml.tpl`, `deploymentMode: SingleBinary` kullanmasına RAĞMEN chart'ın varsayılan SimpleScalable `read`/`write`/`backend` replika sayılarını (3/3/3) SIFIRLAMIYORDU — chart'ın KENDİ doğrulaması KESİN olarak reddediyordu → `replicas: 0` üçü için eklendi. (17) AYNI dosyada `schemaConfig` HİÇ TANIMLI DEĞİLDİ — chart'ın İKİNCİ, BAĞIMSIZ bir doğrulama hatası ("a real Loki install requires a proper schemaConfig") → `tsdb`+`s3` şema config'i eklendi. Bu iki bulgu (16+17) bu dosyanın (values.yaml.tpl) daha önce HİÇBİR gerçek Loki chart kurulumunda çalıştırılmadığının KESİN kanıtı. **Gerçek doğrulanan işlevsellik**: MetalLB (gerçek L2 ARP trafiği, `curl` ile 200 OK — Cilium kube-proxy-replacement ile ÇAKIŞMADI); Harbor (gerçek `docker push`/`pull`/manifest doğrulama + Trivy scan tetikleme, S3 backend MinIO ile); Keycloak (gerçek OIDC login, Bearer token, `kubernetes`+`backstage` client'ları import edildi doğrulandı); kube-prometheus-stack (15/18 hedef GERÇEKTEN scrape ediliyor, Cilium/Hubble ServiceMonitor'ları DAHİL; Grafana login + 4 datasource); OpenCost (GERÇEK custom pricing oranlarıyla hesaplanan `ramCost`); Loki (gerçek push→query round-trip); Tempo (gerçek OTLP trace push→query round-trip, S3 backend). **Rook-Ceph kısmi**: operator/CRD/mon-quorum/mgr/CSI TAMAMI gerçekten çalıştı (`Ready HEALTH_WARN`), ama OSD provizyonu bu ortamda MÜMKÜN DEĞİL — `ceph-volume` (Ceph'in kendisi, Rook değil) loop-backed sanal blok cihazları GEÇERLİ OSD adayı olarak TANIMIYOR; bu GERÇEK bir çevre sınırlaması (Docker Desktop'ın linuxkit VM'i), repo kodunda bir hata DEĞİL. Ayrıca `rbd` kernel modülü de bu VM'de YOK (`modprobe rbd` başarısız) — bare-metal'de bu iki kısıt olmayacak (gerçek diskler + tam Linux kernel). | (1) Bir bileşenin "chart'ı doğru render ediyor / statik testler yeşil" olması, chart'ın GERÇEKTEN kurulabileceği anlamına GELMİYOR — bu oturumda 3 farklı chart (Keycloak realm import, OpenCost extraEnv, Loki x2) chart'ın KENDİ admission/validation mantığı tarafından REDDEDİLDİ; bunların HİÇBİRİ `crossplane render`, `kyverno test` veya YAML syntax kontrolüyle YAKALANAMAZDI — yalnızca gerçek `helm install` ile. (2) Üçüncü taraf Docker Hub imaj dağıtım politikaları (Bitnami örneği) SESSİZCE ve HIZLA değişebilir — bir repo'nun "çalışıyordu" güvencesi ZAMANLA geçersiz hale gelebilir; imaj varlığını periyodik olarak yeniden doğrulamak (CI'da `docker manifest inspect` gibi) kalıcı bir gereksinim. (3) Ceph'i konteyner-tabanlı/sanal ortamlarda (bare-metal DIŞINDA) test etmek YAPISAL bir sınır taşıyor (loop device + rbd modülü) — bu, "test edilmedi" ile "test edilemez (bu ortamda)" arasındaki farkı NET bir şekilde ortaya koydu; ikisi FARKLI risk seviyeleri taşır ve dokümantasyonda AYRI AYRI işaretlenmeli. | gerçek bir `fix-all` kind cluster'ında canlı bug avı. **11 gerçek, doğrulanmış bug bulundu ve düzeltildi**: (1) `PROVIDER_HELM_VERSION=v0.20.0` registry'de hiç yoktu → v0.20.4. (2) Vault liveness probe sealed/uninit pod'ları öldürüyordu → `sealedcode=204&uninitcode=204` eklendi (readiness'e DEĞİL, kasıtlı olarak). (3) `functions.yaml`'da `function-auto-ready` hiç tanımlı değildi (yalnızca `function-kcl`) → eklendi. (4) Crossplane ÇEKİRDEĞİNİN composed CRD kaynakları için RBAC'ı yoktu → aggregate ClusterRole eklendi (`rbac-composed-resources.yaml`). (5) `hashicorp/vault` Terraform provider v4.x'te `auth_login_kubernetes` bloğu YOK → genel `auth_login` bloğuna geçirildi. (6) **provider-terraform'un Observe() adımı, hiç apply edilmemiş bir workspace'te KOŞULSUZ `terraform state list` çağırıyor** — v0.19.0'dan v1.2.0'a kadar DEĞİŞMEYEN, yapısal bir upstream sınırlama (Create()'e HİÇ ulaşılamıyor); provider'ın kendi Vault client'ı da auth başarısız/iptal olduğunda nil-pointer panikliyor. **Çözüm: provider-terraform TAMAMEN kaldırıldı**, tenant Vault provizyonu artık native bir `batch/v1 Job` (5 idempotent `vault write` çağrısı, state YOK) — ADR-0001'in "escape hatch yalnızca gerekince" ilkesiyle daha tutarlı. (7) Crossplane provider'larının ServiceAccount adı ProviderRevision hash'inden üretiliyor, HER sürüm yükseltmesinde DEĞİŞİYOR — provider-terraform'a özel sabit `serviceAccountTemplate` adlı bir `DeploymentRuntimeConfig` eklendi. (8) `provider-terraform-policy.hcl`, ESO'nun tenant-başına kimliğinin kullandığı `eso-tenant-*` path'lerini KAPSAMIYORDU (yalnızca `tenant-*`) → eklendi. (9) cert-manager'ın Vault k8s-auth role'ü yalnızca `cert-manager` namespace'ine bağlıydı ama tenant Issuer'ları `serviceAccountRef` ile HER TENANT NAMESPACE'İNDEKİ kendi `cert-manager` SA'sını kullanıyor (same-namespace token exchange) → `bound_service_account_namespace_selector` (Vault 1.16+) eklendi. (10) Bu selector'ün çalışması için Vault'un KENDİ SA'sının namespace `get` RBAC'ı GEREKİYOR (yoktu) → eklendi. (11) **`function-auto-ready` v0.5.0, ConfigMap/Job/Namespace/ServiceAccount/Secret gibi yaygın tipleri "ready" işaretleyen healthchecks registry'sinden YOKSUNDU** (bu registry v0.6.0'da eklendi) VE Role/RoleBinding/ResourceQuota/LimitRange/CiliumNetworkPolicy'nin ne native bir Ready condition'ı ne de function-auto-ready'de (v0.7.0'da BİLE) bir kaydı VAR — sonuç: `XTenant.status` Synced=True olsa BİLE HİÇBİR ZAMAN Ready=True olamıyordu. Çözüm: v0.7.0'a yükseltme + bu statik tipler için function-kcl'nin `krm.kcl.dev/ready: "True"` annotation'ıyla açık ready işaretleme. Tüm zincir SIFIRDAN (`kubectl delete` + yeniden `apply`) yeniden doğrulandı: Tenant claim → XTenant → Namespace/RBAC/Quota/SA/CiliumNetworkPolicy/Job(Vault bootstrap)/Issuer, TAMAMI Ready=True; gerçek bir `Certificate` (`tenant-issuer` üzerinden) Vault'tan GERÇEKTEN imzalandı ve `openssl x509`/zincir ile doğrulandı. | (1) **Bir composition'ın "işlevsel olarak doğru" görünmesi (kaynaklar oluşuyor, statuslar makul) onun XR'ının GERÇEKTEN `Ready=True`'ya ulaştığı anlamına GELMİYOR** — bu iki şey ayrı ayrı doğrulanmalı; bu repo'da muhtemelen HİÇBİR fazda XTenant'ın `status.conditions[type=Ready]` alanı gerçekten `True` görülmemişti (yalnızca bireysel kaynaklar — ör. Issuer — kontrol edilmişti). (2) Bir Crossplane composition function'ının (ör. function-auto-ready) versiyonu, o function'ın YETENEKLERİNİ (hangi kaynak tiplerini "ready" tanıyabildiğini) DOĞRUDAN etkiler — eski bir pin, "her şey Synced ama hiçbir zaman Ready" gibi SESSİZ ve YANILTICI bir başarısızlık modu üretebilir; yalnızca upstream CHANGELOG'u okuyarak (canlı davranışı gözlemleyip GERİYE doğru neden araştırarak) bulundu. (3) Crossplane provider'larının ServiceAccount adı KARARLI DEĞİLDİR (revizyon hash'i) — bu, dışarıdaki HERHANGİ bir sistemin (Vault kubernetes-auth gibi) o SA adına GÜVENDİĞİ her tasarımı kırılgan yapar; bunu fark etmenin tek yolu GERÇEKTEN bir provider sürüm yükseltmesi yapıp SA adının değiştiğini GÖZLEMLEMEKTİ (statik analiz bunu asla yakalayamaz). (4) provider-terraform gibi bir "escape hatch"i, aslında Terraform'un durum-takibinden hiç FAYDALANMAYAN (5 basit, doğası gereği idempotent API çağrısı) bir iş yükü için kullanmak, ADR'nin KENDİ ilkesine aykırıydı — iş yükünün GERÇEK ihtiyaçlarına (state gerekiyor mu?) bakmadan "zaten kurulu bir provider var" diye onu kullanmak yanlış bir varsayılan; bu, gerçek bir upstream bug'la karşılaşınca ANCAK fark edildi. (5) cert-manager'ın Vault issuer'ının `serviceAccountRef` auth deseni, ClusterIssuer (cert-manager'ın KENDİ namespace'i) ile namespaced Issuer (Issuer'IN KENDİ namespace'i) için FARKLI SA namespace'lerine çözülüyor — bu incelik dokümantasyonda AÇIK değildi, yalnızca GERÇEK bir `Certificate` oluşturup imzalatmaya ÇALIŞARAK (Issuer'ın kendi `status.Ready=True`'sU BUNU DOĞRULAMIYOR — yalnızca bağlantıyı kontrol ediyor) keşfedildi. |
| 2026-09-15 | Faz 12 | "Tüm eksiklikleri tamamla" görevi: Bölüm 3'teki TÜM açık kararlar çözüldü (bazıları KOD ile: cost-center 1:1 Kyverno politikası, ESO per-tenant kimlik, K8s API server OIDC tasarımı+`oidc:` önek uyumu, cosign anahtar üretimi; bazıları KARAR ile: fail-closed kalıcı, Vault Enterprise alınmayacak, LDAP federasyonu gereksiz). Teknik borç: PSS istisna drift kontrolü, PolicyException süre takibi, CI job tekilleştirme, RGW hardcode (Helm kısmı) düzeltildi; Vault'un kendi TLS'i için tam (ama bilinçli olarak otomatikleştirilmemiş) bir runbook yazıldı. GERÇEK bir kind cluster'ında TÜM zincir (artık ESO/cert-manager SA'ları da composition'ın kendisi tarafından üretiyor) yeniden kanıtlandı VE gerçek bir Velero+MinIO DR tatbikatı (backup→disaster→restore) ilk kez uygulandı. | (1) **`policies/security/` ve `policies/rbac/` dizinleri HİÇBİR ArgoCD Application'a bağlı DEĞİLDİ** — Faz 10/6b'de yazılmış ama Faz 6→7 arasında yaşanan AYNI "GitOps wiring boşluğu" burada da tekrarlanmıştı; yalnızca GERÇEK bir cluster'da politikaları test ederken (cost-center politikasını canlı denerken) fark edildi. (2) Kyverno'nun `context.apiCall`'ı, sorguladığı HER CRD için admission-controller'a AÇIKÇA RBAC verilmesini gerektiriyor — bu olmadan politika "unknown" hatasıyla TÜM ilgili istekleri reddediyor (görünüşte "güvenli" ama aslında TAMAMEN ENGELLEYİCİ bir başarısızlık modu) — yalnızca canlı bir admission denemesiyle keşfedildi, hiçbir statik araç bunu yakalayamazdı. (3) `tests/e2e/kind-chain/run.sh`'in kendisinde GERÇEK bir bug vardı (dead-code bir satır + yetersiz Vault-hazır-olma kontrolü + çok agresif liveness probe) — Faz 11'de yalnızca MANUEL adım adım çalıştırma test edilmiş, script'in KENDİSİ hiç baştan sona otomatik çalıştırılmamıştı; bu fazda hem bug bulunup düzeltildi hem de script GERÇEKTEN baştan sona otomatik çalıştırıldı. (4) Velero'nun varsayılan (CSI snapshot'sız) modu K8s objelerini restore eder ama PV İÇERİĞİNİ ETMEZ — CNPG Postgres pod'u restore sonrası `pg_controldata` hatasıyla crash-loop'a girdi; bu, disaster-recovery.md'nin ÖNCEDEN teorik olarak işaretlediği riskin CANLI kanıtıydı. (5) Crossplane, bir namespace'i (kendi composite'i hâlâ dururken) Velero'dan TAMAMEN BAĞIMSIZ olarak kendiliğinden yeniden üretiyor — DR senaryolarını tasarlarken "önce Crossplane'in kendi kendini iyileştirmesini mi engelliyoruz, yoksa Velero'ya mı güveniyoruz" ayrımı GEREKİYOR, ikisi birbirinin YERİNE geçmiyor. |
| 2026-09-14 | Faz 13 (ertelendi) | Multi-cluster hub-spoke görevi (provider-kubernetes uzak ProviderConfig, ArgoCD ApplicationSet cluster generator, spoke-başına Vault Intermediate CA, Cilium ClusterMesh tasarımı, Tenant XRD'ye `clusterRef`) teklif edildi. Görev metninin kendi koşulu ("yalnızca tek-cluster kurulum kanıtlandıktan/stabil olduktan sonra başlat") HENÜZ karşılanmıyordu — hub hiçbir gerçek cluster'a uygulanmadı. Kullanıcıya soruldu, kullanıcı işi ERTELEMEYİ seçti. Hiçbir dosya yazılmadı. | Bir görevin kendi metninde AÇIKÇA belirttiği bir ön koşul karşılanmıyorsa, sessizce ilerlemek (veya sessizce durmak) yerine kullanıcıya SORMAK doğru adımdır — özellikle sonraki adımın (multi-cluster) maliyeti, atlanan ön koşulun (tek-cluster stabilitesi) doğrulanmamış olmasından kaynaklanan riski ÖNEMLİ ölçüde büyütebilecekse. |
| 2026-09-14 | Faz 11 | `platform-ci.yaml` (composition render + Kyverno test, her PR'da) yazıldı. **İLK KEZ** gerçek bir `kind` cluster'ında canlı bir Crossplane reconcile döngüsü çalıştırıldı (önceki tüm fazlar yalnızca statik `crossplane render` kullanmıştı) — bu, üç GERÇEK üretim-engelleyici bug'ı ortaya çıkardı: (1) `versions.env`'in pinlediği Crossplane 1.18.0, bu repo'nun namespaced composed kaynaklarıyla UYUMSUZDU (`resourceRefs[].namespace` şema hatası) — 2.4.0'a yükseltildi; (2) `postgresql/function.k`'deki `serverCASecretName` alanı CNPG'nin gerçek CRD şemasında YOKTU (doğrusu `serverCASecret`) — düzeltildi; (3) `tenant/function.k`'nin ürettiği `tenant-issuer`, hiç var olmayan bir `cert-manager` ServiceAccount'una referans veriyordu — composition'a o ServiceAccount + token-mint RBAC'ı eklendi. Üçü de düzeltildikten sonra TAM zincir (Tenant claim → Namespace/RBAC/Quota/Issuer, gerçek Vault PKI'dan imzalı → Postgres claim → CNPG Cluster healthy + bağlantı Secret'ı → Certificate, AYNI Issuer'dan) GERÇEKTEN çalıştırılıp `openssl verify` ile kanıtlandı. 2 chaos senaryosu (CNPG primary silme →~7s failover; Vault lider pod'u silme →~8s yeni lider seçimi) gerçek zaman damgalarıyla belgelendi (`chaos-test-results.md`). Crossplane/cert-manager/ArgoCD/Kyverno için sıralı bir upgrade/rollback runbook'u yazıldı. | (1) Crossplane'in composed-kaynak reconcile DAVRANIŞI (RBAC gereksinimleri, `resourceRefs` şeması, Namespace+namespaced-kaynak aynı composition'da oluşturulduğunda YAŞANAN geçici "namespace not found" hataları — birkaç reconcile içinde KENDİLİĞİNDEN düzeliyor, hata değil) yalnızca CANLI bir apiserver'a karşı GÖZLEMLENEBİLİR; `crossplane render` bu davranışların HİÇBİRİNİ simüle etmiyor — bu, "yalnızca render ile doğrulanmış" güvencesinin YANILTICI olabileceğinin somut kanıtı (6/7/9/10 fazlarının hepsi yalnızca render kullanmıştı). (2) Bir composed kaynağın "işlevsel olarak doğru" görünmesi (KCL derleniyor, render ediyor, `kyverno test` yeşil) onun GERÇEKTEN çalışacağı anlamına GELMİYOR — CNPG'nin `serverCASecretName` alanı hiçbir statik araç tarafından yakalanamazdı çünkü hiçbiri CNPG'nin GERÇEK CRD şemasına karşı doğrulama yapmıyordu (yalnızca KCL/YAML sözdizimini kontrol ediyorlardı). (3) cert-manager'ın Vault `serviceAccountRef` (ambient/kimlik-ödünç-alma) auth deseni, referans verilen ServiceAccount'ın Issuer İLE AYNI namespace'te var OLMASINI ve cert-manager'ın KENDİ controller SA'sının o SA için TokenRequest oluşturabilmesini GEREKTİRİR — bu iki ön koşul dokümantasyonda AÇIK değildi, gerçek `kubectl describe issuer` hata mesajıyla (`serviceaccounts "cert-manager" is forbidden`) keşfedildi. (4) cosign v3/Kyverno uyumsuzluğunda olduğu gibi (Faz 10), burada da "önce varsayılan/en son sürümü dene, GERÇEK hatayla karşılaşınca kök nedeni ARAÇLARLA (curl/crane, CRD şema dump'ı) araştır" yöntemi, dokümantasyon taramaktan çok daha hızlı sonuç verdi — bu, altıncı kez doğrulanan aynı ders. |
| 2026-09-14 | Faz 10 (yazım) | Kyverno `verifyImages` (Cosign) ile Harbor'dan imzasız imaj deploy'unu engelleyen politika yazıldı ve GERÇEK bir Docker registry + gerçek cosign anahtarlarıyla uçtan uca test edildi (imzalı→kabul, imzasız→red). Cluster-genelinde PSS "restricted" (platform sistemi hariç, iki katmanlı: kube-apiserver `PodSecurityConfiguration` + Kyverno ClusterPolicy) + `PolicyException` tabanlı gerekçeli istisna süreci yazıldı, `kyverno test` ile 36/36 yeşil. Platform reposu + tenant-requests reposu için gitleaks+trufflehog CI job'ları eklendi, gerçek bir test sırrıyla ikisi de doğrulandı. Harbor'a `prevent_vul`/`auto_scan` proje politikası (bootstrap script'e eklendi) + syft SBOM/Trivy gate'li yeniden kullanılabilir bir CI workflow'u yazıldı, syft/trivy yerel olarak gerçek imajlarla test edildi. Vault audit log (`file` device, zaten hazır olan `auditStorage`'ı etkinleştirdi) + break-glass root token rotasyon runbook'u (`vault-unseal.md`'nin eski §6.3'ünü DEVRALDI) yazıldı. DR runbook'u (Velero+Vault snapshot+Rook-Ceph+tam yeniden inşa) yazıldı ama GERÇEK bir test cluster'ında ÇALIŞTIRILAMADI (kabul kriteri #3 açıkça KARŞILANMADI olarak işaretlendi). | (1) **cosign v3.x, Kyverno 1.19.1 ile UYUMSUZ** — v3'ün varsayılan imza formatı (OCI 1.1 Referrers API + sigstore-bundle) Kyverno'nun eski doğrulayıcısı tarafından okunamıyor; `cosign verify` başarılı olduğu HALDE Kyverno "no signatures found" diyordu — kök neden, `curl`/`crane` ile registry'nin `/referrers` endpoint'i incelenerek ve GitHub'dan doğrudan indirilen bir cosign v2.4.1 ile karşılaştırılarak bulundu (dokümantasyon taramadan, gerçek davranış farkıyla — Faz 6/6b/7'nin "gerçek CLI'yi çalıştır" dersinin beşinci tekrarı, ama bu kez ARAÇLAR ARASI bir sürüm uyumsuzluğu). (2) `kyverno apply --registry` yalnızca `localhost`/insecure registry'lerde DEĞİL, herkese açık HTTPS registry'lerde (ttl.sh) de aynı "no signatures found" hatasını verdi — bu, ilk bakışta bir TLS/insecure-registry sorunu SANILDI ama gerçek kök neden imza FORMATIYDI; yanlış hipotezle vakit kaybetmemek için `curl`/`crane` ile registry içeriğini DOĞRUDAN incelemek (varsayımla debug etmek yerine) daha hızlı sonuç verdi. (3) gitleaks'ın kendi `gitleaks.toml` varsayılan allowlist'i, AWS'in dokümantasyonundaki "EXAMPLE" anahtarını (çok bilinen bir placeholder olduğu için) BİLEREK yakalamıyor — güvenlik tarama araçları GERÇEKÇİ/rastgele görünen test verileriyle test edilmeli, "well-known" örneklerle DEĞİL (ilk test denemesi bu yüzden yanlış negatif verdi, ikinci denemede düzeltildi). (4) Kyverno ClusterPolicy'de `mutateDigest: true` iken `validationFailureAction` `Enforce` OLMADIĞINDA (varsayılan `Audit`) policy tamamen `Skipped` durumuna düşüyor — bu, YAML'da `validationFailureAction` alanının GÖRÜNMEZ ama ZORUNLU olduğu bir bağımlılık, gerçek `kyverno apply` hata mesajıyla keşfedildi. |
| 2026-09-14 | Faz 9 (yazım) | kube-prometheus-stack (Grafana sidecar ile ALL-namespace dashboard keşfi) + Loki + Tempo (Ceph RGW S3 backend) + OpenCost (bare-metal manuel fiyatlandırma) yazıldı; Tenant composition'a 9. adım olarak otomatik Grafana dashboard ConfigMap'i eklendi (CPU/mem/network+CNPG paneli); Hubble metriklerine namespace etiketi eklenip tenant-traffic dashboard'u yazıldı; cert-manager expiry PrometheusRule (30/7/1 gün) + Slack/Teams Alertmanager Secret şablonları; `jq`-tabanlı haftalık tenant özeti CronJob'u; `05-observability.sh` bootstrap script'i (OBC→S3 kopyalama, Loki/Tempo'nun S3 sırrı nedeniyle ArgoCD otomatik sync'i KASITLI kapalı, `underlay-root` ile aynı desen). Görev "Faz 9" ve `platform/underlay/observability/*` dedi; ADR-0001 L4 + Faz 0 iskeletine göre `platform/control-plane/observability/` kullanıldı, roadmap'teki Faz 4/Faz 9 çakışması Bölüm 2'de açıkça kaydedildi. | (1) KCL string literal'de JSON içine gömülü `\"` üretmek için escape SAYISI dikkatli hesaplanmalı — `\\\\"` (4 ters eğik çizgi) YANLIŞLIKLA `\\"` üretti (iki ters eğik çizgi + tırnak), `\\\"` (3 ters eğik çizgi) DOĞRU `\"` üretti; gerçek `kclvm_cli` çıktısı `json.loads` ile test edilerek yakalandı. (2) Prometheus/Alertmanager'ın Go template motorunda Sprig'in `default` filtresi YOK — `promtool check rules` ile gerçek hata (`function "default" not defined`) alınarak öğrenildi (Faz 6/6b/7'nin "gerçek CLI'yi çalıştır" dersinin dördüncü tekrarı). (3) `envsubst`, `{{ }}` (Go template) sözdizimine HİÇ dokunmuyor — yalnızca `${VAR}` eşleşir; ilk yazımda gereksiz bir `{{ "{{" }} ... {{ "}}" }}` kaçış deseni eklenmişti, bunun tamamen ETKİSİZ (ne koruyor ne bozuyor) olduğu doğrudan `envsubst` testiyle kanıtlanıp kaldırıldı. (4) Weekly-report script'i başta `python3` ile yazıldı, sonra CronJob'un imaj seçimini sadeleştirmek (ayrı bir python3 bağımlılığı eklememek) için TAMAMEN `jq`'ya çevrildi — `jq`'nun `fromdateiso8601` fonksiyonu ISO8601 "Z" damgalarını epoch'a çevirmek için `strptime`'dan daha güvenilir. |
| 2026-09-14 | Faz 7c / 8 (yerel) | Mevcut iskelet tamamlandı: ApplicationSet/Claims AppProject, fail-closed CI, güvenli yorum workflow'u, gerçek Backstage uygulaması ve Keycloak frontend/backend bağlantısı, Kubernetes/Crossplane durum plugin'i, Ready-gated EntityProvider ve benzersiz path üreten iki form. Gerçek action/render/policy, TypeScript/build, Jest ve Helm/kubeconform testleri çalıştırıldı. Canlı deploy/SSO/PR yapılmadı. | Konfigürasyon tek başına plugin/provider oluşturmaz; Helm 2.6.1 anahtarları backstage altında olmalıdır. PostgreSQL XRD highAvailability default alanındaki mevcut girinti hatası gerçek form→kubeconform testinde bulunup düzeltildi. |
| 2026-09-14 | Faz 0 | Repo iskeleti, ADR-0001, conventions, bu takip dosyası oluşturuldu | Katman sınırları baştan çizilmezse, sonradan çizilemiyor |
| 2026-09-14 | Faz 1 (yazım) | Underlay manifestleri, Harbor/Keycloak values, ArgoCD app-of-apps ve idempotent `01-underlay.sh` yazıldı; 19 şablon render testinden geçti | Çıplak `envsubst` ArgoCD'nin `$values` sözdizimini sessizce siliyor — şablon render'ı **açık değişken listesiyle** yapılmalı. Sessiz bozulma, geçerli YAML üretip Faz 2'de anlaşılmaz bir sync hatası olarak çıkardı. |
| 2026-09-14 | Faz 2 (yazım) | ArgoCD + App-of-Apps (sync-wave 0/1/1/1/2) + Crossplane v2 (provider-kubernetes/helm/terraform + function-kcl, hepsi InjectedIdentity/kimliksiz) + Kyverno (3 ClusterPolicy, Audit modunda) + ESO (yalnızca operatör) yazıldı; `02-control-plane.sh` idempotent kurulum script'i eklendi | (1) ArgoCD'nin git-watched dizinlerinde `${VAR}` bırakmak, Faz1'deki `$values` sorununun daha genel bir hali: chart repo/sürüm gibi ortam-BAĞIMSIZ değerler somutlaştırılmalı, yalnızca gerçekten deployment'a özgü olanlar (repo URL/revizyon) `.env`'de kalmalı — ve script bunu apply ETMEDEN ÖNCE kontrol etmeli. (2) provider-kubernetes/provider-helm'in ProviderConfig'i, provider'ın KENDİ CRD'si register olmadan uygulanamaz — ArgoCD'nin varsayılan health check'i Provider/Function CRD'lerini tanımadığından, özel bir Lua health check olmadan sync-wave sıralaması yalnızca kozmetik kalırdı. (3) `kyverno test`'in gerçek şeması `results[].resources` (ÇOĞUL liste), `resource` (tekil) DEĞİL — dokümantasyon taramadan CLI'yi çalıştırıp hatayla öğrenildi. |
| 2026-09-14 | Faz 3 (yazım) | Vault (HA/Raft, 3 replika, ceph-block PV, Shamir 5/3) + Kubernetes auth (cert-manager + provider-kubernetes rolleri) + PKI hiyerarşisi (pki-root 10y → pki-int-{dev,staging,prod} 2y, rol başına 90g max TTL) + cert-manager (`serviceAccountRef`, statik token Secret yok) + unseal/break-glass runbook'u yazıldı; `03-pki.sh` eklendi | (1) `sh -c script -- "$TOKEN" "$@"` çağrısında `"$@"` TOKEN'ı da içine alır — `shift` olmadan `vault <TOKEN> status` gibi yanlış bir komut oluşur; pozisyonel argüman semantiği elle test edilerek yakalandı. (2) ArgoCD'nin directory-source'u bir dizindeki HER `.yaml` dosyasını (Helm values dosyaları dahil) k8s manifesti gibi parse etmeye çalışır — Helm values'ı `ref: values` ile ayrı tutup gerçek manifestleri özel bir `resources/` alt dizininde toplamak (Faz 2'nin crossplane/resources deseni) gerekiyor; ilk tasarımda `exclude` glob'una güvenmek yerine bu daha sağlam. (3) Vault Namespaces (Root CA izolasyonu için istenen) Enterprise-only bir özellik — OSS'de mount+policy+break-glass ile en yakın karşılığı uygulanabilir ama "gerçek offline root" değildir; bu açıkça dokümante edildi, sessizce varmış gibi yapılmadı. |
| 2026-09-14 | Faz 6 (yazım) | `XTenant` XRD + KCL composition (`function.k`, 10 kaynak: Namespace/RBAC/Quota/LimitRange/2×NetworkPolicy/SA/Workspace/Issuer) yazıldı ve **GERÇEK araçlarla test edildi**: `kclvm_cli` 0.11.2 (KCL mantığı lokal, Docker'sız), gerçek `crossplane render` (Docker + function-kcl/function-auto-ready container'ları — simülasyon değil), `chainsaw lint` + `test --no-cluster` (yapısal doğrulama). 2 örnek claim render edilip `docs/examples-output/`'a kaydedildi. Faz 3'ün cert-manager policy'si ve `03-pki.sh`'i tenant-scoped Vault kaynakları için genişletildi. conventions.md tier tablosu ve networkTier bölümü güncellendi. | (1) KCL'de Python-tarzı `f(x) = ...` fonksiyon tanımı YOK — `lambda x: T -> R { ... }` gerekiyor; ilk deneme `kclvm_cli` ile gerçek hatayla yakalandı. (2) `crossplane render`, bir Claim (`kind: Tenant`) değil, Composition'ın `compositeTypeRef`'iyle eşleşen XR (`kind: XTenant`) bekler — örnek dosyalar (gerçek kullanım şekli olan) Claim kalır, render öncesi bir script XR'a çevirir. (3) `kyverno test` (Faz 2) gibi burada da CLI'yi GERÇEKTEN çalıştırmak (varsayımla YAML yazmak değil) iki gerçek şema/davranış hatasını (yukarıdaki 1 ve 2) daha ORTAYA ÇIKARTMADAN önce fark ettirdi — bu, "yaz-test et-düzelt" döngüsünün üçüncü kez doğrulanan değeri. (4) Görev metninin verdiği tier sayıları ile conventions.md'nin ÖNCEDEN yazılmış tablosu çelişiyordu; en somut/en son yazılan kaynak (bu composition) canonical kabul edilip doküman ona göre güncellendi — tersi değil. |
| 2026-09-14 | Faz 6b (yazım) | Tenant guardrail'leri: 5 Kyverno ClusterPolicy (04-08, hepsi **Enforce**) + RBAC (ClusterRole/ClusterRoleBinding) + 33 test senaryosu yazıldı; hepsi **gerçek `kyverno` CLI 1.19.1 ile YEŞİL**. `compositions/tenant/function.k` PSS etiketleri için güncellendi (08 numaralı politikanın kendi composition'ını kilitlememesi için); `crossplane render` + `verify-sync.sh` tekrar çalıştırılıp doğrulandı. Keycloak realm'ine `platform-tenant-requesters` grubu eklendi. | (1) Kyverno `match.any[].resources.operations` alanı bu CRD sürümünde YOK ("unknown field" hatası) — operasyon scoping'i `preconditions` + `{{ request.operation }}` ile yapılmalı; gerçek CLI hatasıyla yakalandı. (2) `kyverno-test.yaml`'ın `userinfo:` alanı DOKÜMANTASYONDA açık değildi — `UserInfo` fixture şeması (`subjects`→`subject`→doğrusu: `userInfo: {username, groups}`) üç başarısız denemeden sonra CLI hatalarını okuyarak bulundu; ayrıca per-result `userInfo` override YOK, kimlik-bağımlı senaryolar `allowed/`/`denied/` gibi AYRI alt dizinler gerektiriyor. (3) `AnyNotIn` yerine `AllNotIn` operatörü doğru seçim — çok-gruplu bir kullanıcıda (`["diğer-grup","platform-admins"]`) `AnyNotIn` YANLIŞLIKLA reddederdi; gerçek test senaryosuyla (multi-group user) bu ayrım doğrulandı. (4) Kyverno `starts_with()` JMESPath fonksiyonu ServiceAccount prefix eşleşmesi için (rastgele üretilen SA adları, Faz 2/3) çalışıyor — gerçek CLI ile doğrulandı. |
| 2026-09-14 | Faz 7 (yazım) | `XPostgreSQLInstance` XRD + KCL composition (`function.k`, 9-10 kaynak: OBC+Certificate+Cluster+ScheduledBackup+NetworkPolicy+ServiceMonitor+SecretStore+PushSecret+ExternalSecret+opsiyonel-PDB) yazıldı ve **GERÇEK araçlarla test edildi**: `kclvm_cli` (6/6 size×HA kombinasyonu, HA+small reddi dahil), gerçek `crossplane render` (Docker + function-kcl — HA+small kombinasyonu GERÇEKTEN, Docker container'ı içinde reddedildi, `exit=1`), `chainsaw lint` + `test --no-cluster`. `03-pki.sh`'e `kv/` KV v2 mount + `eso-tenant-secrets` Vault rolü eklendi. Faz 6'dan beri eksik olan bir boşluk (XRD/Composition'ların hiçbir ArgoCD Application'a bağlı olmaması) fark edilip KAPATILDI — yeni `04-compositions.yaml.tpl` her iki composition'ı da GitOps'a bağlıyor. | (1) KCL'de C-tarzı `cond ? a : b` YOK — Python-tarzı `a if cond else b` gerekiyor; `_pdbItems` alanında gerçek derleyici hatasıyla yakalandı (Tenant composition'ında da aynı hatayı yapmamıştım çünkü orada ternary kullanmamıştım — bu, aynı hatanın farklı bir composition'da YENİDEN düşülebileceğini gösterdi, "bir kez öğrenildi" güvencesi vermiyor). (2) ESO'nun `PushSecret`/`ExternalSecret`/`SecretStore` CRD şemaları DOKÜMANTASYONDAN DEĞİL, `helm template`'in ürettiği GERÇEK CRD OpenAPI şemasından çıkarıldı (`data[].match.remoteRef.remoteKey` zorunlu, `secretKey` opsiyonel → tüm secret'ı tek blob olarak push etmek mümkün) — üçüncü kez doğrulanan aynı ders: CLI/CRD şemasını varsaymak yerine gerçek kaynaktan okumak hataları PR'dan önce yakalıyor. (3) Vault Kubernetes-auth'ta `serviceAccountRef` boş bırakılırsa ESO OTOMATİK olarak kendi controller kimliğini kullanıyor ("the one bound to the controller will be used") — bu da CRD şemasından, varsayılmadan, doğrulandı. (4) Faz 6'nın XRD/Composition'larının HİÇBİR ArgoCD Application'a bağlı olmadığı bu fazda fark edildi — önceki iki fazın (6, 6b) kapsamı dar tutulduğu için gözden kaçmıştı; bu, "her fazda önceki fazın GitOps entegrasyonunu da kontrol et" şeklinde genel bir ders. |

---

## 6. Bu dosya nasıl güncellenir

1. Faz bitiminde **Bölüm 1**'e kurulan bileşenleri, gerçek sürüm ve doğrulama komutuyla ekle.
2. **Bölüm 2**'de biten satırı ✅ yap, bir sonrakini "sıradaki" olarak işaretle.
3. Faz sırasında çıkan yeni soruları **Bölüm 3**'e, kestirmeleri **Bölüm 4**'e yaz.
4. **Bölüm 5**'e tek satırlık günlük kaydı ekle.
5. Baştaki "Son güncelleme" ve "Aktif faz" alanlarını güncelle.
6. Değişikliği ayrı bir commit olarak at: `docs: PLATFORM_CONTEXT güncellendi (Faz N)`.
